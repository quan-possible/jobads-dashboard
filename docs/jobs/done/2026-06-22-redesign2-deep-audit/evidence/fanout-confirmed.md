### [1] _kpis() YoY reference uses positional series[-13] which is wrong when months have gaps
- area=api-queries kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: api/queries.py:383  cat=aggregation-math / date-windows
- what: yoy_ref = series[-13] assumes the postings series has exactly one row per calendar month, so the 13th-from-last element is always the same month one year prior. In the real data, 355 three-way scope combinations (province + occupation + industry) have fewer than 13 contiguous monthly rows. For the example scope NS + Manufacturing/utilities + Real estate, the 13 available months span 2020-01 to 2025-06 with large gaps (e.g. no rows from 2022-10 to 2024-02). For that scope series[-13]=2020-01 and series[-1]=2025-06, so active_yoy_pct compares five and a half years, not one year, and is shown in the UI as 'year over year'. By contrast, _rank_dim correctly computes year_ago = date(as_of.year-1, as_of.month, 1) and queries that specific date.
- root: _series_frame fetches all available months for the scope without gap-filling zeros for missing months. _kpis blindly indexes by position (series[-13]) rather than finding the row whose month matches (as_of - 12 months).
- fix: Replace the positional index with an explicit lookup: after computing as_of, find a series entry whose month string equals _iso(date(as_of.year-1, as_of.month, 1)). If no such entry exists, set yoy_ref = None. Example: `yoy_ref = next((p for p in series if p.month == _iso(date(as_of.year-1, as_of.month, 1))), None)`.
- verdict: confirmed | The finding is real and accurately described. In `_kpis()` at api/queries.py:383, `yoy_ref = series[-13] if len(series) >= 13 else None` relies on positional indexing. The series is built in `postings_series()` from `_series_frame()`, which queries `filter_cube` filtered only by province/occupation/industry scope — no month-gap-filling is applied. For narrow three-way scopes, the parquet data can have months with zero postings simply absent (no rows), so `series[-13]` can refer to a month well before 12 months ago. By contrast, `_rank_dim()` (line 337) correctly uses `year_ago = date(as_of.yea
- fix_ok: The proposed fix is sound. `_iso(date(as_of.year-1, as_of.month, 1))` produces the correct "YYYY-MM" string matching the format stored in SeriesPoint.month. Setting yoy_ref = None when the exact month is absent is the right fallback — it results in active_yoy_pct = None (already handled at line 404)

### [2] _long_shares() silently falls back to national scope without any indicator in the response
- area=api-queries kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: api/queries.py:908-918  cat=scope / data-honesty
- what: When the conditions / requirements / language table has no rows for the exact scope (province+occupation+industry combination), _long_shares silently re-queries with a national scope (All Canada / All occupations / All industries) and returns those results. The RequirementsResponse object contains no field indicating the scope was changed, and the echoed scope in the response still shows the user's original filter. Employment type, education, experience, and language distributions could all silently reflect national data while the UI renders them as scope-specific.
- root: The fallback branch at lines 909-918 constructs a nat_scope with all-sentinels and re-runs the query with no indication captured for the caller. The response model has no 'data_scope' or 'scope_fallback' field.
- fix: Return a metadata flag alongside the shares, e.g. add `scope_effective: Scope` or a boolean `national_fallback: bool` to RequirementsResponse (and to the _long_shares return tuple), then set it when the fallback path is taken. The frontend can then show a caveat such as 'Showing national figures — scope too narrow for a reliable estimate'.
- verdict: confirmed | The fallback is real and unambiguous. At api/queries.py:908-918, when the scoped query returns an empty DataFrame, _long_shares silently re-runs with a national sentinel scope (ALL_GEO / ALL_OCC / ALL_IND). The function signature returns only (list[CategoryShare], int) — no fallback flag. The RequirementsResponse model in api/models.py:253-261 has no field for scope_effective, national_fallback, or any similar indicator. The requirements() caller at lines 959-963 passes scope=scope (the original user filters) into the response, so the echoed scope always shows what the user asked for, not what
- fix_ok: Yes. The proposed fix is sound: modify _long_shares to return a third bool (fell_back), thread it through requirements(), add national_fallback: bool (or per-dimension flags) to RequirementsResponse, and have the frontend render a caveat when that flag is set. The scope= field in the response should

### [3] FR locale path parses and re-serializes the full figure JSON but does not translate <extra> hover tag content
- area=api-bridge kind=defect sev=LOW final=LOW conf=high
- loc: api/figures.py:188-191  cat=i18n correctness
- what: _FR_CHROME has 'provisional' mapped to 'provisoire' and '3-month average' mapped to 'moyenne sur 3 mois'. However, the pulse factory at src/jobads_dashboard/viz/figures/pulse.py line 88 uses hovertemplate '...{y:,.0f}<extra>provisional</extra>' where the string inside <extra> tags is 'provisional' embedded as a substring of the full hovertemplate string. _localize_chrome only translates exact-match standalone values; it will not translate 'provisional' when it appears inside a longer hovertemplate string. Separately, the factories use the abbreviation '3-mo avg' (not '3-month average') in <extra> tags, which is not in _FR_CHROME at all. The result is that tooltip extra-tag content stays in English in the French locale while the legend entries for the same series are translated.
- root: _localize_chrome applies _fr() to each string value as a unit. It cannot do substring replacement inside Plotly hover template strings because those strings mix display text with Plotly format directives (%{y:,.0f}, <extra>, etc.) and substring replacement would corrupt the template syntax.
- fix: Add variants for <extra>-tag strings to _FR_CHROME that match the factory-literal strings exactly: e.g. 'provisional' (as a standalone hovertemplate suffix like '<extra>provisional</extra>' where 'provisional' is the standalone string), but note this cannot be done via substring match. The cleanest fix is to move the provisional / avg labels into the Plotly trace name (which IS translated) rather than the <extra> tag, or to pass a locale parameter into the factories so they build hovertemplates with already-translated strings rather than post-processing in the bridge.
- verdict: confirmed | The finding is accurate on every factual claim. 

1. pulse.py line 85 uses hovertemplate `"%{x|%b %Y}: %{y:,.0f}<extra>3-mo avg</extra>"` and line 88 uses `"...%{y:,.0f}<extra>provisional</extra>"`. These are compound strings containing Plotly format directives.

2. `_localize_chrome` (api/figures.py lines 205-218) applies `_fr()` to each string value. `_fr()` does an exact-match lookup in `_FR_CHROME`. A compound hovertemplate string like `"%{x|%b %Y}: %{y:,.0f}<extra>provisional</extra>"` is never in `_FR_CHROME`, so it passes through unchanged. The code comment at lines 115-119 in api/figur
- fix_ok: The proposed fix is sound in principle. Moving `provisional` and `3-mo avg` from `<extra>` tags into the trace `name` field is the simpler path — `_localize_chrome` already iterates dict string values and would pick up legend/name strings. The alternative of threading `locale` into factories (with R

### [4] Missing POSTING_LOOKUP file produces unhandled DuckDB error (500) rather than a clean 503/404 in production
- area=api-bridge kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: api/private.py:23  cat=error handling / deployment
- what: The Dockerfile explicitly excludes posting_lookup.parquet ('The private posting_lookup.parquet is excluded via .dockerignore'). In production, unless JOBADS_POSTING_LOOKUP env var is set to a mounted volume, _resolve_posting_lookup() returns a fallback path on the developer's local machine ('/Volumes/ACLMR/...'). Any call to postings() or posting_detail() then calls core.query_df() with a read_parquet() against a non-existent path, causing DuckDB to raise a duckdb.IOException. FastAPI converts this to an uncaught 500 with the internal DuckDB exception message exposed in the JSON error body, leaking file-system path information to the client.
- root: _lookup() at line 23 constructs the SQL expression unconditionally. There is no pre-flight check that the file exists before any query that uses it. The auth layer requires a valid session before reaching these endpoints, which limits exposure to authenticated users, but the 500 + path leak is still a defect.
- fix: Add a startup or first-call check: `if not core.POSTING_LOOKUP.exists(): raise HTTPException(status_code=503, detail='Posting lookup unavailable on this deployment.')` at the top of postings() and posting_detail() in private.py. Alternatively, add a health sub-check or a dependency that guards the private router at startup. This converts the opaque DuckDB 500 into a clean, consistent 503 with no path information in the response.
- verdict: confirmed | The finding is accurate on all key points. _resolve_posting_lookup() in api/core.py (lines 38-49) checks JOBADS_POSTING_LOOKUP env var, then the local DATA_DIR path (which maps to /app/data/derived/labor_market_dashboard_v1/ in the container), then falls back to a hardcoded /Volumes/ACLMR/... path. The .dockerignore at line 14 explicitly excludes posting_lookup.parquet from the image. In a production container without a mounted volume or JOBADS_POSTING_LOOKUP env override, local.exists() returns False and the hardcoded fallback path is stored in core.POSTING_LOOKUP at module import time (core.
- fix_ok: Yes. Adding `if not core.POSTING_LOOKUP.exists(): raise HTTPException(status_code=503, detail='Posting lookup unavailable on this deployment.')` at the top of both postings() and posting_detail() in api/private.py is correct and sufficient. Alternatively, a FastAPI lifespan check or a shared depende

### [5] No rate limiting on POST /api/auth allows unbounded brute-force
- area=api-auth kind=defect sev=MEDIUM final=LOW conf=high
- loc: api/routers/private.py:60-73  cat=auth
- what: The login endpoint has no rate limiting, no account lockout, no per-IP throttle, and no delay on failure. PBKDF2 with 240k iterations costs roughly 0.1 s of CPU per attempt on one worker, but uvicorn/gunicorn can run multiple concurrent workers, which means concurrent attackers bypass that natural slowdown entirely and can test passwords at full worker parallelism.
- root: No middleware (e.g. slowapi, redis-based limiter) or application-level counter is attached to the route. FastAPI's dependency injection makes it easy to add, but was never added.
- fix: Add a per-IP rate limit to POST /api/auth — e.g. slowapi with a Redis or in-memory store: 5 attempts per minute per IP with exponential backoff on repeat failures. Alternatively, add an explicit asyncio.sleep(0.5) on every failed attempt in the route handler as a cheap minimal defence while a proper limiter is set up.
- verdict: confirmed | The finding is factually accurate that no rate limiting exists: api/routers/private.py lines 60-73 show the POST /api/auth handler with no rate limit dependency, no asyncio.sleep on failure, and no per-IP counter. api/main.py confirms no rate-limiting middleware is registered — only CORSMiddleware. No slowapi or equivalent appears anywhere in the API codebase. The core claim is correct.

However, the root-cause framing that "uvicorn/gunicorn can run multiple concurrent workers" overstates the threat for this actual deployment. docker-entrypoint.sh launches `python -m uvicorn api.main:app` with
- fix_ok: Yes — the proposed fix is sound. Both options (slowapi per-IP limiter or asyncio.sleep on failed attempts) are applicable to this FastAPI codebase. The simplest approach is an asyncio.sleep(0.5) on the failure branch inside the login handler (private.py lines 67-70), which costs no new dependency an

### [6] Logout delete_cookie omits secure= flag, failing to clear Secure cookie in some browsers
- area=api-auth kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: api/routers/private.py:78  cat=auth
- what: The set_session_cookie helper (line 29-38) sets the cookie with secure=_COOKIE_SECURE. In production with JOBADS_API_COOKIE_SECURE=true, the cookie has the Secure attribute. The logout handler calls response.delete_cookie(key=auth.COOKIE_NAME, path='/') without passing secure=True. Starlette's delete_cookie defaults to secure=False. Safari and Chrome (in some versions) treat Secure-flagged cookies and non-Secure cookies as separate namespaces: the delete_cookie call creates an unrelated non-Secure cookie set-to-expire, leaving the original Secure session cookie intact. The user appears logged out to the frontend but the session cookie is still valid.
- root: The cookie was set with a conditional Secure attribute but the deletion call does not mirror that attribute. The fix used for set is not symmetrically applied in delete.
- fix: Pass the same flags to delete_cookie that were used at creation: response.delete_cookie(key=auth.COOKIE_NAME, path='/', secure=_COOKIE_SECURE, httponly=True, samesite='lax'). Starlette's delete_cookie accepts all these parameters.
- verdict: confirmed | The code at api/routers/private.py:78 calls response.delete_cookie(key=auth.COOKIE_NAME, path="/") with no secure=, httponly=, or samesite= arguments. Starlette's delete_cookie (confirmed in the project's own venv at .venv/lib/python3.12/site-packages/starlette/responses.py:139) defaults secure=False and httponly=False. The _set_session_cookie helper at lines 29-38 sets the cookie with secure=_COOKIE_SECURE, httponly=True, samesite="lax". When JOBADS_API_COOKIE_SECURE=true in production, the cookie is created with the Secure attribute. The delete_cookie call issues a Set-Cookie with max_age=0 
- fix_ok: Yes. Pass the same attributes used at creation: response.delete_cookie(key=auth.COOKIE_NAME, path="/", secure=_COOKIE_SECURE, httponly=True, samesite="lax"). This mirrors the Secure/HttpOnly/SameSite flags so the browser's cookie-namespace matching succeeds and the session cookie is actually cleared

### [7] PBKDF2 minimum accepted iterations (10,000) is far below OWASP 2023 recommendation
- area=api-auth kind=defect sev=LOW final=LOW conf=high
- loc: api/auth.py:35-36  cat=auth
- what: PASSWORD_HASH_MIN_ITERATIONS = 10_000 and PASSWORD_HASH_MAX_ITERATIONS = 1_000_000. A hash stored in JOBADS_DASHBOARD_PASSWORD_HASH with 10,001 iterations passes validation. OWASP's 2023 password storage cheatsheet recommends 600,000 iterations for PBKDF2-SHA256. At 10,000 iterations a modern GPU can test ~10 million hashes/second against a leaked hash, making an offline attack fast. The server-configured default (PASSWORD_HASH_ITERATIONS = 240_000) is reasonable but the acceptance floor is too low.
- root: The minimum was set liberally to match the original Streamlit app's hash format compatibility rather than a current security baseline.
- fix: Raise PASSWORD_HASH_MIN_ITERATIONS to at least 100_000 (a conservative floor that still accepts older hashes if needed) and document in the validator message that hashes below that count will be rejected with a clear error. Update PASSWORD_HASH_ITERATIONS to 600_000 to match current OWASP guidance for newly generated hashes.
- verdict: confirmed | The code at api/auth.py lines 35-36 is exactly as described: PASSWORD_HASH_MIN_ITERATIONS = 10_000 and PASSWORD_HASH_MAX_ITERATIONS = 1_000_000. A hash stored in JOBADS_DASHBOARD_PASSWORD_HASH with as few as 10,001 iterations will pass _verify_against_hash (line 56 checks only that iterations falls within that range). The server's own default for newly generated hashes (PASSWORD_HASH_ITERATIONS = 240_000, line 34) is more reasonable, but the acceptance floor is factually below OWASP 2023's 600,000 recommendation for PBKDF2-SHA256, and well below even a conservative 100,000 floor. The comment a
- fix_ok: Partially sound. Raising PASSWORD_HASH_MIN_ITERATIONS to 100_000 and PASSWORD_HASH_ITERATIONS to 600_000 closes the gap, but will silently reject any existing hash generated at the old 10,000–99,999 iteration range. The fix should also add a clear error message when the stored hash fails the minimum

### [8] Plain-text password path (JOBADS_DASHBOARD_PASSWORD) accepted silently in production with no warning
- area=api-auth kind=defect sev=LOW final=LOW conf=high
- loc: api/auth.py:109-114  cat=auth
- what: If an operator sets JOBADS_DASHBOARD_PASSWORD instead of JOBADS_DASHBOARD_PASSWORD_HASH, verify_password silently uses hmac.compare_digest on the raw password string. This skips PBKDF2 stretching entirely, making offline brute-force against any logged password trivially fast. There is no startup log message, no warning in the /api/auth status endpoint, and no indication to the operator that they are running in an insecure mode.
- root: The plain-password path was designed for local dev convenience (documented in the docstring) but the code does not distinguish dev from production, nor emit any runtime signal.
- fix: At startup (e.g. in a FastAPI lifespan handler or at import time), log a prominent WARNING if JOBADS_DASHBOARD_PASSWORD is set: 'Running with plain-text password — do not use in production; set JOBADS_DASHBOARD_PASSWORD_HASH instead'. Optionally also surface this in AuthStatus.configured as a separate configured_with_hash bool so the frontend can display a warning.
- verdict: confirmed | api/auth.py lines 109-114 confirm the described behaviour exactly. When JOBADS_DASHBOARD_PASSWORD_HASH is absent, verify_password falls through to the plain-text env var (or Keychain) and calls hmac.compare_digest on the raw strings, skipping PBKDF2 entirely. There is no import-time warning, no lifespan hook, no AuthStatus field exposing the active mode, and no runtime signal of any kind. The module docstring labels this path "convenient for local dev", which is clear intent, but the code does nothing to enforce or surface that intent at runtime. An operator who sets JOBADS_DASHBOARD_PASSWORD 
- fix_ok: Yes. Logging a WARNING at startup (e.g. in a FastAPI lifespan handler or at module import time) when JOBADS_DASHBOARD_PASSWORD is set and JOBADS_DASHBOARD_PASSWORD_HASH is not is the right fix. Optionally adding a configured_with_hash bool to any AuthStatus response would let the frontend surface th

### [9] No minimum length enforcement on JOBADS_API_SESSION_SECRET
- area=api-auth kind=defect sev=LOW final=LOW conf=high
- loc: api/auth.py:125  cat=auth
- what: _SESSION_SECRET is loaded as-is from JOBADS_API_SESSION_SECRET: any non-empty string, including 'a' or '1', is accepted as the HMAC signing secret. A weak secret makes session tokens forgeable by brute-force HMAC key search.
- root: The env-var loader applies no length or entropy validation. The fallback path (secrets.token_hex(32)) generates a strong 256-bit secret, but this path only activates when the env var is absent or empty.
- fix: At import time, after resolving _SESSION_SECRET, assert or log a warning if len(_SESSION_SECRET) < 32 (i.e. fewer than 32 ASCII characters, well below 256 bits of entropy even for random strings). Consider rejecting obviously weak secrets like 'test', 'dev', 'secret' and forcing a restart.
- verdict: confirmed | Line 125 of api/auth.py reads: `_SESSION_SECRET = os.environ.get(SESSION_SECRET_ENV, "").strip() or secrets.token_hex(32)`. The `or` short-circuit means any non-empty string — including a single character — passes straight through; `secrets.token_hex(32)` only fires when the env var is absent or blank. The finding's factual description is exactly right. No length, entropy, or blocklist check exists anywhere in the file. The proposed fix (warn or raise at import time when `len(_SESSION_SECRET) < 32`) is sound and standard. Severity LOW is appropriate: this is an internal, single-operator tool; 
- fix_ok: Yes. Adding `if len(_SESSION_SECRET) < 32: warnings.warn(...)` or raising a `ValueError` after line 125 is the standard hardening pattern and does not change any other behavior. The threshold of 32 ASCII characters (256 bits of key material for a random string, or a reasonable minimum for a passphra

### [10] classical_decompose: 2x12 MA trend is shifted 1 month early
- area=viz-compute kind=defect sev=HIGH final=MEDIUM conf=high
- loc: src/jobads_dashboard/viz/compute.py:195  cat=Economics math correctness
- what: For an even period (the only case where this branch runs), `rolling(12, center=True)` in pandas places its window as [t-6, t+5] — left-biased by half a period, centering at t-0.5. The subsequent `rolling(2, center=True)` is also left-biased, averaging step1[t-1] and step1[t] and placing its center at t-1. The compound result is a trend that is systematically 1 full month earlier than the true centred 2×12 MA. For a linear series s[i]=i the code gives trend[12]=11.0 where the correct 2×12 MA gives 12.0. Because detr = s − trend, every detrended value, every seasonal estimate, and every residual carries this 1-month phase error. In practice a peak or trough that occurred in February will appear to have occurred in January, and a chart labelled 'March anomaly' may actually reflect February data.
- root: Pandas `rolling(w, center=True)` for even `w` applies a left-biased window ([t-w/2, t+w/2-1]). The second `rolling(2, center=True)` is also left-biased. Both shifts compound in the same direction — they do not cancel — producing a net 1-month left shift in the trend rather than a centred result.
- fix: Replace line 195: `trend = trend.rolling(2, center=True, min_periods=1).mean()` with `trend = (trend + trend.shift(-1)) / 2`. This averages step1[t] (centered at t-0.5) with step1[t+1] (centered at t+0.5), correctly centring the 2×12 MA at t with no phase shift.
- verdict: confirmed | The pandas rolling behaviour claimed by the reporter is numerically verified: `rolling(12, center=True)` on an even window places the window as [t-6, t+5] (left-biased by half a period), and the subsequent `rolling(2, center=True)` averages step1[t-1] and step1[t], shifting the result one more step left. Both shifts compound in the same direction. On a linear series s[i]=i the code produces trend[12]=11.0 where the correct 2x12 MA gives 12.0 — exactly the 1-month left bias claimed. The proposed fix `(trend + trend.shift(-1)) / 2` correctly averages step1[t] and step1[t+1], centring at t, and y
- fix_ok: Yes. The proposed replacement `trend = (trend + trend.shift(-1)) / 2` at line 195 of src/jobads_dashboard/viz/compute.py is mathematically correct. It averages step1[t] (centred at t-0.5) with step1[t+1] (centred at t+0.5), placing the result at t with no phase shift. Verified numerically on a linea

### [11] index_to_base: 'if not base' treats a genuine zero as missing, falling back to a potentially-zero first value and producing inf/NaN
- area=viz-compute kind=defect sev=LOW final=LOW conf=high
- loc: src/jobads_dashboard/viz/compute.py:43  cat=Pandas pitfall / NaN propagation
- what: The guard `if not base or np.isnan(base)` fires not only for NaN (the intended case: no data in base_year) but also for `base == 0.0` (the case where a group genuinely had zero postings in the base year, e.g. a new occupation not yet tracked). When the guard fires due to a zero mean it falls back to `g[value].iloc[0]`, which may also be 0.0 (the first row in the group is still a zero-posting period), leaving the divisor at 0 and producing `inf` or `NaN` values throughout the index column for that group. These silent infinities propagate into any chart that calls `index_to_base`.
- root: Python's truthiness rules: `not 0.0` is `True`, so the guard cannot distinguish 'genuinely zero base' from 'missing data'. The fallback `iloc[0]` repeats the same zero value rather than providing a valid non-zero fallback.
- fix: Replace the guard at line 43 with `if pd.isna(base):` (check only for NaN). If zero base is also considered indexable (all-zero group), add a separate guard: after computing `base`, if `base == 0` and the fallback is also 0, return a Series of NaN explicitly rather than dividing by zero. Example: `if pd.isna(base): base = first_nonzero or return NaN series`.
- verdict: confirmed | The code defect is real. At compute.py:43, `if not base or np.isnan(base)` uses Python truthiness on a float: `not 0.0` evaluates to `True`, so the guard fires for a genuine zero base-year mean, not only for NaN. The fallback `g[value].iloc[0]` may also be 0.0 (the first row of the group is a zero-posting period), leaving the divisor at zero. NumPy float division by zero produces `inf` for nonzero numerators and `NaN` for 0/0. The proposed fix — replacing the guard with `if pd.isna(base):` — is sound and precise.

However, the claimed severity is overstated. Both actual call sites of `index_to
- fix_ok: Yes. The proposed fix `if pd.isna(base):` at line 43 of src/jobads_dashboard/viz/compute.py is correct and precise. It replaces the Python truthiness check with an explicit NaN test, which is the only case that the guard is actually intended to handle (no data for the base year). No changes to the f

### [12] review.py crashes at runtime: 12 figure functions it calls do not exist
- area=viz-figures kind=defect sev=HIGH final=HIGH conf=high
- loc: src/jobads_dashboard/viz/review.py:53–108  cat=runtime error
- what: review.py's `_pages()` manifest references 12 figure factory functions that have never been implemented: `pulse.stl_panel`, `pulse.anomaly_flags`, `pulse.sa_vs_nsa`, `pulse.cycle_plot`, `geography.bubble_map`, `geography.share_choropleth`, `geography.lq_choropleth`, `geography.lq_heatmap`, `geography.province_tile_grid`, `occupations.bump_chart`, `occupations.concentration_trio`, `occupations.horizon_wall`. `build_html()` calls each `it.fn(ds)` without a guard, so it raises `AttributeError: module 'pulse' has no attribute 'stl_panel'` at the seventh item of Section 1 — before rendering a single geography or occupations chart.
- root: The review manifest was written ahead of implementation as a design target; the factory files were later written with a different function set and the manifest was never reconciled. There is no `try/except`, `hasattr`, or lazy-import guard in `build_html()`.
- fix: Either implement the missing 12 functions or remove their `Item(...)` lines from `_pages()`. Simultaneously, add the 10 factory functions that exist but are absent from the manifest (`pulse.occupation_trends_grid`, `pulse.momentum`, `geography.demand_map`, `geography.cma_demand`, `geography.ai_exposure_map`, `occupations.skill_churn`, `occupations.ai_exposure_scatter`, `pay.education_wage_proxy`, `pay.wage_by_education`, `skills.skill_occupation_heatmap`, `skills.top_skills_trend`, `skills.ai_skill_diffusion`).
- verdict: confirmed | All 12 function names cited in the finding are genuinely absent from their respective modules. pulse.py exports: kpi_row, demand_ribbon, yoy_bars, seasonality_heatmap, composition_area, diffusion_index, occupation_trends_grid, momentum — none of stl_panel, anomaly_flags, sa_vs_nsa, cycle_plot exist. geography.py exports: demand_map, ranked_provinces, cma_demand, shift_share_bars, yoy_choropleth, ai_exposure_map — none of bubble_map, share_choropleth, lq_choropleth, lq_heatmap, province_tile_grid exist. occupations.py exports: treemap, indexed_lines, contribution_bars, waterfall, dumbbell, noc_
- fix_ok: Yes. The fix is sound: remove the 12 Item(...) lines referencing non-existent functions from _pages() (or implement those functions). The optional step of adding the 10 existing-but-absent functions (pulse.occupation_trends_grid, pulse.momentum, geography.demand_map, geography.cma_demand, geography.

### [13] wage_band: P75 value is invisible in the unified hover tooltip
- area=viz-figures kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: src/jobads_dashboard/viz/figures/pay.py:36–37  cat=encoding correctness / hover UX
- what: The P75 trace is added with `hoverinfo='skip'`. The global template sets `hovermode='x unified'`, which shows all non-skipped traces at the hovered x. Because P75 is skipped, the unified tooltip shows only P25 and Median wages — the upper bound of the advertised wage band is completely absent from interactive inspection. Users see the shaded band visually but cannot read the P75 value on hover.
- root: `hoverinfo='skip'` was used to prevent the P75 trace from appearing as a labelled row in the hover box, but it also suppresses the value. The distinction between suppressing the trace name and suppressing the value was not made.
- fix: Replace `hoverinfo='skip'` on the P75 trace with `hovertemplate='%{x|%b %Y} · P75: %{y:$,.0f}<extra></extra>'`. This adds the P75 value as a properly labelled row in the unified tooltip, parallel to the P25 row directly below it.
- verdict: confirmed | Both factual premises are verified in the source. theme.py line 138 sets hovermode="x unified" in the global template. pay.py line 37 applies hoverinfo="skip" to the P75 trace. In x unified mode, hoverinfo="skip" fully suppresses a trace from the tooltip, so users see P25 and Median in the hover box but not P75 — the upper bound of the named P25–P75 band is invisible on hover. The chart title and subtitle explicitly advertise the P25–P75 band, so omitting P75 from the tooltip is a real asymmetry, not an intentional design choice. The proposed fix (replacing hoverinfo="skip" with a hovertemplat
- fix_ok: Yes. Replace hoverinfo="skip" on the P75 trace (pay.py line 37) with hovertemplate="%{x|%b %Y} · P75: %{y:$,.0f}&lt;extra&gt;&lt;/extra&gt;". This matches the format of the P25 hovertemplate on line 42 and adds the upper-bound value as a correctly labelled row in the unified tooltip.

### [14] Multiple time-series trend charts missing the provisional tail band
- area=viz-figures kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: src/jobads_dashboard/viz/figures/skills.py:25–46 (top_skills_trend), 49–69 (ai_skill_diffusion); also pulse.py:128–149 (composition_area)  cat=honesty chrome correctness
- what: `top_skills_trend`, `ai_skill_diffusion` (skills.py) and `composition_area` (pulse.py) are all time-series charts that extend to the latest data month (which is post-2025 and therefore provisional per `PROVISIONAL_FROM = 2025-01-01`). None of them call `add_provisional_band()`. By contrast, the other trend charts on the same pages — `demand_ribbon`, `yoy_bars`, `momentum` — all add the provisional band. This inconsistency makes it appear that skill and composition data is confirmed when it is not.
- root: The provisional band was added to time-series charts one at a time as they were written. The skills and stacked-area charts were left without it, likely overlooked because their data is not a simple national total.
- fix: Add `add_provisional_band(fig)` (or `add_provisional_band(fig, label=False)` for compactness) to `top_skills_trend`, `ai_skill_diffusion`, and `composition_area`. Update the subtitles of each to mention 'dotted region = provisional (2025+)'.
- verdict: confirmed | All three cited functions are missing the provisional band call. In skills.py, `add_provisional_band` is not even imported (line 16 imports only `add_covid_band` and `add_reference_line`). Both `top_skills_trend` (lines 25–46) and `ai_skill_diffusion` (lines 49–69) call `add_covid_band` but never `add_provisional_band`. In pulse.py, `add_provisional_band` is imported (line 15), but `composition_area` (lines 128–149) calls only `add_covid_band(fig, label=False)` and omits `add_provisional_band`. The contrast with `demand_ribbon`, `yoy_bars`, and `momentum` — all of which do call `add_provisiona
- fix_ok: Yes. The proposed fix is correct and sufficient: (1) add `add_provisional_band` to the import in skills.py line 16, then call `add_provisional_band(fig)` before each `return titled(...)` in `top_skills_trend` and `ai_skill_diffusion`; (2) call `add_provisional_band(fig)` (or `add_provisional_band(fi

### [15] noc_naics_heatmap strips label from noc_name that has no pipe separator
- area=viz-figures kind=defect sev=LOW final=LOW conf=high
- loc: src/jobads_dashboard/viz/figures/occupations.py:34, 98, 143  cat=label mapping
- what: Several factories apply `lambda s: s.split('|')[-1].strip()` to the `noc_name` column (e.g. `_treemap_trace` line 34, `contribution_bars` line 98, `dumbbell` line 143). But `noc_name` is populated via `NOC_SHORT.get(code, 'Unknown')` in `datasource.py`, producing plain names like `'Sales & service'` — no pipe separator. The `split('|')[-1]` on such a string returns the whole string unchanged, so the label is visually correct. However, the lambda silently does nothing and will mislabel data if the column origin ever changes to the raw `'code | label'` scope string. The same issue exists in `industries.py` (lines 45, 107) for `naics_name`.
- root: The `split('|')[-1]` pattern was written to parse raw scope strings (`'3 | Health occupations'`), but the datasource already delivers the pre-mapped short name via `NOC_SHORT`/`NAICS_SHORT`. The factory code was not updated when the datasource convention changed.
- fix: Remove the redundant `split('|')[-1].strip()` lambda. Use `g['noc_name']` (or `c['noc_name']`) directly as the label since it is already the short display name. Add a comment making clear that the column is pre-mapped.
- verdict: confirmed | The finding is real but its title is misleading. The `split('|')[-1].strip()` lambda is confirmed redundant: `datasource.py` lines 84, 92, 127, 171, 304, 322–323 show that `noc_name` and `naics_name` are populated via `NOC_SHORT`/`NAICS_SHORT` dict lookups, delivering plain short names like `'Sales & service'` with no pipe separator. Splitting on `|` returns a single-element list and `[-1]` gives back the entire string unchanged — the lambda is a confirmed no-op on current data. This matches what the finding says: "the label is visually correct."

However, the finding's title says "noc_naics_h
- fix_ok: The proposed fix is sound. Removing `lambda s: s.split('|')[-1].strip()` and using the column value directly (with a comment noting it is already a pre-mapped short name from NOC_SHORT/NAICS_SHORT) is correct and safe. Also covers the unlisted occurrences: occupations.py:118 (waterfall `c["short"]`)

### [16] FR locale: KPI wage tile renders double dollar sign ($25.50 $/h)
- area=web-lib kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/lib/format.ts:32  cat=i18n / display
- what: fmtWage always prepends a hard-coded "$" prefix (line 32: `$${NF1.format(n)}`). The FR i18n dict sets kpiWageUnit to " $/h" (page-pulse.ts line 127) which also contains "$". KpiTile renders value then unit inline, so in FR mode the KPI tile shows "$25.5 $/h" — a double dollar sign with a mixed EN/FR format.
- root: format.ts formatters are locale-agnostic singletons. fmtWage hardcodes an English currency prefix, but the FR unit string was written in the French convention (amount-space-symbol) and also includes "$". There is no locale parameter threading through the formatter.
- fix: Either (a) change fmtWage to accept a locale and omit the "$" prefix for FR (returning "25,50" then the unit supplies " $/h"), or (b) change the FR kpiWageUnit to "/h" (no dollar) so it composes correctly with the existing fmtWage output. Option (b) is the minimal fix: in page-pulse.ts change `kpiWageUnit: " $/h"` to `kpiWageUnit: " /h"`. PostingDrawer.tsx line 20 has a related issue: it hardcodes "/hr" suffix in English regardless of locale.
- verdict: confirmed | The double dollar sign bug is real and traceable through three files. fmtWage (format.ts:32) unconditionally returns `$${NF1.format(n)}` — it always prepends "$". The FR locale dict (page-pulse.ts:127) sets kpiWageUnit to " $/h". page.tsx:121-122 passes fmtWage(kpis.median_wage) as value and t.kpiWageUnit as unit to KpiTile. KpiTile (KpiTile.tsx:42-43) renders value then unit inline with no separator, producing "$25.5 $/h" in FR mode. The EN kpiWageUnit is "/hr" (line 22) which composes correctly because it has no "$". The FR string was written in French convention (amount then symbol) but fmt
- fix_ok: Yes. Option (b) is sound: change page-pulse.ts line 127 from `kpiWageUnit: " $/h"` to `kpiWageUnit: " /h"`. This makes the FR tile display "$25.5 /h" which, while not perfectly following the French convention of placing the currency symbol after the amount, is unambiguous, correct, and consistent wi

### [17] fmtMonth produces English month names in all locales ('Jun 2026' in FR mode)
- area=web-lib kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/lib/format.ts:40-45  cat=i18n / display
- what: The MONTHS array (line 40) and both fmtMonth / fmtMonthShort functions are locale-agnostic. In FR mode, every page eyebrow, every figure "As of" stamp (Figure.tsx line 35), and every date label in the method page renders with English month abbreviations: "En date de Jun 2026" instead of "En date de juin 2026". The pulseDict FR has a monthsShort array (page-pulse.ts line 160: "Janv", "Févr", …) but it is never wired to fmtMonth and is unused.
- root: format.ts has no locale parameter; it was written as a locale-agnostic utility. FR month names exist in the i18n dict but are isolated there and not reachable by the shared formatter.
- fix: Add a locale parameter to fmtMonth (and optionally fmtMonthShort): `fmtMonth(iso, locale: Locale = 'en')`. Supply a FR months array (or use `Intl.DateTimeFormat(locale, { month: 'short' })` to avoid a hand-maintained list). Then thread the locale from server components (available via getLocale()) and from client components (via useI18n().locale) to each call site. There are about 12 call sites across app/ pages and Figure.tsx.
- verdict: confirmed | The bug is real and traceable end-to-end in the source.

web/lib/format.ts lines 40-50: the MONTHS array is hardcoded English and fmtMonth/fmtMonthShort accept no locale parameter.

Every call site passes no locale:
- Figure.tsx:35 — the "as of" stamp on every chart card
- layout.tsx:50 — the TopNav "as of" badge
- app/page.tsx:84 — pulse hero eyebrow
- app/industries/page.tsx:61, app/skills/page.tsx:63, app/wages/page.tsx:68, app/occupations/page.tsx:84, app/geography/page.tsx:91 — page hero eyebrows
- app/method/page.tsx:94, 145, 146, 147 — four method-page date spans

The pulseDict FR month
- fix_ok: The proposed fix is sound. Using Intl.DateTimeFormat(locale, { month: 'short', year: 'numeric' }) is preferable to maintaining a hand-rolled FR array, since it removes the need to sync the web/lib/format.ts MONTHS array with the pulseDict monthsShort array (which is already out of sync — the dict ha

### [18] ExploreView: scope change with offset > 0 triggers a stale-page fetch before offset resets
- area=web-lib kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/explore/ExploreView.tsx:44-68  cat=data fetch / state management
- what: Two separate useEffects manage the same logical state: one watches scopeKey and calls setOffset(0) (line 46-48), and another watches [filters.geo, filters.occ, filters.ind, debouncedQ, offset] and fires the fetch (line 50-67). In React 18, when the scope changes while the user is on page 2 (offset=25), both effects are scheduled in the same render. Effect 3 (fetch) fires first with the new scope but the old offset=25, requesting the wrong page for the new scope. Effect 2 then fires, sets offset=0, which triggers Effect 3 again with the correct offset. The cancelled flag prevents completing the first fetch, but two network requests are issued and there is a brief window where loading state is attached to a wrong-scope fetch.
- root: Offset reset and data fetch are decoupled across two effects that both react to scope changes. The canonical fix is to keep offset in the URL (so a scope URL change also resets offset naturally) or to batch both the scope change and offset reset in one state update.
- fix: Merge the offset into scopeKey tracking: instead of a separate effect that calls setOffset(0), reset offset inside the fetch effect itself: check whether scopeKey changed (track it with a ref) and if so, fetch with offset=0 and call setOffset(0) synchronously before the fetch. Alternatively, store offset in the URL query string alongside filters so that any scope navigation automatically resets pagination.
- verdict: confirmed | The two-effect decoupling is real and the stale-fetch is reachable. When a filter changes while offset > 0, both the reset effect (line 46-48) and the fetch effect (line 50-68) are triggered in the same render. React 18 runs effects in declaration order, so the reset effect fires first and queues setOffset(0), but this is a state update — it does not synchronously change the `offset` variable seen by the fetch effect in the same flush. The fetch effect then runs with the old offset value (e.g., 25), issuing a request for the wrong page of the new scope. setOffset(0) then causes a re-render, an
- fix_ok: Yes — the proposed fix is sound. Either approach works: (1) add a ref tracking the last-seen scopeKey inside the fetch effect; if it changed, use offset=0 for the request and call setOffset(0) before fetching, eliminating the separate reset effect entirely; or (2) store offset in the URL query strin

### [19] Expired session in ExploreView shows a dead error with no re-login path
- area=web-lib kind=defect sev=LOW final=LOW conf=high
- loc: web/components/explore/ExploreView.tsx:62-63  cat=auth / UX
- what: When the user's session cookie expires mid-browse, fetchPostings returns a 401, which jsonOrThrow turns into an AuthError with message "Request failed (401)". ExploreView catches this generically (line 62-63) and sets the error string. The AuthGate component, which is the only place that shows the login form, does not re-check auth status when ExploreView reports a 401. The user sees the raw error string but cannot re-authenticate without a full page reload.
- root: AuthGate's phase state never transitions back from "open" once set, and ExploreView does not communicate auth failures back up to AuthGate. There is no callback or event for "session expired" between the two components.
- fix: Add an optional onAuthExpired callback prop to ExploreView. In the catch block, if the caught error is an instanceof AuthError, call onAuthExpired() instead of (or in addition to) setError. In AuthGate, pass `onAuthExpired={() => setPhase('locked')}` to ExploreView so the login form re-appears automatically when the session expires.
- verdict: confirmed | The code fully supports the finding. ExploreView.tsx:63 catches all errors generically (e?.message ?? "Could not load postings.") with no instanceof AuthError check and no upward callback. AuthGate.tsx line 76 renders <ExploreView /> with zero props; ExploreView's function signature accepts no props at all. Once AuthGate sets phase to "open" there is no mechanism — no event, no context, no callback — for a child 401 to transition it back to "locked". A user whose session expires mid-browse sees the raw AuthError message string inside the table cell and has no re-login path short of a full page
- fix_ok: Yes. The proposed fix is sound. Add an optional onAuthExpired?: () => void prop to ExploreView. In the catch block (ExploreView.tsx line 63), check if the error is instanceof AuthError and call onAuthExpired() before or instead of setError. In AuthGate.tsx line 76, render <ExploreView onAuthExpired=

### [20] PostingDrawer hardcodes English '/hr' wage suffix regardless of locale
- area=web-lib kind=defect sev=LOW final=LOW conf=high
- loc: web/components/explore/PostingDrawer.tsx:20  cat=i18n / display
- what: wageLine() (line 20) appends the string literal "/hr" directly: `return \`${fmtWage(d.wage_hourly)}/hr\``. In FR mode this produces "$25.50/hr" — English suffix, English decimal separator, and English currency position — rather than the French convention "25,50 $/h". The wage_unit field from the API (line 24) is used only for the min/max case, not for wage_hourly.
- root: The wageLine helper predates the i18n layer and was not updated when FR support was added. There is no locale context available to it (it is a plain function outside a React component).
- fix: Accept a locale parameter in wageLine or move the suffix string into the explore i18n dict. The FR explore dict already has fWage: "Salaire" (label). Add a wageUnit key: EN "/hr", FR " /h" and pass t.explore.wageUnit to wageLine. Also fix fmtWage to be locale-aware (see finding on fmtMonth) so the decimal and currency placement are correct.
- verdict: confirmed | The finding is confirmed by direct code inspection. In PostingDrawer.tsx line 19–20, wageLine() is a plain function outside the React component with no locale parameter; it appends the hardcoded string "/hr" and calls fmtWage() which is defined in format.ts line 30–33 as always using en-CA Intl.NumberFormat and a leading "$" — both English conventions. The component does have `const { t } = useI18n()` at line 31, but wageLine is invoked at line 66 as wageLine(detail) with no locale argument passed. The FR i18n dict in pages.ts (lines 197–239) has no wageUnit key; fWage at line 230 is only the 
- fix_ok: Sound. Add a wageUnit key to both EN and FR explore dicts (EN: "/hr", FR: " $/h"), accept a locale or unit string parameter in wageLine, and update fmtWage to be locale-aware (FR: comma decimal, trailing " $") so all three parts — decimal separator, currency symbol position, and unit suffix — are co

### [21] PostingDrawer: aria-modal dialog has no focus trap
- area=web-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/explore/PostingDrawer.tsx:69  cat=a11y
- what: The drawer renders with role="dialog" and aria-modal="true", which tells screen readers to ignore background content. However there is no focus trap: no autoFocus on the close button, no tabIndex management, and Tab will freely move to background table rows and page links behind the overlay. A keyboard user who opens the drawer lands focus nowhere inside it and can navigate the entire page behind it.
- root: aria-modal=true is set as a screen-reader hint but nothing enforces the corresponding keyboard behaviour. The Escape key and backdrop click close the drawer correctly, but there is no mechanism to keep focus inside the aside or to move focus there on open.
- fix: On open (when id transitions from null to a value), move focus to the close button or the aside element using a ref + el.focus(). Add a focus-trap loop: on Tab/Shift-Tab, intercept at the boundary and cycle back inside the aside. The close button already has a ref-accessible element at line 88-95. Alternatively use a library such as focus-trap-react or the native `inert` attribute on the background container when the drawer is open.
- verdict: confirmed | The source code at web/components/explore/PostingDrawer.tsx confirms the finding. Line 69 sets role="dialog" aria-modal="true" on the outer div. The two useEffect hooks (lines 36–49 and 51–62) handle data fetching and Escape/overflow respectively — neither moves focus into the drawer on open nor installs a Tab-key trap. The close button at lines 88–95 has no autoFocus attribute and no ref that would let the component call .focus() on mount. There is no focus-trap library import anywhere in the file. The aria-modal="true" attribute is a screen-reader hint that tells ATs to ignore background con
- fix_ok: Yes. The proposed fix is sound. The simplest correct approach is to add autoFocus to the close button at line 88 (or the aside element at line 76 with tabIndex={-1}) so focus moves inside on open, then intercept Tab and Shift+Tab at the drawer boundary to cycle back. Alternatively, apply the `inert`

### [22] ExploreView: <tr role="button"> destroys table-cell semantics for assistive tech
- area=web-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/explore/ExploreView.tsx:155-168  cat=a11y
- what: Each data row is rendered as `<tr role="button" tabIndex={0}>`. Overriding the implicit `row` role with `button` causes all child `<td>` elements to lose their implicit `cell` role context. Screen readers will announce the row as a flat button with concatenated text rather than a navigable table row with labelled columns (Job Title, Employer, Region, etc.). The column headers in <thead> become orphaned.
- root: The interactive row pattern was implemented by adding role=button to the <tr> instead of using a structural approach that preserves table semantics. ARIA 1.2 forbids certain role overrides on table-owned elements; a <tr role="button"> breaks the required row/cell ownership chain.
- fix: Keep the row as a structural `<tr>` (no role override). Make the job title cell contain a visually-styled `<button>` or `<a>` that triggers the drawer. Place `aria-label` on that interactive element rather than the row. Tab focus moves to the cell-level button, table semantics are preserved, and screen reader users still navigate column-by-column.
- verdict: confirmed | Lines 155–168 of web/components/explore/ExploreView.tsx show `<tr role="button" tabIndex={0} aria-label={...}>` surrounding seven `<td>` children. ARIA 1.2 table/grid ownership rules require that `<tr>` carry the implicit `row` role so that its `<td>` children can carry their implicit `cell` roles. Overriding the row's role to `button` breaks the required row→cell ownership chain: the `<td>` elements are now children of a `button`, which is not a valid ARIA owner for `cell`. Conformant screen readers (JAWS, NVDA in virtual-cursor mode) will announce the entire row as a single flat button whose
- fix_ok: Yes. Keep `<tr>` without `role` override. Move `tabIndex={0}`, the click handler, and `aria-label` onto a `<button>` (or `<a>`) inside the job-title `<td>`. Table semantics are restored, column headers remain anchored, and screen readers can navigate cell-by-column. The rest of the row can still res

### [23] TopNav: collapsed mobile nav links remain keyboard-focusable
- area=web-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/TopNav.tsx:110-148  cat=a11y
- what: When menuOpen is false the mobile nav panel is hidden with `max-h-0 overflow-hidden opacity-0`. This removes it visually but does NOT remove links from the tab order — overflow:hidden and max-height:0 have no effect on keyboard focus. A keyboard user pressing Tab while the hamburger menu is closed will silently focus the hidden nav links, appearing to 'jump' to an invisible location.
- root: The hide/show transition uses a CSS max-height animation, which only affects layout. `display:none` (used correctly on the desktop nav via `hidden` class) removes elements from the focus order; `max-h-0 overflow-hidden` does not.
- fix: Conditionally set `aria-hidden={!menuOpen}` on the panel div AND add `tabIndex={menuOpen ? undefined : -1}` to each Link inside, OR set the HTML `inert` attribute on the panel when closed (`inert={!menuOpen || undefined}`). The `inert` approach is simplest and well-supported in all modern browsers: `<div id="mobile-nav-panel" {...(!menuOpen && { inert: '' })} ...>`.
- verdict: confirmed | The finding is technically correct and confirmed by reading the source. On mobile viewports (below the `md` breakpoint), the panel div at line 110 carries `md:hidden` which applies `display:none` only on md+ screens — on mobile the panel is always rendered in the DOM. When `menuOpen` is false, the only hiding mechanism is `max-h-0 overflow-hidden opacity-0` (line 117). CSS `max-height: 0` with `overflow: hidden` is a purely visual/layout effect; it does not remove elements from the tab order or the accessibility tree. The six Link elements inside the panel (lines 125–144) have no `tabIndex={-1
- fix_ok: Yes. Adding `inert={!menuOpen || undefined}` to the panel div (line 110) is the correct and simplest fix. It atomically removes all descendant links from the tab order and accessibility tree when the menu is closed, without altering the CSS transition animation. The fix is accurate and sufficient.

### [24] MapToggle: tablist/tab pattern missing arrow-key navigation and tabpanel role
- area=web-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/MapToggle.tsx:26-48  cat=a11y
- what: The component uses role="tablist" and role="tab" on buttons, but (1) no onKeyDown handler implements the required ArrowLeft/ArrowRight tab-switching behaviour mandated by the ARIA Authoring Practices Guide for tablist; (2) the content panel (RemoteFigure) has role="img" rather than role="tabpanel"; and (3) tabs have no aria-controls pointing to the panel. Screen reader users following ARIA tab patterns will find that arrow keys do nothing and the associated panel is not announced.
- root: The segmented-control UI was styled as a tab widget but the ARIA keyboard contract for tablist was not fully implemented. The APG requires: roving tabindex or arrow-key selection within the tablist, and a matching tabpanel element with an id referenced by aria-controls on each tab.
- fix: Add an onKeyDown on the tablist div handling ArrowLeft/ArrowRight to shift active focus and selection between tabs (roving tabindex pattern). Assign a stable id to the RemoteFigure wrapper (e.g. `map-panel`), set role="tabpanel" on that wrapper, and add `aria-controls="map-panel"` plus a matching `id` attribute on each tab button. Alternatively, if the segmented control is treated as a toolbar/radiogroup rather than a tablist, switch to role="radiogroup" with role="radio" children, which has simpler keyboard semantics.
- verdict: confirmed | Reading MapToggle.tsx lines 26-48 and RemoteFigure.tsx lines 66-74 confirms all three structural gaps the reporter identified.

Point 1 — arrow-key navigation: The tablist div at line 26 and each tab button at line 33 carry ARIA tab roles, but there is no onKeyDown handler anywhere in the component. The APG tablist pattern requires ArrowLeft/ArrowRight to move selection between tabs. This is absent.

Point 2 — role="img" vs role="tabpanel": RemoteFigure renders with role="img" (RemoteFigure.tsx line 70), which is appropriate when that component is used in isolation. The reporter's claim that i
- fix_ok: Directionally sound. Adding onKeyDown with ArrowLeft/ArrowRight roving-tabindex logic on the tablist div is the right fix for gap 1. For gap 3, wrapping the RemoteFigure call in MapToggle (line 47) with a div carrying role="tabpanel" and a stable id, then adding aria-controls on each tab button refe

### [25] ExploreView: double-fetch fired when filter changes on any page after the first
- area=web-components kind=defect sev=LOW final=LOW conf=high
- loc: web/components/explore/ExploreView.tsx:46-68  cat=React correctness
- what: When a filter changes while offset > 0, two separate useEffect runs fire: one because filters are in the fetch effect's dep array (fires with old offset), and one after scopeKey resets offset to 0. The first fetch is immediately cancelled (cancelled=true from its own cleanup), so no stale data lands, but a wasted credentialed request is sent to the API on every filter change from page 2+.
- root: The scope-reset effect (setOffset(0) on scopeKey change) and the fetch effect share overlapping dependencies. Because offset is also a fetch dep, changing filters triggers the fetch effect once with the stale offset before the reset takes effect in the next render.
- fix: Remove offset from the fetch effect's dep array and instead read it from a ref that is updated synchronously in the pagination handler and also reset in the scopeKey effect. Or: unify the two effects — compute the new offset inside the fetch effect itself by checking whether scopeKey changed (use a ref to track the previous scopeKey), resetting to 0 before fetching when it differs. This eliminates the intermediate render that fires the stale fetch.
- verdict: confirmed | The double-fetch is real and the reporter's mechanical analysis is correct. When a filter changes while offset > 0: (1) both effects are scheduled in the same React commit because scopeKey (a derived value computed inline at render time, not inside an effect) and filters.geo/occ/ind/debouncedQ both change together; (2) Effect 1 (lines 46-48) fires first and calls setOffset(0); (3) Effect 2 (lines 50-68) fires in the same flush with the stale offset value (e.g. 25), issuing a credentialed fetch — confirmed: fetchPostings uses `credentials: "same-origin"` (web/lib/explore.ts line 66); (4) setOff
- fix_ok: Yes. Both proposed fixes are sound. The cleanest option is to track the previous scopeKey in a ref inside the fetch effect: if scopeKey !== prevScopeKeyRef.current, update the ref and use offset 0 for that fetch (without depending on the setOffset state update). This collapses the two effects into o

### [26] Figure: two <figcaption> elements inside one <figure> is invalid HTML
- area=web-components kind=defect sev=LOW final=LOW conf=high
- loc: web/components/Figure.tsx:29-41  cat=HTML validity
- what: The HTML5 content model for `<figure>` permits at most one `<figcaption>` child. Figure.tsx places one for the chart title (line 29) and a second for the optional source/denominator note (line 41). The second figcaption may be ignored by assistive technology as it falls outside the spec's allowed structure, meaning the source note loses its semantic association with the chart.
- root: Both the title and the note are semantically related to the figure and the figcaption element was reused for both. The spec only allows one.
- fix: Keep the first `<figcaption>` for the primary title. Change the note element to a `<p>` or `<footer>` inside the figure (both are valid figure child content). Add an appropriate class or aria attribute if the note needs to be associated with the figure for AT.
- verdict: confirmed | Reading web/components/Figure.tsx in full confirms the structure exactly as described. Line 29 renders `<figcaption className="...font-bold...">{title}</figcaption>` for the chart title, and line 41 renders `{note && <figcaption className="...text-ink-soft">{note}</figcaption>}` for the optional source/denominator note — both are direct children of the `<figure>` element on line 25. The HTML5 content model for `<figure>` allows at most one `<figcaption>` child (either as the first or last child). Two `<figcaption>` elements make the document invalid HTML5, and assistive technology that relies 
- fix_ok: Yes. Changing the note's element from figcaption to a p (or footer) inside the figure is valid HTML5 and preserves the visual and semantic relationship. Adding role="note" or an aria-label if the note needs explicit AT association is optional but reasonable.

### [27] DownloadCSV: error-reset setTimeout leaks if component unmounts during the delay
- area=web-components kind=defect sev=LOW final=LOW conf=high
- loc: web/components/DownloadCSV.tsx:60  cat=React correctness
- what: On fetch failure, a bare `setTimeout(() => setState("idle"), 1800)` is called with no cleanup. If the user navigates away within 1.8 seconds of a download error the timer fires setState on an unmounted component. In React 18 this is a no-op rather than a crash, but the timer handle is never cleared.
- root: The error-state reset was implemented with a bare setTimeout in an event handler rather than a useEffect with a cleanup return.
- fix: Track the timeout id in a ref and clear it on unmount: `const timerRef = useRef<ReturnType<typeof setTimeout>>(); ... timerRef.current = setTimeout(...)`; add `useEffect(() => () => clearTimeout(timerRef.current), [])` for cleanup. Alternatively, move the error reset into a useEffect that watches the `state === 'error'` value.
- verdict: confirmed | The bare setTimeout at web/components/DownloadCSV.tsx:60 is real and exactly as described: no ref, no useEffect cleanup, no clearTimeout on unmount. The code matches the finding precisely. However, the practical impact in React 18 is zero: setState on an unmounted component is a documented no-op in React 18 (the warning was removed intentionally), the timer is 1.8 s and self-clears, and there is no cascading render, memory leak, or wrong state visible to the user. The proposed fix (useRef + useEffect cleanup) is sound hygiene but is fixing a theoretical problem, not a real runtime defect. The 
- fix_ok: yes — the proposed useRef + useEffect cleanup is technically correct hygiene, though the practical impact in React 18 is zero since setState on an unmounted component is a no-op with no warning

### [28] PostingDrawer: dialog named via aria-label instead of aria-labelledby referencing the visible h2
- area=web-components kind=defect sev=LOW final=LOW conf=high
- loc: web/components/explore/PostingDrawer.tsx:69  cat=a11y
- what: The dialog's accessible name comes from the static `aria-label={t.explore.drawerPosting}` string (e.g. "Job posting") rather than the dynamic job title that is rendered inside the drawer's `<h2>` at line 83. When the drawer is open, screen readers announce it as "Job posting dialog" regardless of which job was selected, missing the actual job title as context.
- root: The h2 at line 83 has no id attribute so it cannot be referenced by aria-labelledby on the dialog div. The static aria-label was used as a simpler fallback.
- fix: Assign a stable id to the h2 (e.g. `id="posting-drawer-title"`) and replace `aria-label` on the dialog div with `aria-labelledby="posting-drawer-title"`. This causes screen readers to announce the actual job title when the dialog opens and whenever focus enters it.
- verdict: confirmed | The code at web/components/explore/PostingDrawer.tsx:69 does use `aria-label={t.explore.drawerPosting}` (a static string, e.g. "Job posting") on the role="dialog" div. The h2 at line 83 has no id attribute, rendering it unreferenceable by aria-labelledby. The dynamic job title is rendered inside the h2 at line 84 as `{detail?.job_title ?? (loading ? t.common.loading : t.explore.drawerPosting)}`. This means screen readers announce the dialog as "Job posting dialog" regardless of which specific job was selected — the actual job title is never surfaced as the accessible name of the dialog. This i
- fix_ok: Yes. Add id="posting-drawer-title" to the h2 at line 83 and replace aria-label={t.explore.drawerPosting} on the dialog div at line 69 with aria-labelledby="posting-drawer-title". This is the correct WCAG pattern for dialog naming and has no adverse side effects.

### [29] metadataBase falls back to localhost:3000 in production — OG image URL is broken
- area=web-app kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/app/layout.tsx:21  cat=Metadata / Social sharing
- what: metadataBase is set to `process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"`. NEXT_PUBLIC_SITE_URL is never set in the Dockerfile, render.yaml, or .env.local. In production, Next.js resolves the opengraph-image route against http://localhost:3000, so the og:image meta tag emitted in HTML will contain `http://localhost:3000/opengraph-image`. Social crawlers (Twitter/X, Slack, LinkedIn) will fail to fetch the image, so the custom OG card never appears when the URL is shared.
- root: NEXT_PUBLIC_SITE_URL has no value in any deployed config. The fallback is hardcoded to a local dev address rather than the production domain.
- fix: Add `NEXT_PUBLIC_SITE_URL=https://your-production-domain.onrender.com` (or the real production URL) to the Dockerfile build-time ENV block and to render.yaml's env section. Because NEXT_PUBLIC_ vars are baked at build time, it must be present during `npm run build` in Stage 1 of the Dockerfile.
- verdict: confirmed | The finding is accurate. At web/app/layout.tsx:21, metadataBase is set to `new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000")`. Checking all three deployment configs confirms NEXT_PUBLIC_SITE_URL is absent everywhere:

- Dockerfile Stage 1 ENV block (lines 16-18) sets only NEXT_PUBLIC_API_BASE, JOBADS_API_ORIGIN, and NEXT_TELEMETRY_DISABLED — no NEXT_PUBLIC_SITE_URL.
- render.yaml has no env section at all (only name, type, runtime, plan, region, repo, branch, dockerfilePath, dockerContext, healthCheckPath, autoDeployTrigger).
- web/.env.local contains only NEXT_PUBLIC_API_BA
- fix_ok: The proposed fix is sound. NEXT_PUBLIC_SITE_URL must be added to the Stage 1 ENV block in the Dockerfile (alongside NEXT_PUBLIC_API_BASE) and to a new env section in render.yaml so Render injects the real production domain (e.g., https://jobads-dashboard.onrender.com). Because NEXT_PUBLIC_ vars are 

### [30] Single broken figure factory fails the entire page — no per-figure degradation
- area=web-app kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/app/page.tsx:45–58  cat=Error handling / resilience
- what: Every page wraps all figure fetches (and the meta/overview call) in a single `Promise.all(...)` inside one try/catch. If any one of the 8 figure endpoints in pulse/page.tsx (or 9 in occupations, 10 in geography, etc.) throws — for example, because a single Plotly factory has a data edge-case — the entire `catch` block fires and the whole page renders as `<ApiDown>`. A user visiting the Occupations page while one of its 9 charts has a factory bug sees a full-page "API is down" message even though the API and all other charts are fine.
- root: All figure fetches are batched in one `Promise.all`, so one rejection propagates to all. The per-page try/catch was designed for "API is unreachable" but is now the only failure mode even for partial data errors.
- fix: Use `Promise.allSettled` instead of `Promise.all` for the figure fetches (keep the critical meta/overview in a separate `Promise.all` that still fails fast). After `allSettled`, pass `null` for rejected figures and render a per-slot fallback (e.g. a greyed-out chart placeholder) rather than the full `<ApiDown>` for any single failure.
- verdict: confirmed | The code at web/app/page.tsx lines 43–58 (and identically in web/app/occupations/page.tsx lines 41–76 and other page files) batches api.overview()/api.meta() together with all figure fetches in a single Promise.all. One rejected figure fetch propagates immediately to the catch block, which returns the full-page ApiDown component. RemoteFigure (web/components/RemoteFigure.tsx) accepts a non-nullable FigJSON prop, so there is no per-slot fallback. The described failure mode is real and reachable: any edge-case error in a single Plotly factory (data shape issue, missing column, etc.) will render 
- fix_ok: Yes. The proposed fix is sound. Separate api.meta()/api.overview() into its own Promise.all (keep fail-fast for missing timestamp/KPI data). Use Promise.allSettled for the figure fetches and treat rejected slots as null. Also widen RemoteFigure's fig prop to FigJSON | null and render a greyed-out pl

### [31] ExploreView fires a spurious fetch at the previous page offset on every scope change
- area=web-app kind=defect sev=MEDIUM final=LOW conf=high
- loc: web/components/explore/ExploreView.tsx:46–68  cat=Data fetching / pagination
- what: Two separate `useEffect` hooks manage offset reset and data fetching. When the user changes a filter, React fires both effects in the same batch: the fetch effect (line 50) runs immediately with the new filter values but the stale (non-zero) `offset`, issuing a request for the wrong page. Then the reset effect (line 46) calls `setOffset(0)`, triggering a second render and a second (correct) fetch. The first request is cancelled by the cleanup flag, so wrong data never renders, but an unnecessary HTTP request always goes out on every scope change — N+1 fetches for every filter interaction.
- root: Offset reset and data fetching are split across two effects with overlapping but not identical dependency arrays. React cannot merge state updates across effects when a state value is both written by one effect and read as a dependency by another in the same render.
- fix: Collapse the reset logic into the fetch effect: derive `effectiveOffset` inside the single fetch effect as `offset === 0 || scopeChanged ? 0 : offset`, or use a `useRef` for the previous scopeKey and reset `offset` synchronously (via `useLayoutEffect` or by computing a stable `[scope, offset]` pair). The simplest fix is to remove the separate reset effect and instead pass `0` as the offset directly when the fetch effect detects a scope change (compare a `prevScopeKey` ref).
- verdict: confirmed | The two-effect pattern is real and the extra fetch genuinely fires. When the user is on a non-zero offset page and changes a filter: (1) the fetch effect fires immediately with the new filter values but the stale offset, issuing a request for the wrong page; (2) the reset effect fires in the same flush and schedules setOffset(0), causing a second render and a second correct fetch. The cleanup flag (`cancelled = true`) prevents stale data from ever rendering, so there is no data-correctness problem — but the extra HTTP request is real and always occurs on any filter change from a page > 1. The 
- fix_ok: Partially sound. The prevScopeKey ref + single fetch effect approach is correct. The useLayoutEffect suggestion should be dropped — it can cause SSR hydration issues in Next.js and is unnecessary here. The simplest valid fix: add a `prevScopeKeyRef = useRef(scopeKey)` inside the fetch effect, compar

### [32] Hardcoded `http://127.0.0.1:8530/docs` link on the Developers page is unreachable in production
- area=web-app kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/app/developers/page.tsx:172  cat=Broken link / configuration
- what: The Developers page links users to `http://127.0.0.1:8530/docs` for the interactive OpenAPI docs. In the Docker container, FastAPI runs on 127.0.0.1:8530 as an internal loopback service, not accessible from outside the container. The Next.js rewrite in next.config.ts only proxies `/api/:path*`, not `/docs`. A visitor clicking this link from the deployed site gets a connection-refused error; the FastAPI docs are effectively invisible to external users.
- root: The `/docs` path is not included in the Next rewrite rules, and the link hardcodes the internal loopback address instead of using a relative or proxied path.
- fix: Add a rewrite for `/docs/:path*` and `/openapi.json` in next.config.ts pointing to `${API_ORIGIN}/docs/:path*`, then change the link in developers/page.tsx to the relative path `/docs`. Alternatively, remove the link and note that the OpenAPI spec is available at `/api/openapi.json`.
- verdict: confirmed | The link at web/app/developers/page.tsx line 172 hardcodes `href="http://127.0.0.1:8530/docs"`. The Next.js rewrite in web/next.config.ts line 14 covers only `/api/:path*` — there is no rewrite for `/docs` or `/openapi.json`. In production the FastAPI process binds to the internal loopback; the Next.js server cannot forward `/docs` requests externally because no rewrite rule exists for that path. A visitor clicking the link from the deployed site gets a connection-refused error. The orchestrator also independently flagged this in the live-capture evidence (live-capture.md line 66). The finding
- fix_ok: Yes. Adding `{ source: "/docs/:path*", destination: "${API_ORIGIN}/docs/:path*" }` and `{ source: "/openapi.json", destination: "${API_ORIGIN}/openapi.json" }` to the rewrites array in web/next.config.ts, then changing the href in web/app/developers/page.tsx line 172 from the absolute loopback URL t

### [33] JOBADS_API_COOKIE_SECURE defaults to false — auth session cookie lacks Secure flag in production HTTPS deployment
- area=web-app kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: api/routers/private.py:22  cat=Security / Cookie configuration
- what: `_COOKIE_SECURE` reads `JOBADS_API_COOKIE_SECURE` and defaults to `False` when the var is absent. The Dockerfile and render.yaml set no value for this variable. On the Render deployment (HTTPS), the session cookie for the Explore auth gate is therefore sent without the `Secure` attribute. Per RFC 6265 and browser behavior, a non-Secure cookie is also sent over plain HTTP connections; modern browsers warn about it, and some security scanners will flag the deployed app.
- root: The opt-in pattern (`JOBADS_API_COOKIE_SECURE=true`) requires an explicit deployment step that has not been taken. The safer default for a production-only HTTPS service is to be Secure-by-default and opt out only in dev.
- fix: Set `JOBADS_API_COOKIE_SECURE=true` (or `1`) in the Dockerfile runtime ENV block (Stage 2) and in the render.yaml env section. Optionally invert the logic in private.py to default to `True` and require an explicit `JOBADS_ALLOW_INSECURE_COOKIE=1` for local dev.
- verdict: confirmed | api/routers/private.py line 22 reads JOBADS_API_COOKIE_SECURE and defaults to False when absent. Neither the Dockerfile Stage 2 ENV block nor render.yaml sets this variable, so the session cookie is issued without the Secure attribute on the Render HTTPS deployment. The architecture has a partial mitigation — FastAPI is bound to 127.0.0.1:8530 (internal only) and the browser always reaches it through Next.js's /api/* rewrite proxy over HTTPS, so there is no direct plain-HTTP browser-to-API path. However, the missing Secure flag is still a real misconfiguration: it permits the browser to send t
- fix_ok: Yes. Setting JOBADS_API_COOKIE_SECURE=true in the Dockerfile Stage 2 ENV block and/or in render.yaml is the right fix. Inverting the default in private.py to True (requiring JOBADS_ALLOW_INSECURE_COOKIE=1 for local dev) is also sound and is the safer long-term pattern. Both approaches are correct; t

### [34] Dev-only env file sets NEXT_PUBLIC_API_BASE to port 8531 while next.config.ts rewrite uses JOBADS_API_ORIGIN defaulting to 8530 — SSR and browser fetches hit different ports in development
- area=web-app kind=defect sev=LOW final=LOW conf=high
- loc: web/.env.local:1  cat=Configuration / Developer experience
- what: `.env.local` sets `NEXT_PUBLIC_API_BASE=http://127.0.0.1:8531`. The server-side `api.ts` uses this value for all SSR fetches. But `next.config.ts` reads `JOBADS_API_ORIGIN ?? "http://127.0.0.1:8530"` (default 8530) to set the browser-side `/api/*` rewrite destination. In development, if only one FastAPI instance is running, either SSR page renders fail (if the API is on 8530, not 8531) or browser explore calls fail (if the API is on 8531). The two env vars are meant to configure the same backend but point to different ports.
- root: Two separate environment variables control the same backend address but have independent defaults/values. `.env.local` was presumably updated without also updating `JOBADS_API_ORIGIN`.
- fix: In `.env.local` add `JOBADS_API_ORIGIN=http://127.0.0.1:8531` to match, or unify on one env var. Longer term, consider having `next.config.ts` read `JOBADS_API_ORIGIN` only, and derive `NEXT_PUBLIC_API_BASE` from it (or use only the `/api` rewrite path for all server-side calls).
- verdict: confirmed | The factual claim is correct: web/.env.local line 1 sets NEXT_PUBLIC_API_BASE=http://127.0.0.1:8531, and next.config.ts line 6 reads JOBADS_API_ORIGIN defaulting to http://127.0.0.1:8530. If a developer runs the API on 8531 (matching .env.local), the Next.js rewrite silently proxies browser-side /api/* calls to 8530 instead — which is a dead port. lib/api.ts line 24 confirms SSR fetches use NEXT_PUBLIC_API_BASE directly (bypassing the rewrite), so the two code paths would hit different backends.

However, the finding overstates the severity. The .env.local is gitignored and is explicitly docum
- fix_ok: Yes. Adding JOBADS_API_ORIGIN=http://127.0.0.1:8531 to web/.env.local is the minimal fix. The longer-term suggestion to derive NEXT_PUBLIC_API_BASE from JOBADS_API_ORIGIN in next.config.ts is also sound but is a bigger refactor. Both are correct.

### [35] Desktop and mobile navigation landmarks share the same aria-label, violating WCAG 2.4.1
- area=web-app kind=defect sev=LOW final=LOW conf=high
- loc: web/components/TopNav.tsx:45, 113  cat=Accessibility
- what: The desktop `<nav>` (line 45) and the mobile nav panel `<div role="navigation">` (line 113) both use `aria-label={t.nav.primary}` — the same translated string. WCAG 2.4.1 (Bypass Blocks) and ARIA authoring practices require that multiple navigation landmarks on a page have distinct, descriptive labels so screen reader users can distinguish them in the landmark list. With identical labels, the user sees two navigation regions with the same name and cannot tell them apart.
- root: The mobile nav was added as a copy of the desktop nav without updating its aria-label to a distinct value.
- fix: Add a `nav.mobile` key to the nav dict and apply it to the mobile panel: `aria-label={t.nav.mobile}` (e.g. "Mobile navigation" / "Navigation mobile"). Alternatively use a single `<nav>` that contains both desktop and mobile views, since they represent the same navigation.
- verdict: confirmed | Both the desktop nav at line 45 and the mobile panel at line 113 of web/components/TopNav.tsx use aria-label={t.nav.primary}. The i18n dictionary at web/lib/i18n/dict/nav.ts resolves t.nav.primary to "Primary" (EN) and "Principale" (FR) — identical strings for both landmarks. No "mobile" key exists in the dict, so there is no distinct label available for the mobile panel. A screen reader's landmark list will expose two navigation regions with the same name, violating WCAG 2.4.1. The finding is accurate, the cited lines are correct, and the proposed fix (add a nav.mobile i18n key and apply it t
- fix_ok: Yes. Adding a distinct nav.mobile translation key (e.g. "Mobile navigation" / "Navigation mobile") to web/lib/i18n/dict/nav.ts and applying it as aria-label={t.nav.mobile} on the div at line 113 of TopNav.tsx fully resolves the violation. The alternative of wrapping both views in a single nav elemen

### [36] PostingDrawer scroll-lock effect re-runs on every render that produces a new `onClose` reference, momentarily releasing body scroll
- area=web-app kind=defect sev=LOW final=LOW conf=high
- loc: web/components/explore/PostingDrawer.tsx:51–62  cat=Interaction / scroll lock
- what: The `useEffect` at line 51 depends on both `id` and `onClose`. `onClose` is passed as `() => setActiveId(null)` from `ExploreView` — an inline arrow function that is re-created on every render of ExploreView. When ExploreView re-renders (e.g., a fetch completes or the debounce timer fires while the drawer is open), a new `onClose` reference is produced, the effect cleanup runs and sets `document.body.style.overflow = ""`, then the effect immediately re-fires and sets it back to `"hidden"`. This produces a brief scroll-unlock flicker when data arrives while the drawer is open — noticeable on slower connections.
- root: The effect includes `onClose` in its dependency array. Because `onClose` is an unstabilised callback, any parent re-render invalidates the effect.
- fix: Stabilise `onClose` in ExploreView with `useCallback`: `const handleClose = useCallback(() => setActiveId(null), [])`. This ensures the same function reference is passed to PostingDrawer across renders, preventing unnecessary effect re-runs.
- verdict: confirmed | The code pattern is real and exactly as described. ExploreView.tsx line 216 passes `onClose={() => setActiveId(null)}` as an inline arrow — a new reference on every render. PostingDrawer.tsx lines 51–62 lists `onClose` in the effect dependency array alongside `id`. Any ExploreView re-render (fetch completion, debounce timer, pagination state change) while the drawer is open produces a new `onClose` reference, triggering effect cleanup (`overflow = ""`) followed immediately by effect re-run (`overflow = "hidden"`).

However, the claimed observable symptom — "brief scroll-unlock flicker noticeab
- fix_ok: Yes — `useCallback(() => setActiveId(null), [])` in ExploreView stabilises the reference and eliminates the unnecessary teardown/re-setup cycle. The fix is correct and has no downside. However the stated motivation (visible scroll-unlock flicker) is wrong; the real motivation is avoiding redundant e

### [37] Mobile nav panel remains keyboard-focusable and AT-readable when visually closed
- area=ux-global kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/TopNav.tsx:110-148  cat=Accessibility
- what: When the mobile menu is closed (menuOpen=false), the nav panel is hidden only via max-h-0 + overflow-hidden + opacity-0. It has no aria-hidden, no inert attribute, and no display:none. All nav links inside it are reachable via keyboard Tab and are fully exposed to screen readers, creating a ghost navigation that a keyboard user silently traverses before reaching main content.
- root: CSS max-height animation was chosen for the open/close transition. overflow:hidden clips visual rendering but does not remove elements from the accessibility tree or tab order. No aria-hidden toggling or inert attribute was added alongside the visual toggle.
- fix: Add aria-hidden={!menuOpen} to the panel div (line 111) and also set the inert attribute when closed so browsers natively block all keyboard and AT interaction: <div ... aria-hidden={!menuOpen} {...(!menuOpen ? { inert: '' } : {})}. Alternatively, conditionally render the panel using {menuOpen && ...} which removes it from the DOM entirely when closed (though this removes the CSS transition unless entry animation is handled separately).
- verdict: confirmed | The code at web/components/TopNav.tsx lines 110–118 confirms the finding. The mobile nav panel is always rendered in the DOM; when menuOpen is false it is hidden only via Tailwind classes max-h-0 and opacity-0 (plus overflow-hidden from line 116). There is no aria-hidden, no inert attribute, no tabIndex=-1 on children, no display:none, and no conditional render. The panel carries role="navigation" (line 112), so a screen reader exposes it as a permanent navigation landmark regardless of open state, and keyboard Tab reaches every Link inside it (lines 125–144) when the menu is visually closed. 
- fix_ok: Yes. Both proposed fixes are sound. Adding aria-hidden={!menuOpen} to the panel div (line 111) hides it from the AT tree. Adding the inert attribute when closed (e.g. {...(!menuOpen ? { inert: '' } : {})}) additionally removes all links from tab order natively. Together they fully resolve the ghost-

### [38] No skip-to-main-content link: sticky nav forces keyboard users to tab through 11+ items on every page
- area=ux-global kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/app/layout.tsx:56-68  cat=Accessibility
- what: The root layout has no skip navigation link. With a sticky TopNav containing 9 section links, a locale toggle, and a hamburger button, keyboard-only users must tab through all of these on every page load and every client-side navigation to reach the main content area. WCAG 2.4.1 (Bypass Blocks, Level A) requires a mechanism to skip repeated navigation.
- root: The <main> element at line 63 has no id attribute, and no skip link is present anywhere in layout.tsx. The omission is likely an oversight since the rest of the keyboard handling (Escape closes menu, aria-current on active link, aria-expanded on hamburger) is thoughtful.
- fix: Add id='main-content' to the <main> element (line 63). Add a visually-hidden skip link as the very first child of <body>: <a href='#main-content' className='sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 focus:px-4 focus:py-2 focus:bg-canvas focus:text-navy focus:border focus:border-orange'>Skip to main content</a>. Wire the i18n string through t.common.
- verdict: confirmed | layout.tsx line 63 has `<main className="flex-1">{children}</main>` with no id attribute. A grep across the entire web/ source tree (excluding node_modules) finds no skip link, no "sr-only focus:" pattern, and no "main-content" anchor anywhere in the application code. The NAV array (lib/nav.ts) contains 8 items, not 9 as stated — a minor counting error. On desktop the full tab-stop sequence before reaching main content is: Brand link + 8 nav links + LocaleToggle = 10 items (the hamburger is md:hidden, which Tailwind renders as display:none, removing it from the tab order on desktop). The findi
- fix_ok: Sound. Add id="main-content" to the main element at layout.tsx:63, and insert a visually-hidden skip link as the first child of the body element. Standard Tailwind sr-only/focus:not-sr-only pattern is correct; the design-system color tokens cited (bg-canvas, text-navy, border-orange) all exist. One 

### [39] No dark-mode CSS: OS dark-preference users get unmodified cream/white — design system gap vs shipped aclmr_dark theme
- area=ux-global kind=craft sev=P2 final=LOW conf=high
- loc: web/app/globals.css:10-75  cat=Design system parity
- what: globals.css has exactly one @media block: prefers-reduced-motion. There is no @media (prefers-color-scheme: dark) override. Users who have set OS dark preference receive the full cream canvas (#fbf8f5), navy text (#16242f), and orange accents — unchanged. The Python viz layer already ships a complete aclmr_dark palette (theme.py lines 112, 178-182) with dark paper backgrounds and adjusted ink. This design investment is entirely unused in the web layer, creating a parity gap between the static chart-review page (which respects system theme) and the live app.
- root: The web redesign was built as explicitly light-only (confirmed by the live-capture note). The dark Plotly theme exists for the standalone chart-review page and was not ported to CSS tokens. No :root.dark or @media dark block was ever authored for globals.css.
- fix: If light-only is a deliberate product choice, add a comment to :root in globals.css and a CLAUDE.md note so future contributors do not re-investigate. If dark mode is desired, extract DARK surface/ink values from viz/theme.py into a @media (prefers-color-scheme: dark) { :root { ... } } block in globals.css, overriding --canvas, --surface, --ink, --ink-soft, --ink-faint, --ink-invert, --hairline, --card-border, and --surface-navy.
- verdict: confirmed | The factual claims are all accurate. globals.css (lines 10-75 and 244-251) has exactly one @media block — prefers-reduced-motion — and no @media (prefers-color-scheme: dark) override. The body and html elements receive the cream canvas (#fbf8f5) and navy ink (#16242f) unconditionally. src/jobads_dashboard/viz/theme.py does define an aclmr_dark palette (line 112, registered at lines 178-182) that is used only by the standalone static review page, not by the Next.js app. There is no color-scheme meta tag, no dark-class toggle, and no comment anywhere in the codebase documenting a deliberate ligh
- fix_ok: The proposed fix is sound in both branches: documenting the light-only choice with a comment in globals.css :root and a CLAUDE.md note is the right minimum action; extracting DARK surface/ink values from viz/theme.py into a @media (prefers-color-scheme: dark) { :root { ... } } block overriding --can

### [40] Typography scale uses 10+ arbitrary rem values with no named tokens — ad-hoc sizing
- area=ux-global kind=craft sev=P2 final=P3 conf=high
- loc: web/app/globals.css:145-188  cat=Typography system
- what: globals.css defines four named type utilities (.eyebrow at 0.72rem, .h-display clamp(2rem–3rem), .h-section clamp(1.3rem–1.7rem), .h-card at 0.95rem, .lede clamp(1.05rem–1.2rem)). But across the chrome components alone — TopNav, Footer, Brand, LocaleToggle, FilterSpine, KpiTile, Select — 10 additional arbitrary rem values appear (0.62, 0.68, 0.7, 0.74, 0.78, 0.82, 0.85, 0.9, 0.92, 0.95rem). None of these map to a declared scale step or token. The named scale covers only headings and lede copy; the entire UI-chrome tier (labels, captions, controls, nav) is ad-hoc. This breaks the single-coherent-scale standard and makes future resizing changes require grep-and-replace hunts.
- root: The design tokens in globals.css were written for editorial hero content. The functional UI layer (nav labels, control text, table headers, data annotations) was styled inline without referencing or extending the scale. No --text-caption / --text-label / --text-control CSS variables or utility classes were defined.
- fix: Add three named scale utilities to globals.css covering the UI tier: a label class (0.7–0.72rem, bold, uppercase, for form labels and eyebrows), a caption class (0.74–0.78rem, for secondary UI text, control labels), and a fine class (0.62–0.68rem, for table headers and metadata). Replace the ad-hoc inline rem values in components with these classes. This reduces the current 10-value soup to 3 named steps beneath the already-defined editorial scale.
- verdict: confirmed | The core observation is correct: globals.css defines named type utilities only for editorial/heading content (.eyebrow at 0.72rem, .h-display, .h-section, .h-card, .lede), while UI-chrome components use Tailwind arbitrary rem values inline with no corresponding tokens. In the 7 cited chrome components (TopNav, Footer, Brand, LocaleToggle, FilterSpine, KpiTile, Select), there are approximately 8–10 distinct inline rem values: 0.62rem (×5 in Brand, FilterSpine, Select, AuthGate, PostingDrawer), 0.68rem (KpiTile), 0.7rem (FilterSpine, KpiTile, LocaleToggle), 0.74rem (Footer), 0.78rem (TopNav desk
- fix_ok: The proposed fix is sound as a direction: adding named utility classes (.label, .caption, .fine) to globals.css and replacing the repeated inline rem values would reduce the de-facto-token count from ~10 to ~3 named steps. The specific rem ranges suggested (label 0.70–0.72rem, caption 0.74–0.78rem, 

### [41] Footer eyebrow text overrides .eyebrow color with text-orange-soft — specificity gap causes wrong token on dark background
- area=ux-global kind=defect sev=LOW final=MEDIUM conf=high
- loc: web/components/Footer.tsx:24, 36  cat=Color token misuse
- what: The global .eyebrow class sets color: var(--orange-deep) (#a25518). On canvas (#fbf8f5) that is 5.16:1 — acceptable. In the footer, both eyebrow elements use class='eyebrow text-orange-soft', intending to override to orange-soft (#f0c79f) for legibility on navy (#041c2c). The Tailwind utility wins (specificity) and contrast is 11.08:1 — correct. But the mechanism is fragile: if Tailwind v4 specificity rules change or the class order changes, .eyebrow's --orange-deep on navy would render at 3.19:1, failing WCAG AA. More importantly, this is an undocumented specificity dependency — a future developer editing the eyebrow class or removing text-orange-soft would silently break contrast on the dark footer.
- root: The .eyebrow utility hardcodes color: var(--orange-deep) in globals.css. On dark surfaces this needs to be orange-soft, but there is no .eyebrow--invert or [data-surface=dark] .eyebrow variant. The footer works around this with a Tailwind override rather than a proper dark-surface token.
- fix: Add an .eyebrow-invert utility (or a .on-dark .eyebrow rule) to globals.css that sets color: var(--orange-soft). Use class='eyebrow-invert' in the footer instead of the specificity override. Alternatively, change the .eyebrow definition to use currentColor and set color on the container, making the eyebrow inherit its context's ink color.
- verdict: confirmed | The footer eyebrow contrast problem is real, but the prior auditor's mechanism analysis is wrong in a way that makes the actual situation worse than described.

The finding claims "The Tailwind utility wins (specificity) and contrast is 11.08:1 — correct. But the mechanism is fragile." This is factually backwards for Tailwind v4.

In Tailwind v4, `@import "tailwindcss"` emits all utility classes inside named `@layer` blocks (`@layer utilities` etc.). The `.eyebrow` rule in globals.css (lines 147–154) is declared outside any `@layer` — it sits in the implicit unlayered scope. CSS `@layer` casca
- fix_ok: The fix direction is sound — adding an `.eyebrow-invert` utility (or `.on-dark .eyebrow` scoped rule) in globals.css using `color: var(--orange-soft)` would work, because unlayered CSS rules take precedence over `@layer utilities`. Using `class="eyebrow-invert"` in the footer instead of `class="eyeb

### [42] Brand tagline at 0.62rem (9.9px) is below practical readable minimum
- area=ux-global kind=craft sev=P3 final=P3 conf=high
- loc: web/components/Brand.tsx:15  cat=Typography legibility
- what: The brand tagline 'LABOUR MARKET' renders at 0.62rem = 9.92px in all-caps uppercase. At this size, the text is physically below the threshold where letterforms are reliably distinguishable at arm's length on typical displays. Even though it clears WCAG AA contrast (ink-soft on canvas = 5.19:1), the size itself undermines legibility — the tag line read in the orchestrator's live observation is essentially decoration at desktop zoom levels and invisible at mobile.
- root: The Brand component opts for compact visual hierarchy (ACLMR large + tagline small beneath it), but the tagline was set to a size below the practical lower bound (~11px) used in most design systems for non-decorative type.
- fix: Raise the tagline to 0.68–0.7rem (10.9–11.2px) minimum, or use 0.72rem to align it with the .eyebrow scale step already defined in globals.css. The 3px increase is visually negligible for the brand mark but crosses the practical readability floor.
- verdict: confirmed | Brand.tsx line 15 uses `text-[0.62rem]` for the "LABOUR MARKET" tagline, yielding 9.92px at the 16px base font size established in globals.css line 128. The project's own design system defines `.eyebrow` at `font-size: 0.72rem` (11.52px) as the sanctioned minimum for small non-decorative text (globals.css lines 147-148, labeled "AA text orange (eyebrows, small accents)"). The tagline is non-decorative — it is the product's brand description — yet it sits 1.6px below the project's own floor for small text. The live-capture confirms the tagline is visible in the top nav ("ACLMR / LABOUR MARKET")
- fix_ok: Yes. Changing `text-[0.62rem]` to `text-[0.72rem]` at Brand.tsx:15 aligns the tagline with the project's `.eyebrow` font-size token (globals.css:148), the smallest sanctioned text size in the design system. The 1.6px visual change to the brand mark is negligible and the fix requires no other changes

### [43] KPI tile bottom row crushes sparkline at mobile (375px, 2-col grid)
- area=ux-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/KpiTile.tsx:45-64  cat=Responsive layout
- what: The bottom row is a single flex row sharing space between the delta chip (shrink-0, whitespace-nowrap) and the sparkline wrapper (flex-1). At mobile 375px with a 2-column grid, each tile is roughly 160px wide. The DEMAND INDEX tile delta reads '▼8.3% vs baseline' — about 95px at 0.82rem bold — leaving only ~47px for the 132px-wide sparkline. The orchestrator confirmed this looks cramped (sparkline squeezed inline beside 'MoM') or absent/clipped (DEMAND INDEX). The bottom two tiles (VS LAST YEAR, MEDIAN WAGE) have no delta chip so their sparklines fill the full row and appear correct.
- root: The delta chip uses `shrink-0 whitespace-nowrap` so it never compresses. The sparkline uses `flex-1 min-w-0` and can shrink to near-zero, but at 47px × 34px the sparkline is barely readable. There is no breakpoint or layout adaptation to stack delta above sparkline on narrow tiles.
- fix: Either (a) stack the delta chip and sparkline vertically when the tile is narrow — wrap the bottom row in `flex-col` on xs, `flex-row` on sm — or (b) move the sparkline to its own row always, so it always fills the full card width. Option (b) is simpler: place the sparkline in a separate `<div>` after the delta chip row. The ACTIVE POSTINGS and DEMAND INDEX tiles both need this fix.
- verdict: confirmed | The layout math confirms the defect. At 375px: gutter=20px (clamp floor), container inner width=335px, 2-col grid with gap-4 (16px) gives each tile 159.5px, minus p-5 card padding (40px) = 119.5px inner content width. The bottom row is a single flex row. The delta chip (shrink-0 whitespace-nowrap) for "▼8.3% vs baseline" at 0.82rem (~13px) occupies roughly 90–95px, leaving only ~20px for the sparkline wrapper (flex-1 min-w-0). Sparkline.tsx renders with style={{ width: "100%", maxWidth: 132, height: 34 }}, so it scales to ~20px wide × 34px tall — technically present (not absent: overflow-visib
- fix_ok: Yes. Option (b) — place the sparkline in a separate div after the delta chip row — is the correct fix. It is simpler than the flex-col/flex-row responsive approach and ensures the sparkline always fills the full card width regardless of delta chip length. Both the ACTIVE POSTINGS and DEMAND INDEX ti

### [44] KPI delta indicator convention is inconsistent across the four tiles
- area=ux-components kind=craft sev=P1 final=MEDIUM conf=high
- loc: web/app/page.tsx:94-127  cat=Visual consistency / information design
- what: DEMAND INDEX shows a colored arrow-chip below the value (▼8.3% text-neg). ACTIVE POSTINGS shows a colored arrow-chip (▲11.7% text-pos). VS LAST YEAR encodes its delta as the main value itself (−7.6% in text-navy-deep with no directional color — no arrow, no chip). MEDIAN WAGE shows no delta at all. This mixes three different conventions for communicating change: colored chip, signed main value, and absent. A user scanning the strip cannot immediately tell which tiles are 'up vs down' by the same visual grammar.
- root: VS LAST YEAR was built with `value={fmtPct(kpis.active_yoy_pct, { sign: true })}` (the yoy pct becomes the headline number) and no `delta` prop, so KpiTile renders no arrow chip and no directional color on the value. The other two tiles pass yoy/mom as the `delta` prop. The design intent for each tile was implemented ad-hoc without a shared convention.
- fix: Decide one convention for all change tiles: the delta chip below the headline value is the standard. For VS LAST YEAR, pass the raw `kpis.active_yoy_pct` as `delta` prop and use a neutral headline value (e.g. 'year over year') or keep the sign-prefixed pct as value but also pass it as `delta` so the chip appears with color. At minimum, apply `trendClass(kpis.active_yoy_pct)` to the value span — but changing to a chip is the more consistent approach. KpiTile itself should also apply directional color to the value text when `hasDelta` is false but the value is a signed figure.
- verdict: confirmed | The finding is confirmed. Reading page.tsx lines 94–127 and KpiTile.tsx in full: tiles 1 (DEMAND INDEX, line 98: delta={baselineGap}) and 2 (ACTIVE POSTINGS, line 107: delta={kpis.active_mom_pct}) both pass a numeric delta prop, so KpiTile renders an arrow chip with text-pos/text-neg coloring. Tile 3 (VS LAST YEAR, lines 112–118) passes no delta prop — only value={fmtPct(kpis.active_yoy_pct, { sign: true })} — so hasDelta is false, no chip is rendered, and the value span unconditionally gets text-navy-deep with no directional color. Tile 4 (MEDIAN WAGE) intentionally shows no change. KpiTile.t
- fix_ok: Yes, the proposed fix is sound. Passing kpis.active_yoy_pct as the delta prop on the VS LAST YEAR tile (with deltaLabel="vs last year" or equivalent) and using a neutral headline value would align it with the arrow-chip convention used by the other two change tiles. KpiTile already handles the chip 

### [45] Figure component uses two <figcaption> elements inside one <figure>
- area=ux-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/Figure.tsx:29, 41  cat=Accessibility / HTML validity
- what: HTML permits at most one <figcaption> per <figure>. Figure.tsx places the chart title in a first <figcaption> (line 29) and the note/source line in a second <figcaption> (line 41). This is invalid markup. NVDA and JAWS may announce both as the figure caption, announce only the first, or behave inconsistently across browser/AT combinations.
- root: The developer wanted both the title and the footnote to be semantically associated with the figure, and used <figcaption> for both. Only one is correct; the second should be a different element.
- fix: Keep the title as `<figcaption>`. Change the note element to `<p>` (or `<footer>` inside `<figure>`). If the note needs to be programmatically linked to the figure, add `id` to the figure and `aria-describedby` pointing to the note element. The CSS styling can stay identical — only the HTML element changes.
- verdict: confirmed | The code at /Volumes/ACLMR/jobads-dashboard/.claude/worktrees/redesign2/web/components/Figure.tsx is exactly as described. Line 29 renders `<figcaption className="text-[1.02rem] font-bold ...">{title}</figcaption>` and line 41 conditionally renders `{note && <figcaption className="mt-3 border-t ...">note</figcaption>}`. Both are direct children of the `<figure>` element at line 25. The HTML spec (and WHATWG living standard) states a figure element may contain at most one figcaption child. Having two is invalid markup. The finding is fully accurate: wrong element on line 41, real HTML validity 
- fix_ok: Yes. The proposed fix is sound. Change line 41's second `&lt;figcaption&gt;` to `&lt;p&gt;` (or `&lt;footer&gt;` inside `&lt;figure&gt;`). If the note needs a programmatic link to the figure, add an `id` to the `&lt;figure&gt;` and `aria-describedby` on the note element. The CSS class string on that

### [46] MapToggle tablist missing tabpanel role and keyboard arrow-key navigation
- area=ux-components kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/MapToggle.tsx:26-49  cat=Accessibility — ARIA tabs pattern
- what: MapToggle implements `role='tablist'` with `role='tab'` buttons and `aria-selected`, which declares the ARIA tabs pattern. The ARIA Authoring Practices (APG) tabs pattern requires: (1) the panel element has `role='tabpanel'`, (2) each tab has `aria-controls` pointing to its panel, (3) the panel has `aria-labelledby` pointing to its tab, and (4) arrow keys cycle through tabs (the tab key moves to the panel). None of (1–4) are implemented. AT users encounter a tablist with no associated panels, and keyboard-only users cannot navigate tabs with arrow keys.
- root: The segmented control was built as a visual toggle. The ARIA roles were added but only partially — the button-level attributes without the corresponding panel wiring and keyboard handler.
- fix: Add `id` props to each tab button (e.g. `tab-${o.value}`). Wrap the RemoteFigure in a div with `role='tabpanel'`, `id='panel-${active}'`, `aria-labelledby='tab-${active}'`. Each button gets `aria-controls='panel-${o.value}'`. Add `onKeyDown` to the tablist that moves focus with ArrowLeft/ArrowRight and calls `setActive`. Alternatively, if the toggle is not meant to be a full tabs widget, remove `role='tablist'`/`role='tab'` and use `role='group'` + `aria-pressed` on each button — simpler and accurate.
- verdict: confirmed | The code at web/components/MapToggle.tsx lines 26-49 is exactly as described. The component declares role="tablist" on the wrapper div (line 26) and role="tab" + aria-selected on each button (lines 31-34), which commits it to the ARIA tabs pattern. The RemoteFigure on line 47 has no role="tabpanel", no id, no aria-labelledby. No button has aria-controls. There is no onKeyDown handler anywhere in the component. All four deficiencies stated in the finding are verifiably present in the source. This is not a lint false positive, not handled elsewhere, and not merely stylistic — the APG tabs patter
- fix_ok: Yes. Both proposed paths are valid. The simpler path — replace role="tablist" with role="group" and replace role="tab"+aria-selected with aria-pressed on each button — is correct and avoids adding panel IDs and a keyboard handler. The full tabs-pattern path is also correct if the toggle is intended 

### [47] Chart figure font (Inter) is not loaded by the web app; axis labels fall back to system-ui
- area=ux-components kind=craft sev=P2 final=P2 conf=high
- loc: src/jobads_dashboard/viz/theme.py:121  cat=Chart craft / typography consistency
- what: The Python viz theme declares `_FONT = 'Inter, -apple-system, Segoe UI, Helvetica, Arial, sans-serif'` which is serialized into every figure's JSON template and consumed by Plotly in the browser. The Next.js app only loads PT Sans (via `next/font/google`). Inter is not loaded, so Plotly axis labels, hover tooltips, colorbar text, and annotation text all render in `-apple-system`/system-ui while all editorial chrome (headlines, captions, tiles) renders in PT Sans. The mismatch is visible at axis and legend scale.
- root: The Python theme was designed independently (for the standalone static review page) without coordinating the font with the web host. The bridge inlines the full template including its font stack.
- fix: Either (a) change `_FONT` in theme.py to match the web app's PT Sans stack (`'PT Sans, ui-sans-serif, system-ui, sans-serif'`) — this is the simplest fix and ensures consistency end-to-end, or (b) add `Inter` to the Next.js font load in layout.tsx alongside PT Sans. Option (a) is recommended as PT Sans is already loaded and the dashboard's brand typeface.
- verdict: confirmed | Inter is not loaded anywhere in the Next.js app (layout.tsx loads only PT_Sans; no Inter import exists in web/ outside unrelated identifiers). The Python viz theme at src/jobads_dashboard/viz/theme.py:121 sets `_FONT = "Inter, -apple-system, Segoe UI, Helvetica, Arial, sans-serif"` which is serialised into every figure's JSON template via _template() at line 124–127. RemoteFigure.tsx line 9–11 explicitly states it does NOT inject the web Plotly template — it passes fig.data and fig.layout (including the inlined font family) straight to newPlot. So any figure served from the Python bridge and r
- fix_ok: Yes. Option (a) — change `_FONT` in src/jobads_dashboard/viz/theme.py:121 to `"PT Sans, ui-sans-serif, system-ui, sans-serif"` — is the correct fix. PT Sans is already loaded by the web layer so the font will resolve; the fallback stack still degrades gracefully. Option (b) (loading Inter in layout.

### [48] DownloadCSV component is fully built but never imported or used anywhere in the app
- area=ux-components kind=craft sev=P3 final=P3 conf=high
- loc: web/components/DownloadCSV.tsx:1-78  cat=Dead code / feature completeness
- what: DownloadCSV is a client component with full fetch/CSV/download logic, a loading state, an error state, and the underlying `toCSV`/`downloadCSV` helpers in `web/lib/csv.ts`. No page or component in the app imports it. The baseConfig in theme.ts removes the `toImage` modebar button with a comment 'our own CSV/PNG buttons handle download' — but those buttons are never wired up. Users have no way to download chart data.
- root: The component was built speculatively or was removed from pages during a refactor without being deleted.
- fix: Either wire DownloadCSV into Figure's `actions` slot for charts that have downloadable endpoints (passing the relevant `/api/...` endpoint and filename), or delete the component and `web/lib/csv.ts` if data download is out of scope. If wiring up: Figure already accepts an `actions?: ReactNode` prop intended for exactly this purpose.
- verdict: confirmed | DownloadCSV.tsx (lines 1–78) and web/lib/csv.ts exist and are fully implemented. A grep across all .tsx/.ts files under web/ finds only DownloadCSV.tsx itself importing from csv.ts — no page or component imports DownloadCSV. Figure.tsx (line 20) does expose an `actions?: ReactNode` prop and renders it at line 32, confirming the wiring slot is ready but unused. theme.ts line 69–77 explicitly removes the Plotly `toImage` modebar button with the comment "our own CSV/PNG buttons handle download", yet those buttons are never placed. The finding is accurate in every detail. The proposed fix — pass D
- fix_ok: Yes. Figure already has `actions?: ReactNode` (Figure.tsx:20,32). Passing `<DownloadCSV endpoint="..." filename="..." />` there is the right approach. The component and csv.ts helpers are complete and require no changes themselves. The only open question is which charts should expose a download endp

### [49] DownloadCSV error and loading states have static aria-label; AT users hear no state change
- area=ux-components kind=defect sev=LOW final=LOW conf=high
- loc: web/components/DownloadCSV.tsx:72  cat=Accessibility
- what: The button's `aria-label` is hardcoded as 'Download data as CSV' regardless of state. When the download fails (state='error'), the button shows '✕' but still announces 'Download data as CSV' to screen readers. Similarly during loading ('…'), AT users hear no indication of activity. `disabled` is only applied during loading, not during the error state, so a screen reader user could re-click during the 1.8s error reset window without knowing why the button shows '✕'.
- root: The aria-label was written for the idle state and not updated to reflect transient states.
- fix: Make aria-label state-aware: `state === 'loading' ? 'Downloading...' : state === 'error' ? 'Download failed, click to retry' : 'Download data as CSV'`. Also add `disabled={state !== 'idle'}` so the button is non-interactive during both loading and the brief error flash.
- verdict: confirmed | The code at web/components/DownloadCSV.tsx line 72 contains a hardcoded aria-label="Download data as CSV" that never changes regardless of state. Line 65 computes a state-aware visible buttonLabel ("…" for loading, "✕" for error) but the aria-label is divorced from that logic. Line 71 sets disabled={state === "loading"} but not during the error state. Both factual claims in the finding hold exactly as described: (1) AT users hear "Download data as CSV" during loading and error states, receiving no indication of activity or failure; (2) the button is re-clickable during the 1.8-second error win
- fix_ok: The proposed fix is sound. Make aria-label state-aware: state === 'loading' ? 'Downloading...' : state === 'error' ? 'Download failed, click to retry' : 'Download data as CSV'. Also change disabled to disabled={state !== 'idle'} so the button is non-interactive during both loading and the brief erro

### [50] Select component crashes if options array is empty
- area=ux-components kind=defect sev=LOW final=LOW conf=high
- loc: web/components/Select.tsx:18-19  cat=Defensive programming
- what: `const current = value ?? options[0].value` and `const isDefault = current === options[0].value` throw a TypeError if `options` is an empty array. While current callers always pass non-empty option arrays (GEO_OPTIONS, OCC_OPTIONS, IND_OPTIONS), the component has no guard and would crash silently in any future caller passing a dynamic/empty options list.
- root: No null/empty check before accessing `options[0]`.
- fix: Add an early return or guard: `if (!options.length) return null;` before line 18. Alternatively, provide a default value: `const current = value ?? options[0]?.value ?? '';`.
- verdict: confirmed | Lines 18–19 of web/components/Select.tsx do unconditionally access options[0] without any guard. The TypeScript prop type is `options: Option[]` which admits empty arrays at the type level. However, all current call sites (FilterSpine.tsx lines 28–48) pass only the three statically-defined module constants GEO_OPTIONS (12 entries), OCC_OPTIONS (11 entries), and IND_OPTIONS (22 entries) from web/lib/options.ts — none of which can ever be empty. There is no dynamic, API-fed, or filtered option list anywhere in the codebase. The crash path is therefore not reachable today. The finding is confirme
- fix_ok: Yes. Both proposed fixes are sound: `if (!options.length) return null;` before line 18 is the cleaner guard (prevents the component rendering meaninglessly with no options). The optional-chaining fallback `options[0]?.value ?? ''` is also valid but slightly more subtle. Either fix resolves the laten

### [51] Figure component renders two <figcaption> elements inside one <figure>
- area=ux-pages kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/components/Figure.tsx:29, 41  cat=Accessibility / HTML validity
- what: The Figure wrapper renders both the chart title (line 29) and the source note (line 41) as `<figcaption>` elements inside the same `<figure>`. HTML allows only one `<figcaption>` per `<figure>`. Screen readers and validators will flag this; assistive technology may announce only the first caption or behave unpredictably. This defect propagates to every chart on all 7 data pages.
- root: The author wanted the note to look like a figure caption (small, italic-style text below the chart) and reused the `<figcaption>` element rather than a `<p>` or `<div>`. The spec states a figure may contain at most one figcaption.
- fix: Keep the title as `<figcaption>` (it is the primary caption). Change the note container from `<figcaption>` to `<p>` (or `<footer>`) and restyle if needed. The existing `.text-[0.78rem] leading-relaxed text-ink-soft` classes can stay; just swap the element tag.
- verdict: confirmed | Reading /Volumes/ACLMR/jobads-dashboard/.claude/worktrees/redesign2/web/components/Figure.tsx confirms the finding exactly. Line 29 renders `<figcaption className="text-[1.02rem] font-bold ...">` for the chart title, and line 41 renders a second `<figcaption className="mt-3 border-t ...">` for the optional note, both inside the same `<figure>` element (line 25). The HTML spec (and WHATWG living standard) permits at most one figcaption per figure. This is not a lint artefact or style opinion — it is a structural HTML validity violation that propagates to every chart rendered through this compon
- fix_ok: Yes. The fix is sound: keep line 29 as `<figcaption>` (primary caption / accessible name for the figure) and change line 41 from `<figcaption>` to `<p>` (or `<footer>`). The existing Tailwind utility classes `mt-3 border-t border-hairline pt-3 text-[0.78rem] leading-relaxed text-ink-soft` transfer u

### [52] KPI strip: third tile (VS Last Year) uses a sign-prefix value with no arrow/colour chip, inconsistent with tiles 1–2
- area=ux-pages kind=craft sev=P2 final=P2 conf=high
- loc: web/app/page.tsx:113-117  cat=Visual consistency / content hierarchy
- what: Tiles 1 and 2 (Demand Index, Active Postings) display a coloured ▲/▼ chip built by the `delta` prop and styled `text-pos`/`text-neg`. Tile 3 (VS Last Year) puts the directional value directly into the `value` field via `fmtPct(kpis.active_yoy_pct, { sign: true })` and passes no `delta` prop. The result is a bare '−7.6%' in the large number slot with no coloured indicator and no arrow — visually inert beside the two tiles that carry clear direction signals. Tile 4 (Median Wage) intentionally has no direction, so the real inconsistency is tile 3.
- root: The YoY tile's entire data point IS the percentage change (there is no separate base value), so the developer pushed the formatted change into `value`. But the tile component's visual language expects `value` to be a neutral headline number and `delta` to carry direction colour. Using `value` for a directional figure silently drops the colour and arrow.
- fix: For tile 3, pass `delta={kpis.active_yoy_pct}` and a neutral `value` such as the formatted absolute level or keep the sign-formatted text but also pass `delta` so the chip renders. Alternatively, standardise tile 3 to use the same delta-chip pattern: `value={fmtPct(kpis.active_yoy_pct)}` (unsigned) and `delta={kpis.active_yoy_pct}` with `deltaLabel={t.kpiYoyContext}` — matching exactly how tile 2 handles its MoM change.
- verdict: confirmed | The code at web/app/page.tsx lines 112-118 shows tile 3 passes `value={fmtPct(kpis.active_yoy_pct, { sign: true })}` with no `delta` prop. KpiTile.tsx lines 27 and 46-59 show `hasDelta` is only true when `delta !== null && delta !== undefined`; without it the chip block is bypassed entirely and a bare `<span />` renders. The live-capture evidence (line 34) independently confirms the rendered output: "VS LAST YEAR −7.6%" with no arrow, while tiles 1 and 2 show "▼8.3%" and "▲11.7%". The orchestrator also flagged this independently as candidate finding #3. Root cause is correct: the only directio
- fix_ok: Yes. The proposed fix is sound. Pass `delta={kpis.active_yoy_pct}` to tile 3 so `hasDelta` becomes true and the coloured arrow chip renders. The `value` prop should then be either the unsigned percentage `fmtPct(kpis.active_yoy_pct)` (letting the delta chip carry direction), or keep the sign-formatt

### [53] Explore page hero uses a different layout template from every other page
- area=ux-pages kind=craft sev=P2 final=P2 conf=high
- loc: web/app/explore/page.tsx:14-19  cat=Page rhythm / consistency
- what: All seven other data/tool pages wrap the hero in `<section class='border-b border-card-border bg-gradient-to-b from-surface-alt/60 to-canvas'>` with `py-10 md:py-14` inside. Explore uses a bare `<div class='container-x py-10'>` with an `<header>` child and no border, no gradient, and no responsive padding step. The visual separator between the hero text and the AuthGate card is absent, making the page start feel unfinished relative to the rest of the product.
- root: The explore page appears to have been written or modified independently; the standard hero section template (border-b + gradient) was not applied.
- fix: Wrap the explore hero content in the standard pattern: `<section class='border-b border-card-border bg-gradient-to-b from-surface-alt/60 to-canvas'><div class='container-x py-10 md:py-14'>…</div></section>`. Move the `<header>` inside as a `<div class='max-w-2xl'>` block, consistent with other pages.
- verdict: confirmed | Reading web/app/explore/page.tsx lines 14-19 confirms the bare `<div className="container-x py-10">` with no border-b, no gradient, and no md:py-14. Checking all 7 other page.tsx files confirms they all use `<section className="border-b border-card-border bg-gradient-to-b from-surface-alt/60 to-canvas"><div className="container-x py-10 md:py-14">` — pulse/home (app/page.tsx:81-82), occupations (81-82), industries (58-59), wages (65-66), geography (89-90), skills (60-61), method (45-46), developers (163-164). The explore page is the sole exception. The live-capture.md (line 57) also describes t
- fix_ok: Yes. The proposed fix is correct: wrap the hero content in `<section className="border-b border-card-border bg-gradient-to-b from-surface-alt/60 to-canvas"><div className="container-x py-10 md:py-14">…</div></section>` and remove the outer bare `<div className="container-x py-10">`, consistent with 

### [54] fmtMonth always outputs English month names — all page eyebrow date stamps stay English in the French locale
- area=ux-pages kind=defect sev=MEDIUM final=MEDIUM conf=high
- loc: web/lib/format.ts:40-46  cat=Internationalisation
- what: `fmtMonth` uses a hardcoded English month array (`['Jan', 'Feb', …]`). Every page eyebrow date stamp (e.g. 'Géographie · Mar 2026') calls `fmtMonth(asOf)` regardless of the current locale, so a French-speaking user sees English month abbreviations mixed into otherwise French chrome. The `pulseDict` already ships a `monthsShort` array in both locales (en and fr), confirming the intent to localise months — but `fmtMonth` is never locale-aware.
- root: fmtMonth was written as a simple utility before the i18n system was introduced. It was never updated to accept or look up the current locale.
- fix: Accept an optional `locale` parameter (`'en' | 'fr'`, default `'en'`) or use `Intl.DateTimeFormat(locale, { month: 'short', year: 'numeric' })` so the output is natively localised. In each page, pass the resolved locale when calling `fmtMonth(asOf, locale)`. Alternatively, wire it to the existing `monthsShort` arrays already in `page-pulse.ts`.
- verdict: confirmed | The code confirms the defect. `fmtMonth` at `web/lib/format.ts:40-46` uses a hardcoded English MONTHS array and accepts no locale parameter. Every page calls it to render the eyebrow date stamp (e.g., `geography/page.tsx:91`: `{t.eyebrow} · {fmtMonth(asOf)}`). Pages do resolve locale via `getLocale()` and translate `t.eyebrow` correctly, but `fmtMonth(asOf)` is never passed the locale. In French the result is mixed-language: e.g. "Géographie · Mar 2026" instead of "Géographie · Mars 2026". The French `monthsShort` array in `page-pulse.ts:160` (`["Janv", "Févr", "Mars", ...]`) confirms localise
- fix_ok: Yes. Adding an optional `locale: 'en' | 'fr' = 'en'` parameter to `fmtMonth` (and `fmtMonthShort`) and branching on it (or using `Intl.DateTimeFormat(locale === 'fr' ? 'fr-CA' : 'en-CA', { month: 'short', year: 'numeric' })`) is the right approach. Each call site that has a locale in scope (all the 

### [55] Occupations treemap: the 'Unknown' 18% block (2nd-largest tile) has no explanation in the page or chart note
- area=ux-pages kind=craft sev=P1 final=MEDIUM conf=high
- loc: web/lib/i18n/dict/page-occupations.ts:28  cat=Honesty chrome / microcopy
- what: The treemap at the top of /occupations shows 'Unknown 110,651 (18%)' as the second-largest block — bigger than any named sector except Sales & service. There is no caveat, no note, and no explanation anywhere on the page about what 'Unknown' means (postings with no NOC code assigned). Readers are likely to treat it as a real occupation group or wonder if it reflects data quality, AI-exposed roles, or some other classification. The treemap's chart note says only 'Area ∝ postings in the selected year. Drag the slider or press play to move through time.' — completely silent on the unknown category. Visually, the block competes for prominence with the real top-level categories.
- root: The 'Unknown' category exists in the underlying data (postings with no NOC match) and the viz factory passes it through as-is. The page chrome was not updated to include a caveat, and no editorial decision was made to either suppress the block or de-emphasise it.
- fix: Add to the treemap chart note: 'Unknown = postings with no occupation code. This reflects coverage gaps in the source data, not a separate employer category.' Optionally, move 'Unknown' to visually last by sorting/ordering in the factory, or apply a muted fill so it reads as metadata rather than signal. The most direct fix is one sentence in the note in both EN and FR.
- verdict: confirmed | The finding is confirmed by direct code and live-capture evidence.

1. The `treemap()` function in `src/jobads_dashboard/viz/figures/occupations.py` does NOT call `_real_groups()`. Every other chart on the page (indexed_lines at line 71, contribution_bars at line 96, waterfall at line 116, dumbbell at line 138, ai_exposure_scatter at line 207, noc_naics_heatmap at line 165) filters out the Unknown category via `_real_groups()`. The treemap function passes raw `ds.noc_broad` directly to `_treemap_trace()`, so the Unknown group (rows where noc_name contains "Unknown") flows through as a data til
- fix_ok: Yes. The proposed fix is sound: add one sentence to the treemap note in both EN and FR in `web/lib/i18n/dict/page-occupations.ts` (line 28 for EN, line 95 for FR) explaining that Unknown = postings with no occupation code, reflecting source-data coverage gaps. A stronger fix would also apply `_real_

### [56] Skills page hero title has no terminal period — inconsistent with every other data page
- area=ux-pages kind=craft sev=P3 final=P3 conf=high
- loc: web/lib/i18n/dict/page-skills.ts:11  cat=Microcopy consistency
- what: The skills hero title 'What employers are asking for' renders without a terminal period. Every other page ends its hero h1 with a period: pulse adds it in JSX (`{headline}.`), occupations and industries add it in JSX (`{t.hero}.`), wages and method embed it in the dict value. Skills is the only declarative sentence that trails off with no terminal punctuation. French title ('Ce que les employeurs demandent') has the same gap.
- root: The period was not added either to the dict value or to the JSX render at `web/app/skills/page.tsx:65`.
- fix: Either add '.' to the dict string (`heroTitle: 'What employers are asking for.'`) or add it in JSX: `{t.heroTitle}.` — consistent with how occupations and industries do it.
- verdict: confirmed | The finding holds up under direct inspection. web/lib/i18n/dict/page-skills.ts line 11 has `heroTitle: "What employers are asking for"` (no terminal period), and web/app/skills/page.tsx line 65 renders it as `{t.heroTitle}` with no appended period. Every other declarative-sentence hero title in the app has terminal punctuation via one of two consistent patterns: (a) period embedded in the dict value (wages: "What job ads say about pay." at page-wages.ts:10; method: "How to read this dashboard." at page-method.ts:15), or (b) period appended in JSX (occupations page.tsx:86 `{t.hero}.`; industrie
- fix_ok: Yes. Add a terminal period to both EN and FR `heroTitle` values in web/lib/i18n/dict/page-skills.ts (lines 11 and 107), or append `.` in the JSX at web/app/skills/page.tsx line 65: `{t.heroTitle}.` — consistent with the occupations/industries/home pattern. Both approaches are correct; the dict-side 

### [57] Developers page has no i18n — all copy is hardcoded English on a bilingual product
- area=ux-pages kind=craft sev=P3 final=LOW conf=high
- loc: web/app/developers/page.tsx:164-238  cat=Internationalisation
- what: The Developers page contains entirely hardcoded English strings: the eyebrow 'Developers', the h1 'Public data API', the introductory paragraph, the section headers ('Shared parameters', 'Scope parameters', 'Reference', 'Endpoints', 'Aggregate data only'), and the privacy note. Every other page (pulse, occupations, geography, wages, skills, industries, method, explore) resolves its locale and drives UI copy from a dict. A French-speaking user who switches the locale toggle sees all other pages in French but lands on an all-English Developers page.
- root: API documentation is often treated as English-only in developer tooling, and the page was written before or outside the bilingual design decision. No `getLocale()` call and no dict were added.
- fix: Add a `developersDict` with EN/FR entries (eyebrow, h1, intro text, section headers, privacy note). The endpoint paths, param names, and type strings can remain English as they are code-level identifiers. At minimum, translate the surrounding prose to match the bilingual chrome on all other pages.
- verdict: confirmed | The finding is real and verifiable directly from source. web/app/developers/page.tsx (lines 159–238) contains no getLocale() call, no dict import, and no i18n dict file (there is no web/lib/i18n/dict/page-developers.ts). Every other content page — pulse (page-pulse.ts), occupations (page-occupations.ts), geography (page-geography.ts), wages (page-wages.ts), skills (page-skills.ts), industries (page-industries.ts), and method (page-method.ts) — has a matching dict file and calls getLocale() to resolve locale-driven copy. The developers page eyebrow ("Developers"), h1 ("Public data API"), intro 
- fix_ok: Yes. Adding a developersDict (mirroring the pattern in page-method.ts or page-pulse.ts) and calling getLocale() at the top of DevelopersPage is the correct fix. Endpoint paths, param names, and type-signature strings can remain English. The surrounding prose (eyebrow, h1, intro, section headers, pri

### [58] Pulse hero headline always renders in English regardless of locale
- area=ux-pages kind=craft sev=P2 final=P2 conf=high
- loc: web/app/page.tsx:72-76  cat=Internationalisation / content hierarchy
- what: The most prominent piece of content on the dashboard — the h1 headline 'Canada's posting demand is 8% below its 2019 baseline.' — is always generated in English from a hardcoded template string. When the user switches to French, the eyebrow, lede, KPI labels, and all chart chrome switch to French, but the h1 stays English. A code comment acknowledges this ('left in English as specified') but the outcome is a visible language inconsistency at the exact spot a French reader's eye lands first.
- root: The headline is constructed from live API data (the demand gap is a number derived at render time) and the team chose not to translate the surrounding sentence fragments. The pulseDict has no FR counterpart for the headline template.
- fix: Add FR headline template strings to `pulseDict.fr`: `headlineAbove: 'La demande d'offres au Canada dépasse de {n}% son niveau de 2019'` and `headlineBelow: 'La demande d'offres au Canada est inférieure de {n}% à son niveau de 2019'`. Use these templates when `locale === 'fr'` in the headline construction at line 73–76. The null fallback ('Canada\'s posting demand') also needs a FR form.
- verdict: confirmed | The finding is confirmed by direct code inspection. In web/app/page.tsx lines 72–76, the headline is constructed entirely in English with no locale branching. The comment on line 72 — "Headline is API-derived (composed from data) — left in English as specified." — confirms this was a deliberate deferral. The pulseDict FR entry (web/lib/i18n/dict/page-pulse.ts lines 112–216) contains fully translated strings for eyebrow, lede, KPI labels, and all chart chrome, but has no headline template keys at all (no headlineAbove, headlineBelow, or equivalent). The live-capture evidence (live-capture.md li
- fix_ok: Yes. Add FR template strings (e.g. headlineAbove and headlineBelow keys, plus a null fallback) to pulseDict.fr in web/lib/i18n/dict/page-pulse.ts, then branch on locale in the headline construction at web/app/page.tsx lines 73–76 to select the appropriate template. The approach is straightforward an

