### [1] wages() with dim='occupation' and dim='province' silently ignores the user's scope filters
- area=api-queries kind=defect sev=HIGH final=LOW conf=high
- loc: api/queries.py:548-598  cat=aggregation-math / scope
- what: When dim='occupation', the query reads from wage_by_noc which contains only province_scope='All Canada' and industry_scope='All industries'. When dim='province', wage_by_province contains only occupation_scope='All occupations' and industry_scope='All industries'. In both cases the function accepts scope.geo / scope.occ / scope.ind but applies none of them to the SQL — the WHERE clause is only `month = ? AND {vary} <> ?`. The result is that a user who has filtered to, say, BC + Health occupations sees national all-occupation wage bars labeled as if they are specific to that scope.
- root: The marginal wage tables (wage_by_noc, wage_by_province) are single-dimension roll-ups with all other scope dimensions set to the 'All …' sentinels. The wages() function does not add scope filters for the orthogonal dimensions, nor does it fall back to wage_cube (which does have cross-dimensional rows). There is no warning in the response that the returned wages are not scope-specific.
- fix: For dim='occupation': query wage_cube with WHERE province_scope=? AND industry_scope=? AND occupation_scope <> ALL_OCC AND occupation_scope <> UNKNOWN_OCC — i.e. use the same cross-dimensional cube that _kpis already uses. For dim='province': query wage_cube with WHERE occupation_scope=? AND industry_scope=? AND province_scope <> ALL_GEO. Add a 'scope_applied' boolean field to WagesResponse or add a caveat to the existing caveats list when the exact scope has no wage data and a fallback is used.
- verdict: refuted | The code-level claim is accurate: wages() with dim='occupation' queries wage_by_noc whose parquet contains only province_scope='All Canada', industry_scope='All industries', and wages() with dim='province' queries wage_by_province whose parquet contains only occupation_scope='All occupations', industry_scope='All industries'. Any scope parameters passed are never applied, confirmed by reading the actual parquet: `SELECT DISTINCT province_scope, industry_scope FROM monthly_wage_by_noc_broad` returns exactly one row `('All Canada', 'All industries')`.

However, the finding's severity of HIGH (da
- fix_ok: The proposed fix (querying wage_cube with cross-dimensional filters for dim='occupation' and dim='province') is technically sound and would be the correct approach if the endpoint is ever wired to a scoped UI. It is not needed now because the endpoint is unreachable from the frontend, but it is a re

### [2] wages() dim='occupation' unknown-sentinel exclusion is redundant and masks a structural gap
- area=api-queries kind=defect sev=LOW final=LOW conf=high
- loc: api/queries.py:576-580  cat=aggregation-math / correctness
- what: For dim='occupation', where_extra is built as an f-string literal: `f" AND {vary} <> '{unknown}'"` (line 576). This is safe only because vary and unknown are both hardcoded module-level constants, never user input. However the WHERE clause already excludes UNKNOWN_OCC via this extra clause, while wage_by_noc only holds 'All Canada + All industries' rows — UNKNOWN_OCC rows in that table have province_scope and industry_scope set to those sentinels too, so the query without the extra clause would still include them. The double-exclusion is harmless but signals that the function was written as if it were filtering a cross-dimensional table. The real fix (using wage_cube) would make both the all_sentinel and unknown exclusions meaningful.
- root: The wages() function was written against a table schema richer than what wage_by_noc actually provides, or the table was later narrowed to national-only without updating the query logic.
- fix: As part of the wage scope-fix (Finding 1), switch dim='occupation' to query wage_cube and the where_extra f-string can be replaced with parameterised placeholders, eliminating the non-parameterised string concatenation entirely.
- verdict: refuted | The finding claims `where_extra` in `wages()` for `dim='occupation'` is redundant because UNKNOWN_OCC rows in `wage_by_noc` supposedly also carry province/industry scope sentinels that would filter them out. Inspection of the real parquet file at `data/derived/labor_market_dashboard_v1/monthly_wage_by_noc_broad.parquet` refutes this entirely.

The actual table has exactly 11 distinct `occupation_scope` values: 10 NOC broad groups plus "Unknown occupation group". Every row — including UNKNOWN_OCC rows — has `province_scope = "All Canada"` and `industry_scope = "All industries"`. The query at li
- fix_ok: The proposed fix (switch to wage_cube and use parameterised placeholders) would be a legitimate refactor for scope-aware occupation-wage queries, but it does not fix a real bug here. The current code is correct for the table it actually queries.

### [3] postings_series() applies no lower date bound, returning full corpus history in every call
- area=api-queries kind=defect sev=LOW final=LOW conf=medium
- loc: api/queries.py:265-283  cat=performance / correctness
- what: _series_frame fetches all months from earliest_month to scope.end for the scope, with no scope.start filter applied. With 123 months in the corpus this is a modest table scan today, but every API call to /overview, /series/postings, and any consumer of _kpis fetches the full history regardless of the user's selected window. More importantly, because scope.start is silently ignored, the 'series' field in OverviewResponse always contains the full history even when the caller set a start date, which may be surprising to front-end consumers expecting a windowed result.
- root: _scope_where() only encodes geo/occ/ind dimensions; date range is intentionally omitted so _kpis can access the full history for its YoY calculation. But postings_series() is also the endpoint for /series/postings which the UI uses for the chart, so the front-end receives and must then re-filter the full series.
- fix: In postings_series(), after computing the full df via _series_frame, apply a start-date filter: `start_ts = pd.Timestamp(core.month_floor(scope.start)); df = df[df['month'] >= start_ts]`. _kpis already receives the full series (from overview()) before this filter, so split the call: keep _kpis using the full series and let postings_series filter for the chart endpoint.
- verdict: refuted | Reading queries.py lines 265–302 confirms that _series_frame fetches the full corpus history (no start-date SQL clause) and postings_series() only clips at scope.end. The finding's description of the code is accurate. However, the claim that this is a defect — that "scope.start is silently ignored" producing a wrong result — does not hold up.

First, the design is intentional. _kpis() (lines 377–410) indexes into the series at [-2] (MoM), [-13] (YoY), and uses all tail values for _streak(). These calculations require at least 13 months of history anchored at scope.end. If the series were filte
- fix_ok: No. The proposed fix is self-contradictory: it says "_kpis already receives the full series before this filter," but _kpis receives the return value of postings_series() directly (queries.py line 457-458), so filtering postings_series() by start_ts would truncate _kpis input and silently break the Y

### [4] geography() and _province_trends() omit scope.geo when fetching province-level rows
- area=api-queries kind=defect sev=LOW final=LOW conf=high
- loc: api/queries.py:485-510  cat=aggregation-math / scope
- what: geography() queries filter_cube with `WHERE month=? AND occupation_scope=? AND industry_scope=? AND province_scope <> 'All Canada'`. It correctly omits province_scope from the filter (since the point is to compare provinces), but this means that if a user selects a specific province (scope.geo = 'ON'), geography() still returns all provinces — i.e. the geographic breakdown ignores the geo filter entirely. The per10k and LQ values are then computed from those all-province totals, and items are sorted by value, so the user's geography filter has no effect on the geographic view.
- root: The function intentionally varies province to show a breakdown, but it does not guard against the case where the user has already selected a specific province. In that case the geography endpoint should either return a single province, signal that the geo filter conflicts with a geographic breakdown, or display the province within its regional context.
- fix: If scope.geo != core.ALL_GEO, either (a) restrict the WHERE to province_scope = scope.geo (single-province view) or (b) still return all provinces but highlight the selected province and add a 'filtered_to' note in GeographyResponse. Option (b) is more useful for comparison. At minimum document the intended behaviour.
- verdict: refuted | The finding assumes that a user can select a specific province (scope.geo = 'ON') and that this gets passed to queries.geography(), causing the geographic breakdown to silently ignore the province filter. This path does not exist in the codebase.

The Geography page (/Volumes/ACLMR/jobads-dashboard/.claude/worktrees/redesign2/web/app/geography/page.tsx) uses only api.figure() calls through the pre-built figure bridge (lines 53-61) — it explicitly comments "National only (the figure bridge is national by construction)" (lines 34-36). It has no province selector and never calls the REST /geograp
- fix_ok: The proposed fixes (option a: restrict to single province, option b: return all provinces with highlight) are both reasonable if the endpoint is ever wired to a province-aware UI, but neither is actionable now. The fix description is sound in principle but premature given the endpoint is currently u

### [5] DuckDB connection is shared across threads with a global lock but cursors are not guarded against concurrent execute()
- area=api-queries kind=defect sev=LOW final=LOW conf=medium
- loc: api/core.py:126-144  cat=correctness / concurrency
- what: query_df() creates a fresh cursor per call (`connection().cursor()`) and holds _LOCK only during connection initialisation, not during query execution. DuckDB's in-process connection object is not thread-safe for concurrent cursor execution even when individual cursors are used. Under uvicorn's async event loop with multiple concurrent API requests, two threads can hold different cursors on the same _CON and call cur.execute() simultaneously, leading to undefined behavior or silent data corruption.
- root: The comment 'Thread-safe via a fresh cursor' is incorrect: DuckDB cursors share the underlying connection state. The lock protects only the one-time initialisation of _CON, not per-query execution. A correct approach is either a connection pool (one connection per thread) or wrapping each query_df call in the lock.
- fix: Either: (1) use a threading.local() connection so each thread gets its own DuckDB connection: `_LOCAL = threading.local()` and `def connection(): if not hasattr(_LOCAL, 'con'): _LOCAL.con = duckdb.connect(':memory:'); return _LOCAL.con`; or (2) wrap the cur.execute() call inside a with _LOCK block. Option 1 avoids lock contention at the cost of separate in-memory databases — acceptable since all queries read from parquet files on disk.
- verdict: refuted | The finding's core claim is wrong. In DuckDB 1.5.4 (the installed version), `connection.cursor()` does not return a shared-state DB-API 2.0 cursor. Per DuckDB's own docstring it "Create[s] a duplicate of the current connection" — each call produces a new, independent `DuckDBPyConnection` object backed by the same in-process database, with its own execution state. This is not the same semantics as a DB-API cursor that shares underlying connection state. An empirical concurrency test (10 threads simultaneously calling `cursor()` and `execute()` on the same parent connection) produces correct res
- fix_ok: No. The proposed fixes are unnecessary. DuckDB cursor() creates a duplicate connection (independent execution state), not a shared-state DB-API cursor. The current design is correct. Wrapping execute() in a global lock would serialize all queries; using threading.local() would fragment the in-memory

### [6] Missing Vary header on locale-sensitive figure endpoint allows CDN cache poisoning
- area=api-bridge kind=defect sev=MEDIUM final=LOW conf=high
- loc: api/routers/figures.py:25-27  cat=caching / correctness
- what: GET /api/figure/{chart_id}?locale=fr returns French-localized Plotly JSON, and ?locale=en returns English. The Response object sets no Vary header. A CDN or reverse proxy that caches by URL path while stripping or ignoring query parameters (common Cloudflare, Varnish, and nginx proxy_cache_key configurations) will cache whichever locale was first requested and serve it for all subsequent locale variants. A user switching to French could receive English charts silently.
- root: The FastAPI Response is constructed with only content and media_type. FastAPI does not add Vary automatically. The locale is embedded in the URL query string, not in a request header, so no Vary: header is generated to signal to intermediaries that the response content depends on the locale parameter.
- fix: Add `headers={"Vary": "Accept-Language"}` (or `"Cache-Control": "private, no-store"`) to the Response constructor on line 25. Concretely: `return Response(content=figures.build(chart_id, locale=locale), media_type="application/json", headers={"Cache-Control": "private, max-age=3600", "Vary": "Accept-Language"})`. Using private + max-age delegates caching to the Next.js fetch layer (which already deduplicates by full URL including locale) while blocking shared CDN caches from conflating locales.
- verdict: refuted | The finding's threat model requires a CDN or reverse proxy that caches by URL path while stripping or ignoring query parameters. The actual deployment has no such intermediary. render.yaml shows a single Render.com free-tier web service; the Dockerfile (api/routers/figures.py, Dockerfile, render.yaml, web/next.config.ts) confirms a single-container setup where FastAPI listens only on 127.0.0.1:8530 (loopback, never exposed) and browser /api/* traffic goes through Next.js's own rewrite proxy — not through Cloudflare, Varnish, nginx proxy_cache, or any shared CDN. The Next.js server-side fetch c
- fix_ok: No. The proposed fix (`Vary: Accept-Language`) names the wrong dimension — locale is a query parameter, not the Accept-Language header. A Vary header on a response header that does not match the actual differentiator would give false protection. If a CDN were ever added, the correct mitigation would

### [7] figure/build() calls to_plotly_json() twice: once for is_multipanel, once for the final payload
- area=api-bridge kind=defect sev=LOW final=LOW conf=high
- loc: api/figures.py:240-246  cat=performance / correctness
- what: build() serializes the figure to JSON twice: first at line 240 via fig.to_plotly_json() to inspect the layout keys for the is_multipanel check, then again at line 246 via fig.to_json() to produce the payload string. For large figures (choropleth maps carrying a full TopoJSON topology), to_plotly_json() can be expensive. The two calls are also not equivalent: to_plotly_json() returns a Python dict merging template defaults into layout, while to_json() uses Plotly's custom encoder (numpy arrays, Timestamps). The is_multipanel heuristic therefore reads from the template-merged dict, which may reflect template-injected axes that the figure itself did not create.
- root: The margin reclaim and the final payload are computed separately without reusing the intermediate dict. The is_multipanel check was added as a patch after the main serialize path was established.
- fix: Derive is_multipanel from the graph-object state rather than the serialized dict: `is_multipanel = len(fig.layout._subplot_xaxis_keys) > 1` or simply `is_multipanel = any(k.startswith('xaxis') and k != 'xaxis' for k in fig.to_plotly_json().get('layout', {}))` — but reuse the same dict for the FR localization path too. Concretely: compute `data = json.loads(fig.to_json())`, derive `is_multipanel` from `data["layout"]`, apply margin correction to `data["layout"]` directly, apply `_localize_chrome(data)` when locale is fr, and return `json.dumps(data, ensure_ascii=False)` in all cases. This reduces serialization to one pass.
- verdict: refuted | The finding conflates two distinct issues that are both overstated or wrong.

1. "Two full serializations" is inaccurate. `to_plotly_json()` at line 240 calls `to_dict()`, which does a deepcopy of the internal `_layout` dict and returns a Python dict — no JSON encoding occurs. The one actual JSON encoding happens at line 246 via `to_json()`. So the code does one JSON encoding, plus one dict deepcopy (layout-key inspection only). The deepcopy overhead is ~2ms on a large figure and does not involve choropleth TopoJSON because that lives in trace `data`, not `layout`; the `is_multipanel` check ac
- fix_ok: The proposed fix is mechanically sound but solves a problem that does not exist at the stated severity. Collapsing to one `json.loads(fig.to_json())` pass is a minor cleanup; deriving `is_multipanel` from `data["layout"]` is equivalent to the current approach. The fix is safe to apply but not necess

### [8] register_templates() sets pio.templates.default on every call outside the _REGISTERED guard
- area=api-bridge kind=defect sev=LOW final=LOW conf=high
- loc: src/jobads_dashboard/viz/theme.py:177-184  cat=thread safety / initialization
- what: register_templates() is idempotent for template registration (guarded by _REGISTERED), but line 184 `pio.templates.default = default` executes unconditionally on every call. pio.templates is a module-level Plotly singleton. If register_templates() is ever called from concurrent threads after startup (e.g., from multiple worker threads at uvicorn startup, or from test fixtures), multiple threads write to the same global object simultaneously. In Python's GIL model this is not data-corrupting, but under multi-process uvicorn (--workers N) each process reinitializes the global independently, which is correct — however under uvicorn's thread pool (sync routes run in anyio thread pool workers), the unconditional write at line 184 on every call is unnecessary noise. More importantly, if the default ever diverges from 'aclmr_light' between calls (e.g., a test that sets a different default), the next call overwrites it silently.
- root: The _REGISTERED guard only protects template object creation, not the default-setting side-effect. The intent was idempotency but the implementation is partially idempotent.
- fix: Move `pio.templates.default = default` inside the `if not _REGISTERED:` block, or add a second guard `if pio.templates.default != default:`. Since register_templates() is called once at module import (api/figures.py:49) and is already protected by Python's import lock, this is a LOW-risk hardening: `if not _REGISTERED: pio.templates["aclmr_light"] = _template(LIGHT); pio.templates["aclmr_dark"] = _template(DARK); pio.templates.default = default; _REGISTERED = True`.
- verdict: refuted | The finding is structurally accurate — pio.templates.default = default at line 184 of src/jobads_dashboard/viz/theme.py is outside the _REGISTERED guard. However, the threat model does not hold against the actual codebase. Every call site (api/figures.py:49, src/jobads_dashboard/viz/review.py:118, src/jobads_dashboard/viz/figures/_common.py:9) invokes register_templates() with no arguments, so the default parameter is always the literal "aclmr_light". There is no caller that passes a different value and no test that sets a divergent default. The unconditional write at line 184 therefore always
- fix_ok: The proposed fix (move pio.templates.default = default inside the if not _REGISTERED block) is technically correct and would make the guard fully cover both side-effects. It is safe to apply. However, it fixes a hypothetical rather than a real defect, so it is optional hardening rather than a requir

### [9] DuckDB lock scope too narrow: _LOCK only guards connection creation, not concurrent cursor execution
- area=api-bridge kind=defect sev=LOW final=LOW conf=medium
- loc: api/core.py:127-146  cat=thread safety
- what: The _LOCK (threading.Lock) at line 127 is held only during _CON initialization (lines 133-136). query_df() acquires no lock during cursor creation or query execution. The comment at line 141 states 'Thread-safe via a fresh cursor', relying on DuckDB's documented guarantee that cursors from the same connection are independently thread-safe for reads in DuckDB >= 0.6. This is correct for DuckDB >= 1.1.0 (required by pyproject.toml). However, the PRAGMA threads=4 at line 136 tells DuckDB to use up to 4 internal threads per query. Under concurrent requests each spawning their own cursor, DuckDB may spawn up to 4*N threads where N is the number of concurrent FastAPI sync route threads. The PRAGMA is set once but applies connection-wide; there is no per-cursor throttle. Under high concurrency this could exhaust the thread pool or saturate CPU beyond what the single uvicorn worker intended.
- root: The PRAGMA threads=4 is appropriate for a dedicated process but, combined with uvicorn's anyio sync thread pool, the total thread count under load is N_requests x 4 DuckDB threads. The lock design is otherwise correct for the concurrency model documented by DuckDB.
- fix: Lower PRAGMA threads to 2, or add a threading.Semaphore to limit the number of concurrent DuckDB cursors in flight (e.g. `_SEM = threading.Semaphore(4)` and acquire it around query_df). Alternatively, switch to async DuckDB if the stack moves to async FastAPI routes. This is a deployment-time tuning concern; the correctness guarantee from DuckDB's cursor model stands.
- verdict: refuted | The finding is refuted on three grounds.

First, the lock design is correct and the code comment at line 141 is accurate. DuckDB >= 1.1.0 (as required by pyproject.toml) guarantees cursor-level read isolation on a shared connection. Each `query_df()` call gets its own cursor via `connection().cursor()`, executes, and closes it. No shared mutable state is accessed outside the double-checked lock that guards `_CON` initialization (lines 132-136). The reporter themselves concedes: "The lock design is otherwise correct for the concurrency model documented by DuckDB."

Second, the thread-multiplica
- fix_ok: The proposed fix is not harmful but is unnecessary. Lowering PRAGMA threads or adding a semaphore would reduce internal DuckDB parallelism without addressing any correctness issue. The existing cursor-per-query pattern with DuckDB >= 1.1.0 is the correct approach for this workload.

### [10] Unbounded offset allows authenticated user to paginate and dump the entire posting_lookup table
- area=api-auth kind=defect sev=MEDIUM final=LOW conf=high
- loc: api/routers/private.py:87  cat=private posting queries
- what: The offset query parameter is declared as Query(0, ge=0) with no upper bound. The /api/postings response always returns the real total row count matching the current filter. An authenticated client can request offset=0, offset=100, offset=200, … until offset >= total and reconstruct the full posting_lookup dataset across N requests (each returning up to 100 rows). The PostingsResponse.total field docstring claims it is 'capped for performance' but no cap exists anywhere in private.py or the router.
- root: No MAX_OFFSET or MAX_TOTAL constant is enforced. The description in models.py (line 296) was written as a forward-looking intent but was never implemented.
- fix: Define a MAX_TOTAL constant (e.g. 10_000) in private.py. In the postings() function, cap the COUNT(*) result: total = min(actual_count, MAX_TOTAL). In the router, add le=MAX_TOTAL to the offset query parameter or clamp it in the function. This prevents full enumeration while keeping the surface useful for the Explore view's intended page-by-page browsing.
- verdict: refuted | The finding claims this is a security/auth defect allowing unauthorized data exposure, but the premise is wrong for this system's threat model.

The `/api/postings` endpoint is protected by `require_session` (private.py router line 82). The auth model is a single shared password for a researcher/private surface — there are no per-user rows, no row-level ownership, and no concept of user-A's data being hidden from user-B. Any holder of a valid session cookie is explicitly authorized to browse all posting-level data; that is the purpose of the Explore surface.

The "unbounded offset allows full 
- fix_ok: The proposed cap (MAX_TOTAL constant, capping COUNT result, adding le= to offset) is technically sound as a performance guardrail, but the security framing is wrong. If applied, it should be described as a performance/resource-limit measure, not a security fix. The docstring in models.py:296 should 

### [11] posting_detail uses SELECT * — future schema additions are silently fetched into memory
- area=api-auth kind=defect sev=LOW final=LOW conf=medium
- loc: api/private.py:135  cat=private posting queries
- what: posting_detail executes SELECT * FROM posting_lookup WHERE posting_id = ?. The function then accesses named columns explicitly for the PostingDetail response. Any future parquet column added to posting_lookup (e.g. a raw PII field, internal scoring column, or annotator note) is silently fetched into the in-process DataFrame even though it is never returned to the client. If the column mapping in _row() or posting_detail() is accidentally extended to access a new column by name, it would be returned without a model change.
- root: SELECT * was used for convenience to avoid re-listing all columns; explicit field mapping provides an illusory safety net.
- fix: Replace SELECT * with an explicit column list matching exactly the fields consumed by PostingDetail. This makes the query self-documenting and ensures that adding a sensitive column to the parquet schema cannot accidentally reach the API response.
- verdict: refuted | The SELECT * at api/private.py:135 does load all parquet columns into the in-process DuckDB result, but nothing sensitive can reach the API response under the current code for three independent reasons:

1. Pydantic boundary. PostingDetail is a Pydantic model. posting_detail() constructs it by calling base.model_dump() (which only contains the 15 fields declared in PostingRow) plus the 11 explicitly-named fields on lines 144–155. Pydantic serialisation will only ever output declared model fields, so any extra DataFrame column is silently ignored at the serialisation boundary regardless of what
- fix_ok: The proposed fix (replace SELECT * with an explicit column list) is technically sound and would improve consistency with the postings() list path. However, it is not necessary for correctness or security given the Pydantic boundary. If applied, it should list all columns consumed: posting_id, month,

### [12] shift_share: zero-base national groups silently deflate industry_mix and inflate competitive_shift
- area=viz-compute kind=defect sev=MEDIUM final=LOW conf=high
- loc: src/jobads_dashboard/viz/compute.py:109  cat=Economics math correctness
- what: When a NAICS or NOC group has zero national postings in the base period, `g_grp[j]` becomes NaN (line 109: `grp_b.replace(0, np.nan)`). In the inner loop, `im = (e_rg_b * (g_grp - g_nat)).sum()` drops the NaN term silently because pandas `Series.sum()` uses `skipna=True` by default. The identity NS+IM+RS = actual still holds mechanically because RS = actual − NS − IM absorbs the dropped term. But the economic attribution is wrong: growth in a newly-emerged group (zero base nationally) is entirely credited to competitive shift rather than industry mix, with no warning. Given that partial NAICS/wage field coverage is an explicit product caveat, new or previously-uncovered groups will routinely trigger this path.
- root: Using `grp_b.replace(0, np.nan)` produces NaN group growth rates, and pandas `.sum(skipna=True)` silently omits those NaN products from the IM computation. The RS residual absorbs the missing attribution, distorting the IM/RS decomposition without raising any error or flag.
- fix: Before the loop, identify groups where `grp_b[j] == 0` and handle them explicitly: either (a) set g_grp[j] = 0 (treat new groups as zero national growth, keeping their regional growth entirely in RS, which is the standard convention) or (b) exclude those groups from all components and document the exclusion. Option (a): replace `grp_b.replace(0, np.nan)` with `grp_b.where(grp_b != 0, 0.0)` — but this requires a rethink since 0/0 is still undefined. The simplest correct fix is to set g_grp[j] = g_nat for all zero-base groups (so IM[j] = 0 for those groups, and their entire regional impact goes to RS), which matches standard SS practice for entries. Document this in the docstring.
- verdict: refuted | The finding claims that a group with zero national postings in the base period (`grp_b[j] == 0`) can have a non-zero regional base value (`e_rg_b[j] > 0`), causing a meaningful NaN term to be silently dropped by `skipna=True`. This is structurally impossible.

`grp_b = piv_b.sum(axis=0)` is the column sum across ALL regions in `piv_b` (line 107). Since `piv_b` is built with `fill_value=0.0` and job-postings are non-negative, `grp_b[j] == 0` implies every region's base value for group `j` is zero. Therefore `e_rg_b[j] = piv_b.loc[r][j] == 0` for every region `r`.

At line 115: `e_rg_b[j] * (g_g
- fix_ok: The proposed fix is unnecessary. The current code is correct because the zero-base-group path always carries a zero regional weight, so the NaN term contributes exactly zero to IM. If the maintainer wants to eliminate the NaN multiplication for clarity, using `grp_b.replace(0, 1.0)` (so that 0-base 

### [13] classical_decompose: seasonal centering uses full-series observation mean instead of 12-unique-month mean, biasing seasonal factors for unbalanced series
- area=viz-compute kind=defect sev=LOW final=LOW conf=high
- loc: src/jobads_dashboard/viz/compute.py:199  cat=Economics math correctness
- what: At line 199, `seasonal = seasonal_means - seasonal_means.mean()` subtracts the mean over all N observations (where N may not be a multiple of 12). If the series has an unbalanced number of months (e.g. 61 months: January appears 6 times, other months 5), `seasonal_means.mean()` up-weights January's raw seasonal by its extra occurrence. This produces unique monthly seasonal factors that do not sum to zero over one cycle — a test on a 61-month series with seasonal amplitude ~50 units gave a sum error of 9.1 rather than 0. The bias is a constant offset applied to all 12 factors, so relative inter-month comparisons are preserved, but the factors reported to the chart and residuals are shifted by that constant. For `robust_z` on residuals, the constant is absorbed by the median subtraction, so anomaly detection is unaffected; the chart display of the seasonal component is the affected output.
- root: `seasonal_means.mean()` computes an observation-count-weighted average over the full time axis, not the unweighted mean of the 12 unique monthly mean values. The standard classical decomposition normalises by the simple average of the 12 period averages.
- fix: Replace line 199: `seasonal = seasonal_means - seasonal_means.mean()` with `seasonal = seasonal_means - detr.groupby(detr.index.month if hasattr(detr.index, 'month') else (np.arange(len(detr)) % period)).mean().mean()`. This subtracts the unweighted mean of the 12 unique period means, ensuring the 12 seasonal factors always sum to zero regardless of series length.
- verdict: refuted | The cited line (compute.py:199) does contain the described observation-count-weighted centering: `seasonal_means` is produced by `.transform("mean")`, so it has one entry per time-series observation, and `.mean()` on it does give an unbalanced-series-weighted result rather than the simple unweighted mean of the 12 period means. The reporter's algebra is correct in isolation.

However, the finding is refuted on materiality grounds: `classical_decompose` and `robust_z` are dead code. A comprehensive grep shows exactly two occurrences of each function name across the entire codebase — both are th
- fix_ok: The proposed fix is mathematically correct. Replacing `seasonal_means.mean()` with `detr.groupby(...).mean().mean()` would give the standard unweighted mean of the 12 period averages. However, the fix is only worth applying if `classical_decompose` is ever connected to a live figure; in the current 

### [14] Cross-section charts silently blank when exact base/end months absent from data
- area=viz-figures kind=defect sev=HIGH final=LOW conf=high
- loc: src/jobads_dashboard/viz/figures/occupations.py:94–111, 114–134, 137–158; also industries.py:103–116; geography.py:150–170; pay.py:64–79, 82–114, 124–151  cat=data correctness / silent failure
- what: Seven figure factories use hardcoded exact timestamps (`base=2019-06-01`, `end=2024-12-01`) when slicing the data for decomposition and cross-section charts. `compute.contribution_to_growth` and the dumbbell/waterfall factories do an exact equality lookup (`df[df['month'] == base]`). If the derived parquet bundle does not contain those exact months — because the data starts later, ends earlier, or uses different date offsets — the slice returns an empty Series. `contribution_to_growth` then returns `contribution_pp=NaN` for every group (the `if total_base else np.nan` branch), the waterfall and dumbbell silently render with no visible data, and `wage_dumbbell` / `wage_demand_quadrant` / `education_wage_proxy` draw empty scatter plots. No assertion, warning, or fallback is raised.
- root: `_stable_window()` and `_STABLE_END = pd.Timestamp('2024-12-01')` are module-level constants computed at import time, independent of the actual data available. The exact-equality lookup in `compute.contribution_to_growth` was designed for known-clean data and does not fall back to the nearest available month.
- fix: Replace exact equality with nearest-available lookup in each factory: `end = df[df['month'] <= _STABLE_END]['month'].max()` (and equivalently for `base`). Alternatively, expose a `nearest_month(df, target)` helper in `compute.py` and use it at every call site. Add an assertion or early-return guard that raises or logs a warning when the resolved month differs from the target by more than one month.
- verdict: refuted | The finding rests on two claims: (a) the hardcoded timestamps might not exist in the data, and (b) exact-equality lookups would silently return empty slices. Both are wrong given the actual codebase.

1. load_tables (src/jobads_dashboard/dashboard/data.py:79) calls `pd.to_datetime(frame["month"])` unconditionally on every table that has a month column. The raw parquet stores datetime.date objects (confirmed: direct read returns dtype=object, and `pd.Timestamp('2019-06-01') in df['month'].values` is False). After load_tables, every month column is datetime64[ns], and the equality comparisons wo
- fix_ok: The proposed `df[df['month'] <= target]['month'].max()` nearest-available fallback is a sound defensive pattern, but it is not needed for the current data. If adopted, it should cover all seven call sites consistently and add the suggested warning when the resolved month differs from the target by m

### [15] add_unstable_band shading window hard-wires 2016 as the left boundary
- area=viz-figures kind=defect sev=LOW final=LOW conf=high
- loc: src/jobads_dashboard/viz/theme.py:230–237  cat=honesty chrome correctness
- what: `add_unstable_band` sets `x0 = UNSTABLE_BEFORE.replace(year=2016)`, hard-coding 2016 as the left edge of the unstable zone. If the data window changes (e.g. a dataset starting in 2018 or earlier), the shaded band will either overshoot the actual data range or be absent from the visible area entirely. The function also has no parameter to override this left boundary. The `quality.coverage_lines` and `pay.language_gap` charts both call it, producing potentially misleading shading.
- root: The 2016 value was chosen to match the known data start year and was inlined rather than derived from `DataSource.first_month` or passed as a parameter.
- fix: Add an `x0` keyword argument to `add_unstable_band` (defaulting to `UNSTABLE_BEFORE.replace(year=2016)` for backwards compatibility). Figure factories that have access to a `DataSource` should pass `x0=ds.first_month` so the shaded band is anchored to the actual data start. Alternatively, derive the boundary from the figure's existing traces at call time, analogous to how `add_provisional_band` infers `x1`.
- verdict: refuted | The hard-coded value `UNSTABLE_BEFORE.replace(year=2016)` produces `2016-01-01`, which is the exact first month in every relevant dataset: `monthly_overall.parquet`, `coverage_by_field_monthly.parquet`, and `monthly_language.parquet` all start on 2016-01-01. There is no mismatch between the shaded band's left edge and the actual data range. The finding's concern is purely hypothetical — "if the data window changes" — but no such change has occurred and the function is not broken today. The proposed fix (adding an `x0` parameter) would be a reasonable defensive improvement, but it addresses a n
- fix_ok: The proposed fix is structurally sound as a defensive improvement, but it is not needed to correct a current bug. If it were applied, defaulting `x0` to `UNSTABLE_BEFORE.replace(year=2016)` preserves backward compatibility. Passing `ds.first_month` from callers would make the band data-driven. Howev

### [16] CSV escapeField does not sanitize formula-injection characters (=, +, -, @)
- area=web-lib kind=defect sev=MEDIUM final=LOW conf=high
- loc: web/lib/csv.ts:4-12  cat=security / CSV export
- what: escapeField wraps fields containing commas, double-quotes, or newlines in RFC-4180 quotes, but does not prefix-escape fields whose first character is `=`, `+`, `-`, or `@`. Spreadsheet applications (Excel, LibreOffice Calc) interpret these as formula prefixes. Job-posting fields (job_title, employer, description_full) come directly from scraped internet data and could contain values like `=HYPERLINK("http://evil.com","Click me")`, which would execute as a formula when the CSV is opened.
- root: The CSV helper was written to be RFC-4180 compliant, which does not address formula injection. The OWASP recommendation for untrusted data is to prepend a tab or single-quote before any cell starting with =+-@.
- fix: In escapeField, after computing `s`, add: `if (/^[=+\-@]/.test(s)) return `'${s}`;` (prefix with a single quote, which neutralises the formula in most spreadsheet apps while being visible). Alternatively prefix with `\t` (tab). Apply before the comma/quote wrapping check so quoted fields are also covered.
- verdict: refuted | The formula-injection gap in escapeField is real at the code level — the function at web/lib/csv.ts:4-12 does not prefix-escape fields starting with =, +, -, or @. However, the finding's MEDIUM severity classification requires a reachable attack surface, and there is none. The DownloadCSV component (web/components/DownloadCSV.tsx) is the sole consumer of toCSV/downloadCSV, and that component is never imported or rendered anywhere in the application — no page, layout, or other component mounts it. A comprehensive grep of all .tsx/.ts files under web/ (excluding node_modules) shows zero import s
- fix_ok: The proposed single-quote prefix fix is directionally correct but the tab prefix is safer across spreadsheet applications. Neither matters until the DownloadCSV component is actually wired into a page — the fix should be applied at that time, not now.

### [17] Locale toggle buttons have ~24px touch target height — fails WCAG 2.5.5 and is borderline on 2.5.8
- area=ux-global kind=defect sev=MEDIUM final=LOW conf=high
- loc: web/components/LocaleToggle.tsx:28-38  cat=Accessibility / Touch targets
- what: Each locale button (EN / FR) uses px-2 py-1 with text-[0.7rem]. The rendered height is approximately 11.2px × 1.4 line-height + 8px padding = ~23.7px. WCAG 2.5.8 (AA, WCAG 2.2) requires a minimum 24×24px click target. The button falls just under this floor. WCAG 2.5.5 (AAA) requires 44×44px. For a language toggle that users may need on every page visit, this is a real usability defect on mobile and touch screens.
- root: The toggle was designed as a tight inline control to fit the nav bar. The py-1 (4px top + 4px bottom) padding was not sized for touch interaction.
- fix: Change py-1 to py-2 (8px top + 8px bottom), which brings the touch target to ~31px — clearly above 2.5.8's 24px floor. For a closer approach to 2.5.5 AAA (44px), use py-2.5 or add a min-h-[44px] with flex items-center on the container div.
- verdict: refuted | The prior auditor calculated a rendered button height of ~23.7px using line-height 1.4, but the actual body line-height in globals.css (line 129) is 1.6. Buttons in LocaleToggle.tsx inherit this value. With text-[0.7rem] = 11.2px font size and line-height 1.6, the content box is 11.2 × 1.6 = 17.92px. Adding py-1 (4px top + 4px bottom = 8px total vertical padding) gives a rendered height of ~25.9px. This is above the WCAG 2.5.8 24px minimum, so the finding's core claim that the button "falls just under" the 24px floor is wrong. The finding's arithmetic is built on a line-height assumption (1.4)
- fix_ok: The proposed py-1 → py-2 change is technically harmless and would push the target further above the floor, but it is not needed to satisfy WCAG 2.5.8. It is a cosmetic improvement, not a bug fix. If the team wants to pursue WCAG 2.5.5 AAA (44px), a min-h-[44px] approach is still valid but that is an

### [18] Suspense fallback in layout renders a 68px height placeholder on all non-explore pages, causing a brief layout shift
- area=ux-global kind=defect sev=LOW final=LOW conf=medium
- loc: web/app/layout.tsx:60-62  cat=Layout / CLS
- what: FilterSpine is wrapped in Suspense with a fallback that renders an h-[68px] placeholder bar. FilterSpine itself returns null on every route except /explore (line 16 of FilterSpine.tsx). During server-side streaming and initial hydration on non-explore pages, the fallback renders, then disappears — producing a 68px layout shift. On /explore, the fallback may also not match the actual FilterSpine height (which includes labels, selects, and py-3 padding, making it taller than 68px on mobile wrap breakpoints).
- root: FilterSpine is a Client Component that uses usePathname() to decide whether to render. The Suspense wrapper was added to cover the asynchronous streaming phase. Since FilterSpine is client-only, the server always streams the fallback first, then the client resolves. The fallback height is a best-guess approximation that only matches the desktop, one-row FilterSpine layout.
- fix: Since FilterSpine.tsx is 'use client' and returns null on most routes, the simplest fix is to pass the current pathname from the server-rendered layout as a prop or use a server-side segment-based check, letting the server emit null directly instead of the fallback. Alternatively, change the fallback to null (removing the placeholder div) and accept the brief paint-in rather than the layout shift — a pop-in is less jarring than content that shifts downward.
- verdict: refuted | The finding assumes that Next.js App Router always streams the Suspense fallback before the Client Component resolves, producing a 68px placeholder on non-explore pages. This is incorrect for this specific case.

FilterSpine.tsx uses only `usePathname()` (line 3, 10). In Next.js App Router, `usePathname()` is synchronous and resolves during server-side rendering of Client Components — it does NOT cause a component to suspend. The Suspense boundary only intercepts components that actually call `use()`, `useSearchParams()`, or otherwise suspend. Because `usePathname()` never suspends, the Client
- fix_ok: The proposed fix is technically sound — using a null fallback or threading the pathname as a prop are both valid improvements. However, they solve a non-problem: since `usePathname()` resolves synchronously in App Router SSR, the fix has no observable effect on CLS. It is a reasonable code-clarity c

### [19] Sparkline endpoint dot uses SVG radius in viewBox units; appears as a compressed pixel at narrow widths
- area=ux-components kind=craft sev=P3 final=LOW conf=high
- loc: web/components/Sparkline.tsx:50  cat=Chart craft / visual detail
- what: The endpoint circle is defined as `r={2.4}` in SVG viewBox coordinates. The stroke uses `vectorEffect='non-scaling-stroke'` to stay crisp at any scale, but this attribute applies only to `<path>`, not to `<circle>`. At mobile widths where the SVG is rendered at ~47px wide (from a 132px viewBox), the circle radius scales to 2.4 × (47/132) ≈ 0.85px — barely a sub-pixel dot, losing its intended accent-dot purpose.
- root: `vectorEffect='non-scaling-stroke'` is not inherited by sibling elements; the circle needs its own `vectorEffect` attribute or should use a fixed pixel radius via CSS.
- fix: Add `vectorEffect='non-scaling-stroke'` to the `<circle>` element, and change `r={2.4}` to a stroke-width-relative radius so it reads as a visible dot at all scales. Alternatively, use `strokeWidth={4}` with `r={0}` to turn it into a point marker — that scales cleanly as a stroke. Or simply add `vectorEffect="non-scaling-stroke"` to the circle and increase `r` to ~3 for a consistently visible dot.
- verdict: refuted | The finding's root cause and proposed fix are both wrong. The circle at line 50 uses `fill={stroke}` with no stroke attribute — it is a pure fill shape. `vectorEffect="non-scaling-stroke"` only affects stroke rendering; adding it to a fill-only `<circle>` does nothing. The reporter's claim that the circle "needs its own `vectorEffect` attribute" is therefore incorrect.

The geometric scaling claim (r = 2.4 scales with the viewBox) is factually true, but the 47px scenario is not grounded in the actual layout. The SVG has `style={{ width: "100%", maxWidth: width, height }}` where `width` default
- fix_ok: No. Adding `vectorEffect="non-scaling-stroke"` to a fill-only circle has no effect on fill geometry — the attribute only governs stroke paint. The "strokeWidth={4} with r={0}" alternative suggested would make the dot invisible (a zero-radius filled circle). The fix does not address the actual scalin

### [20] Wages hero lede runs to 3+ lines — over-long for a two-sentence hero intro
- area=ux-pages kind=craft sev=P3 final=LOW conf=high
- loc: web/lib/i18n/dict/page-wages.ts:12  cat=Reading measure / content hierarchy
- what: The wages lede is 251 characters (~3.4 lines at max-w-2xl with the lede font). Other page ledes run 130–155 characters (~2 lines), which is the intended reading measure for a hero intro. The wages lede packs three distinct ideas into two sentences: (1) partial wage coverage, (2) skew toward roles where pay signals recruitment, and (3) that the dashboard shows ranges not point estimates. The information is accurate and important, but the density is higher than any other hero paragraph and sets a slower-to-scan tone for the page entry point.
- root: Wage data carries more caveats than other data (coverage + advertised vs paid + ranges vs medians) and the author addressed all three in the hero paragraph. The result is thorough but denser than the ~2-line standard.
- fix: Trim to the two most essential ideas at entry: coverage caveat and the range-not-point-estimate framing. Move the 'skewed toward roles where pay is a recruitment signal' nuance to the chart note of the wageBand figure, where it belongs as a data-quality observation. Suggested: 'Only a share of postings list a wage, so coverage is partial. This page shows hourly ranges — the 25th, median, and 75th percentile of posted wages — not point estimates.'
- verdict: refuted | The auditor's finding rests on a false baseline. The claim is that "other page ledes run 130–155 characters (~2 lines)" — but the skills page lede (page-skills.ts:13) is 210 characters, which already exceeds that ceiling by 36%, yet was not flagged. The wages lede is 251 characters — only 19% longer than skills. There is no codebase standard, design spec, or instruction in CLAUDE.md/AGENTS.md that sets a ~2-line / 130–155 character limit for hero ledes. The three ideas packed into the wages lede (partial coverage, coverage skewed toward pay-signal roles, ranges not point estimates) are all ent
- fix_ok: No. The proposed fix is reasonable as a preference but is not justified by any codebase standard. The claim that skills/geography/occupations ledes are all ≤155 characters is false (skills is 210 chars), so the comparative argument collapses. The suggestion to move the skew caveat to the wageBand ch

