The decorative pixel-tile motif — a deterministic mosaic of squares in the
four gradient stops (navy biased left, orange right). Use small clusters near
headings or summaries and wider grids as restrained panel decoration.

    <PixelTiles rows={3} cols={3} size={8} />
    <PixelTiles rows={3} cols={12} size={10} gap={2} />

Always decorative (`aria-hidden`). It never carries text, meaning, or identity
and never substitutes for or becomes part of the official ACLMR wordmark.
