The decorative pixel-tile brand motif — a deterministic mosaic of squares in the four gradient stops (navy biased left, orange right); use 3x3 beside the wordmark, wider grids as hero/panel decoration.

    <PixelTiles rows={3} cols={3} size={8} />
    <PixelTiles rows={3} cols={12} size={10} gap={2} />

Always decorative (aria-hidden). Never carries text or meaning; pair it with a wordmark or heading so the composition isn't a bare fragment.
