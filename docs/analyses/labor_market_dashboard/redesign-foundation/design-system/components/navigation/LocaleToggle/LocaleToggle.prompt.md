The EN/FR language switch that sits in the top nav — a square two-segment control, active segment navy.

    <LocaleToggle locale="en" onChange={(l) => setLocale(l)} />

Every production string is bilingual; place this in any chrome you design.
