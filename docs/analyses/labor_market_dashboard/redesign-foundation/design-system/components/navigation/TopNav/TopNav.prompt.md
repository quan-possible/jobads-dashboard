The dashboard's sticky header — signature gradient ribbon on top, brand lockup left, bold uppercase tabs with a 3px orange underline on the active tab, EN/FR toggle right; the gated Explore tab is an outlined orange pill shown only when authenticated.

    <TopNav activeHref="/occupations" />
    <TopNav authenticated activeHref="/explore" />

Desktop treatment only. Needs a wide canvas (1100px+); links wrap poorly below that.
