The redesign's sticky header — solid ACLMR navy shell, official light wordmark,
bold uppercase tabs with a 3px orange active underline, EN/FR toggle right, and
a thin gradient rail below. The gated Explore tab is a gradient-ring pill shown
only when authenticated.

    <TopNav activeHref="/occupations" />
    <TopNav authenticated activeHref="/explore" />

`tone="source"` is reserved for the source-state comparison kit. Do
not use it as the redesign default.

Desktop treatment only. Needs a wide canvas (1100px+); links wrap poorly below that.
