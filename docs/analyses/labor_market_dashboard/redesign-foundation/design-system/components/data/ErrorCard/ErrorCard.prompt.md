The route-level failure state — centred card with an orange eyebrow, an uppercase apology heading, the cause, and a square Retry.

    <ErrorCard reset={reset} title="view" />
    <ErrorCard reset={reset} title="explore" body="generic" locale="fr" />

Renders its own container-x + tall vertical padding; that air is the real page layout, not a preview defect.
