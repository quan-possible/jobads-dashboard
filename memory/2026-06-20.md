# 2026-06-20

## Deep audit (audit-only, no code changed)

- Ran `/deep-audit` over the Streamlit dashboard. The skill is templated for the React
  "Prism" app; adapted it to this Python/Streamlit project (same four phases + job-file
  structure, Streamlit/pytest/preview substituted for React/npm/Vite).
- Findings job (OPEN): `docs/jobs/active/2026-06-20-deep-audit/` — `findings.md`
  (defects S/L), `ux-audit.md` (craft U), `fix-spec.md` (fix per item),
  `remediation-plan.md`, `rejected.md`, `JOB.md`, plus `evidence/`.
- Result: **31 confirmed defects + 2 uncertain** (0 HIGH, 11 MEDIUM, 20 LOW) and **9
  design-craft items**. No HIGH: the data/logic surface is sound.
- Headline clusters: wide-table column overlap (`table-layout:fixed`, illegible on
  mobile — L01); dark-color-scheme metric labels go white-on-white (L02, fix = pin
  `color-scheme: light`); security batch (S01 LIKE escaping, S03 Markdown injection in
  posting detail, S02 no auth rate-limit); robustness (S05 empty-bundle IndexError, S06
  metadata I/O escapes the branded panel); data-display (S08 duplicate Data-Quality
  month card, S09/S10 misleading share denominators); ETL (S15 schema validation covers
  2 of ~13 files, S16 untrimmed NOC/NAICS, S21 unconditional posting-lookup scan).
- Method: live walk on an ungated local instance (`:8530`), plus an 11-slice Sonnet
  code fan-out with adversarial verification (10 candidates refuted → `rejected.md`).
  Baseline gate at audit time: `py_compile` OK, `pytest -q` 29 passed.
- Audit-only at first: no app code/config changed; the public `:8522` service was left
  running and healthy.

## Deep-audit fixes implemented (same day)

- User then said "implement the fixes". Applied `fix-spec.md` in `remediation-plan.md`
  order on branch **`audit/fixes-2026-06-20`** (still **uncommitted** — held per the
  commit rule; user owns commit + redeploy).
- Product decisions taken first (via question): S09/S10 → **keep true national basis,
  relabel honestly** (heatmap caption + chart title "share of NOC-coded postings"), not
  a math change; S21 → **gate the Explore lookup behind the Search button** (snapshot
  scope at submit so unrelated reruns never re-scan the 105 MB file).
- Applied 29/31 defects + 6/9 craft across `app.py`, `prepare.py`, `data.py`, `cli.py`
  (+ tests). Highlights: FIX-A tables auto-layout+scroll; FIX-B = **new
  `.streamlit/config.toml [theme] base=light`** (the CSS alone did NOT fix it — Streamlit's
  theme JS flips text tokens to near-white in a dark browser regardless of
  `color-scheme:light`; pinning base=light is the real fix); FIX-C window split; U01
  teal/orange change colors; S01 LIKE-escape; S03 `escape_markdown`; S15 full schema
  validation; S16 TRIM noc/naics.
- Not applied (deliberate): S27 (no zero wages in data), S23 (Streamlit-inherent),
  U02 (no skill-label source), U08 (font self-hosting needs asset bundling), L06 edge-clip
  (legend move removes the overlap; residual clip needs live mobile tuning).
- Verified: `pytest -q` **34 passed** (29 + 5 new); live render on ungated `:8531`
  confirmed the visual fixes via `getComputedStyle`; AppTest confirmed S08/S11/S12/S21
  with zero exceptions. Public `:8522` untouched.
- Next: review branch → commit → redeploy via ngrok/Cloudflare (AGENTS rule 6) → close
  job to `docs/jobs/done/`. Job ledger updated with the full applied/deferred list.
