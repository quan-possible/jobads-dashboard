# 2026-08-12

## Dashboard redesign production cutover

- Authorization: Bruce approved the previously gated production cutover with
  “ok then do it.” The existing Mac Next.js/FastAPI/Cloudflare topology was the
  release target; no parallel service was created.
- Integration: fetched origin, confirmed canonical `463d919a` was the direct
  ancestor of audited candidate `b7bebe5b`, fast-forwarded local `main`, and
  preserved rollback ref
  `backup/main-pre-dashboard-redesign-20260812-463d919a`.
- Security/configuration: copied the exact installed public LaunchAgent to
  `tmp/deployment/2026-08-12-dashboard-redesign/com.aclmr.jobads-dashboard-public.plist.before`
  (SHA-256
  `05de524d64fdf5030dc0471b2be4885d4761fed6d3d15388ac6a21877afb2795`),
  added Uvicorn `--no-proxy-headers`, validated the plist, and restarted only
  `com.aclmr.jobads-dashboard-public`. The Cloudflare LaunchAgent remained
  running and the hostname stayed
  `https://topics-marion-although-restore.trycloudflare.com`.
- Build and data: installed the locked dependencies, built Next 16.3 with build
  ID `R1XRrat8L-kJFQH4OLwqr`, passed 362 Python tests, ESLint, TypeScript, both
  zero-vulnerability npm audits, and reconciled all 25,356,735 postings with the
  upstream source window.
- Runtime: local and public health passed. Anonymous and anonymous `full=1`
  treemaps both showed 10 categories; a verified team session showed 11 and
  returned `private, no-store`. Posting list/detail responses were also
  `private, no-store`. Ten wrong-password attempts with rotating forged XFF
  returned eight `401` then two `429`, confirming the repaired proxy boundary.
- Live product: in-app Browser inspection passed the desktop Pulse and mobile
  Pulse/Geography/Explore surfaces, mobile menu Escape focus return, EN/FR
  switching, real team login, 25-row posting results, selected-posting detail
  without known mojibake or literal entities, and logout relock. Deployed
  screenshots are under the redesign job's deep-audit evidence.
- Publication boundary: after committing this cutover record, local `main` is
  18 commits ahead of `origin/main` and
  was deliberately not pushed. Render CLI authority is expired and the
  reachable Render hostname appears to expose an older Streamlit build; pushing
  could silently create a second, conflicting production. The missing Keychain
  credential remains a hardening follow-up; the existing mode-600 local source
  matched the installed production hash and enabled verification without
  exposing the secret.

## Dashboard copy and ACLMR palette pass

- Reworked all nine routes in English and French for concise, intuitive,
  posting-accurate language. Chart titles are short, primary analytical pages
  show the data date once in the masthead, and generic repeated caveats moved
  out of chart chrome while chart-specific evidence limits remain.
- Compared the running product with live `aclmr.ca`; primary text on navy now
  uses exact white and secondary copy retains its muted hierarchy.
- Repaired French metadata, chart hover text, labels, and generated narrative
  grammar. The public narrative now uses subject-neutral annual-change frames.
- Next's persistent public fetch cache retained an earlier API narrative during
  QA. Production builds now clear only `.next/cache/fetch-cache` before build,
  and current public HTML was verified after restart.
- Next build `ifCthJv9b7_bykFttVtY1`, ESLint, TypeScript, all 382 Python
  tests, EN desktop plus FR mobile all-route sweeps, and inspected 1440/390 px
  captures passed. Local and public health remained 200 on the existing stack.
- An independent reviewer found generated English labels and English-style
  numbers inside French Plotly figures, plus wrong ACLMR footer expansion and
  misleading active-listing language. All were repaired and rechecked on every
  public route; regression tests now cover the known figure-label leaks.
- A second review found endpoint labels extending two mobile time axes years
  beyond the data. The duplicate labels were removed; both live axes now end at
  March 2026 and the regression is tested.
- Final review also caught mechanically truncated English education labels.
  Curated short labels now render in full, and the independent rereview returned
  `READY` on the public release.

## Measure-first dashboard language

- Replaced the Pulse conclusion sentence with the neutral title “Canadian job
  postings” and removed the automated Summary panel. The page now presents KPIs
  and charts without telling the reader what conclusion to draw.
- Reviewed user-facing language across all nine routes, both locales, Python
  figure factories, Plotly axes/legends/hovers, Explore states, Method,
  Developers, navigation, and footer. Titles name the measure directly;
  reader-facing shorthand such as YoY, MoM, LQ, beta, `n`, P25/P75, and percentage
  points is expanded or replaced with plain language.
- Kept only context that materially changes interpretation: job ads are not
  employment, advertised wages are not paid wages, industry charts use coded
  postings, small samples are gated, shift-share is accounting rather than
  causal, and task exposure is not observed automation.
- Live responsive QA caught comparison labels clipping inside the 390 px French
  KPI cards. The header and delta rows now wrap at mobile widths while retaining
  the desktop one-line layout.
- Release evidence: 383 Python tests, 255 focused figure/Explore tests, ESLint,
  TypeScript, and the Next 16.3 webpack build passed. EN desktop and FR mobile
  route sweeps found no banned narrative copy or page-level overflow; final
  desktop and mobile Pulse captures were inspected. Build
  `Yr_bsL_RHqDGbg96bezak` is healthy locally and through the unchanged public
  tunnel.
- A final adversarial pass returned `READY`. Its only non-blocking observation,
  a truncated French mobile seasonality colorbar title, was shortened to
  `moyenne annuelle` and verified in the final live figure.
