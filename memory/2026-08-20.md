# 2026-08-20

## Explore discovery and local relaunch

- Bruce asked to make the gated Explore surface discoverable without exposing it:
  keep the tab visible and require sign-in after it is opened.
- The Next.js primary navigation, mobile menu, and footer now show Explore to
  signed-out visitors with the existing Team treatment. The page-level `AuthGate`
  remains the sole signed-out password prompt; the competing top-navigation login
  popover was removed. Authenticated sessions retain team status and sign-out.
- Server enforcement did not change. Explore and posting APIs still require a
  verified session, and uncapped chart responses still require `full AND authed`
  and remain private and uncached.
- The production Next.js bundle was rebuilt and restarted locally on
  `127.0.0.1:8522`, with FastAPI on `127.0.0.1:8530`. Persistent public,
  Cloudflare, and ngrok LaunchAgents remain disabled and unloaded.

## Verification

- Desktop and mobile browser checks confirmed the visible Team-tagged Explore
  entry, the page-level gate, no duplicate signed-out login control, no horizontal
  overflow, and no console warnings or errors.
- `PYTHONPATH=src .venv/bin/pytest -q`: `383 passed`.
- `npm run build` in `web/`: passed, including TypeScript and all routes.
- `.venv/bin/jobads-dashboard validate`: `validated: true`; 25,356,735 postings
  reconciled to the canonical upstream source window.
- Anonymous `GET /api/postings` through Next returned `401`; `GET /api/auth`
  reported `authenticated: false` and `configured: true`.
