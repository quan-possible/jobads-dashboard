# 2026-06-29

## Monday public dashboard launch check

- Read the automation memory, project `AGENTS.md`, `README.md`, `MEMORY.md`, latest daily memory files, the dashboard spec, and relevant hosting notes before changing service state.
- Verified `com.aclmr.jobads-dashboard-public` was already running from `/Volumes/ACLMR/jobads-dashboard`: FastAPI listens on `127.0.0.1:8530`, Next listens on `127.0.0.1:8522`, `JOBADS_DASHBOARD_AUTH_REQUIRED=true`, and `JOBADS_DASHBOARD_DATA_ROOT=/Volumes/ACLMR/jobads-dashboard/data/derived/labor_market_dashboard_v1`.
- Local health passed:
  - `http://127.0.0.1:8522/healthz` returned `HTTP/1.1 200 OK` with `{"status":"ok","service":"web"}`.
  - `http://127.0.0.1:8530/health` returned `HTTP/1.1 200 OK` with `{"status":"ok"}`.
  - `http://127.0.0.1:8522/api/auth` returned `HTTP/1.1 200 OK` with auth configured and unauthenticated.
  - `http://127.0.0.1:8522/` returned `HTTP/1.1 200 OK`.
- The old Streamlit route `http://127.0.0.1:8522/_stcore/health` returned `404`, which is expected for the current Next/FastAPI app.
- The previously recorded Cloudflare quick-tunnel URLs were unhealthy: `https://invitation-discount-refuse-nonprofit.trycloudflare.com` and `https://friends-reality-requirements-enemies.trycloudflare.com` no longer resolved.
- `cloudflared` had rotated to quick-tunnel URLs that returned Cloudflare 404 because the LaunchAgent was reading the unrelated default `~/.cloudflared/config.yml` for the `decaude` named tunnel.
- Backed up `/Users/brucenguyen/Library/LaunchAgents/com.aclmr.jobads-dashboard-cloudflared.plist` to `tmp/com.aclmr.jobads-dashboard-cloudflared.plist.20260629-090507.bak`, then added `--config /dev/null`, reloaded the LaunchAgent with `launchctl bootout` / `launchctl bootstrap`, and verified the loaded arguments include `--config /dev/null`.
- Current public URL: `https://forecasts-equilibrium-preference-formats.trycloudflare.com`.
- Public health passed:
  - `https://forecasts-equilibrium-preference-formats.trycloudflare.com/healthz` returned `HTTP/2 200` with `{"status":"ok","service":"web"}`.
  - `https://forecasts-equilibrium-preference-formats.trycloudflare.com/api/auth` returned `HTTP/2 200` with auth configured and unauthenticated.
  - `https://forecasts-equilibrium-preference-formats.trycloudflare.com/` returned `HTTP/2 200`.
- The documented Keychain lookup `security find-generic-password -a jobads-dashboard-public -s jobads-dashboard-public-password -w` still exits `44`, so Andrew was not emailed and no draft was prepared because the required plaintext password source is unavailable under the automation rules.
