# 2026-06-03

## Mac Mini public dashboard recovery

- Recovered the persistent Mac Mini public dashboard at `https://047e-2001-56a-f068-c900-dc77-45fd-2bfb-31da.ngrok-free.app`.
- Confirmed the Mini LaunchAgents for the public Streamlit app and ngrok were running, with the public app on `127.0.0.1:8522` and the ngrok tunnel still pointing at that port.
- Found the actual Explore lookup bug: the dashboard date filter is month-based, but posting lookup was using the selected end month as the first day of that month. That silently excluded most current-month postings, including known March 31 records.
- Fixed posting lookup bounds so the selected end month searches through the last day of that month.
- Added an explicit `Search postings` button in the Specific postings query flow, so users can type a posting ID or text term and run the query without relying on Streamlit's hidden Enter-to-apply behavior.
- Updated the public app copy from `private lookup index` to `bounded lookup index` because the current Mini website is public to anyone with the link.
- Deployed the patched app to `/Users/brucenguyen/jobads-dashboard/src/jobads_dashboard/dashboard/app.py` on `bruces-mac-mini` and restarted `com.aclmr.jobads-dashboard-public`.

## Verification evidence

- Local focused checks: `PYTHONPATH=src pytest -q tests/test_posting_lookup.py tests/test_app_surface.py` passed with `14 passed`.
- Remote focused checks on `bruces-mac-mini`: `PYTHONPATH=src .venv/bin/pytest -q tests/test_posting_lookup.py tests/test_app_surface.py` passed with `14 passed`.
- Mini public health: `curl -fsS http://127.0.0.1:8522/_stcore/health` returned `ok`.
- Public browser check: Explore -> Specific postings -> search `65563604` -> `Search postings` returned the `Server` row and a bounded description excerpt mentioning Big Bear Eatery and Taphouse.

## Full selected-description follow-up

- Expanded `posting_lookup.parquet` with `description_full` while keeping `description_excerpt` for search/list compatibility.
- Updated Explore so the selected posting detail panel shows `Full description` when the enriched lookup column is present, with excerpt fallback for older bundles.
- Kept the table bounded: full text appears only for the selected posting detail, not as a bulk table column or download.
- Rebuilt only the local `posting_lookup.parquet` and copied it to both Mini data paths:
  - `/Users/brucenguyen/jobads-dashboard-public-data/labor_market_dashboard_v1/posting_lookup.parquet`
  - `/Users/brucenguyen/jobads-dashboard/data/derived/labor_market_dashboard_v1/posting_lookup.parquet`
- Verification evidence:
  - Local focused checks with posting lookup, app surface, and refresh-contract tests passed with `22 passed`.
  - Remote focused checks on the Mini passed with `15 passed`.
  - The enriched public lookup file is `105,604,302` bytes.
  - Posting `65563604` has a `2,247` character `description_full`.
  - Public browser check showed `Full description`, not `Description excerpt`, and included responsibilities/qualifications text for posting `65563604`.

## Public password gate

- Added a Streamlit password gate before dashboard data loading so public visitors must authenticate before seeing filters, metrics, Explore, or posting details.
- The gate is controlled by service environment variables:
  - `JOBADS_DASHBOARD_AUTH_REQUIRED=true`
  - `JOBADS_DASHBOARD_PASSWORD_HASH=<pbkdf2_sha256 hash>`
- The Mac Mini public LaunchAgent was updated with auth required and a salted PBKDF2 password hash; the password itself is not stored in repo docs or memory.
- Restarted `com.aclmr.jobads-dashboard-public`; Mini health on `127.0.0.1:8522` returned `ok`.
- Verification evidence:
  - Local focused app/posting tests passed with `16 passed`.
  - Remote focused tests on `bruces-mac-mini` passed with `16 passed`.
  - Public Browser check confirmed the live URL shows a restricted password screen, rejects an incorrect password, unlocks with the correct password, and still supports Explore -> Specific postings for posting `65563604` with full description.
  - Screenshots saved:
    - `docs/analyses/labor_market_dashboard/screenshots/browser-2026-06-03-password-gate.png`
    - `docs/analyses/labor_market_dashboard/screenshots/browser-2026-06-03-password-gated-lookup.png`

## Sidebar icon toggle follow-up

- Replaced the visible sidebar toggle labels with an icon-only chevron control.
- Hid Streamlit's native Material arrow SVG inside the toggle, because it was visually fighting the custom CSS and made the icon look asymmetric.
- Set Streamlit's header layer to ignore pointer events so it no longer intercepts clicks on the fixed filter button.
- Copied the patched `app.py` to `bruces-mac-mini:/Users/brucenguyen/jobads-dashboard/` and restarted both Mini app surfaces:
  - public LaunchAgent on `127.0.0.1:8522`
  - private app behind the local tunnel on `127.0.0.1:8521`
- Verification evidence:
  - `PYTHONPATH=src pytest -q tests/test_app_surface.py` passed with `11 passed`.
  - `python -m py_compile src/jobads_dashboard/dashboard/app.py` passed.
  - Local Browser check on `http://127.0.0.1:8520/` clicked the marked top-left button coordinate and toggled sidebar `true -> false -> true`.
  - Mini Browser check through `http://127.0.0.1:8521/` clicked the same coordinate and toggled sidebar `false -> true -> false`.
  - The local and Mini rendered styles no longer contain visible `Open filters` or `Close filters` text; the native SVG is hidden and the CSS chevron is centered.
  - Local `127.0.0.1:8520`, Mini public `127.0.0.1:8522`, and Mini private `127.0.0.1:8520` health checks returned `ok`.
- Saved the plaintext password in macOS Keychain under service `jobads-dashboard-public-password` and account `jobads-dashboard-public`.
- Added a non-secret `AGENTS.md` reminder with the retrieval command:
  - `security find-generic-password -a jobads-dashboard-public -s jobads-dashboard-public-password -w`
