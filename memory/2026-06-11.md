# 2026-06-11

## External-drive public website launch

- Compared the public website source at `/Users/brucenguyen/jobads-dashboard` and `/Users/brucenguyen/jobads-dashboard-public-data/labor_market_dashboard_v1` against `/Volumes/ACLMR/jobads-dashboard`.
- Found the runtime app file and public data bundle already matched the external-drive copy. The only data discrepancy was a stale zero-byte `tmp_posting_lookup.parquet` on the external drive, moved to `tmp/tmp_posting_lookup.20260611-184823.parquet.bak`.
- Kept the external-drive `prepare.py` behavior because it preserves full posting-description spacing, while the older public-site copy normalizes full descriptions.
- Repointed `/Users/brucenguyen/Library/LaunchAgents/com.aclmr.jobads-dashboard-public.plist` so the public service runs the Streamlit script from `/Volumes/ACLMR/jobads-dashboard/src/jobads_dashboard/streamlit_app.py` and uses `/Volumes/ACLMR/jobads-dashboard/data/derived/labor_market_dashboard_v1` as its data root.
- Preserved the password gate environment. The agent still uses `/Users/brucenguyen/jobads-dashboard/.venv/bin/python` as the Python wrapper because launchd hit `PermissionError: Operation not permitted` when trying to start the external-drive venv directly.

## Verification evidence

- Focused external-drive checks passed: `.venv/bin/python -m pytest -q tests/test_app_surface.py tests/test_posting_lookup.py` returned `16 passed`.
- Local website health passed at `http://127.0.0.1:8522/_stcore/health`.
- LaunchAgent state showed the service running with working directory `/Volumes/ACLMR/jobads-dashboard`, `PYTHONPATH=/Volumes/ACLMR/jobads-dashboard/src`, and external-drive data root.
- Active ngrok tunnel still points to port `8522`, but public requests returned `ERR_NGROK_727`, meaning the ngrok account has reached its monthly HTTP request limit. Public access needs ngrok quota/billing relief or a different tunnel; the dashboard itself is healthy locally.

## Cloudflare public tunnel

- Installed `cloudflared` with Homebrew because ngrok was blocked by the monthly request limit.
- Added `/Users/brucenguyen/Library/LaunchAgents/com.aclmr.jobads-dashboard-cloudflared.plist` so the tunnel survives the current terminal session.
- The Cloudflare quick tunnel points to `http://127.0.0.1:8522`, which is the external-drive dashboard service with the password gate still enabled.
- Current public URL: `https://invitation-discount-refuse-nonprofit.trycloudflare.com`.
- Verification evidence:
  - `curl http://127.0.0.1:8522/_stcore/health` returned `ok`.
  - `curl https://invitation-discount-refuse-nonprofit.trycloudflare.com/_stcore/health` returned `ok`.
  - The public root returned HTTP `200`.
- Caveat: this is an account-less Cloudflare quick tunnel, so it has no uptime guarantee and the URL may rotate if the tunnel restarts. Use a named Cloudflare tunnel or paid/resolved ngrok setup for a stable production URL.
