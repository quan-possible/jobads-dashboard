# 2026-07-06

## Monday public dashboard launch check

- Read the automation memory, project `AGENTS.md`, `README.md`, `MEMORY.md`, latest project daily memory, the dashboard spec, and relevant hosting notes before changing service state.
- Verified `com.aclmr.jobads-dashboard-public` was already running from `/Volumes/ACLMR/jobads-dashboard`: FastAPI listens on `127.0.0.1:8530`, Next listens on `127.0.0.1:8522`, `JOBADS_DASHBOARD_AUTH_REQUIRED=true`, and `JOBADS_DASHBOARD_DATA_ROOT=/Volumes/ACLMR/jobads-dashboard/data/derived/labor_market_dashboard_v1`.
- Verified `com.aclmr.jobads-dashboard-cloudflared` was already running and still passes `--config /dev/null` while tunneling to `http://127.0.0.1:8522`.
- Local health passed:
  - `http://127.0.0.1:8522/healthz` returned `HTTP/1.1 200 OK` with `{"status":"ok","service":"web"}`.
  - `http://127.0.0.1:8530/health` returned `HTTP/1.1 200 OK` with `{"status":"ok"}`.
  - `http://127.0.0.1:8522/api/auth` returned `HTTP/1.1 200 OK` with auth configured and unauthenticated.
  - `http://127.0.0.1:8522/` returned `HTTP/1.1 200 OK`.
- The old Streamlit route `http://127.0.0.1:8522/_stcore/health` returned `404`, which is expected for the current Next/FastAPI app.
- The prompt-recorded Cloudflare quick-tunnel URL `https://invitation-discount-refuse-nonprofit.trycloudflare.com` and previous project URL `https://forecasts-equilibrium-preference-formats.trycloudflare.com` no longer resolved.
- Current public URL recovered from `/Users/brucenguyen/jobads-dashboard-logs/cloudflared-launchd.err.log`: `https://condos-burns-yield-phrase.trycloudflare.com`.
- Public health passed:
  - `https://condos-burns-yield-phrase.trycloudflare.com/healthz` returned `HTTP/2 200` with `{"status":"ok","service":"web"}`.
  - `https://condos-burns-yield-phrase.trycloudflare.com/api/auth` returned `HTTP/2 200` with auth configured and unauthenticated.
  - `https://condos-burns-yield-phrase.trycloudflare.com/api/meta` returned `HTTP/2 200` with the dashboard metadata.
  - `https://condos-burns-yield-phrase.trycloudflare.com/` returned `HTTP/2 200`.
- The documented Keychain lookup `security find-generic-password -a jobads-dashboard-public -s jobads-dashboard-public-password -w` still exits `44`, so Andrew was not emailed and no draft was prepared because the required plaintext password source is unavailable under the automation rules.
