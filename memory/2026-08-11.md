# 2026-08-11

## 04:03 - Project harness migration

- Outcome: Added `STATUS.md` for live orientation and condensed `MEMORY.md` into a bounded, oldest-to-newest historical index.
- Decisions: `AGENTS.md` now routes current state, history, dated evidence, jobs, product contracts, implementation, scratch, and recovery material to distinct owners. `README.md` uses the same routing and directs startup through `STATUS.md` before history.
- Preservation: Backed up `AGENTS.md` and `README.md` under `tmp/` and preserved the exact pre-migration `MEMORY.md` at `archive/memory/versions/MEMORY.md-2026-08-11-040333.md` before rewriting it.
- Current state established: `feat/login-uncapped` has diverged from `origin/main` with 57 feature-only commits and 10 main-only commits, is not contained by a remote branch, and contains the committed 10-category and authenticated-team-view work. The completed cap job and evidence moved to `docs/jobs/done/`; the dependent login job remains active for integration and promotion.
- Verified: The local Next.js and FastAPI health routes, auth status route, both LaunchAgents, and the current Cloudflare quick tunnel were healthy. The tunnel hostname remains ephemeral and must be recovered from the cloudflared log. The documented Keychain lookup still returned exit 44, so password-bearing handoff remains blocked until its credential source is restored or confirmed. A fresh-context adversarial review passed after correcting four documentation defects.
- Limit: No product code, service configuration, deployment, branch, remote, or secret was changed during this migration.

## 04:14 - Dashboard components imported to claude.ai/design

- Outcome: Ran `/design-sync` end to end. All 14 presentational `web/components/` are now a design system at `https://claude.ai/design/p/c9764078-3970-42c2-be34-2e6acce106b9` ("ACLMR Design System"), so the Claude Design agent builds ACLMR UI from the real shipped components instead of generic ones. 43 preview cells across 14 components, every cell graded `good`; `package-validate` exits clean at 14/14. The sync inputs were later committed locally as `a741d8c1`.
- Why it needed scaffolding: `web/` is an application, not a published package — no `dist/`, no exports, no Storybook. Four committed dot-prefixed inputs under `web/` supply what a library build normally gives for free: `.ds-entry.tsx` (public surface + `DsPreviewProvider` app-root stand-in), `.ds-process-shim.ts`, `.ds-tailwind-entry.css`, `tsconfig.ds.json`, plus one `"types"` line in `web/package.json`. Full rationale in `.design-sync/NOTES.md`.
- Decisions: scoped to the 14 presentational components (excluded `RemoteFigure`, `TunableFigure`, `FilterSpine`, `TopNavAuth` and all of `components/explore/` — they need live API, auth, or router state). Widened the compiled Tailwind with an explicit `@source inline` safelist (45 KB → 155 KB) because compiled Tailwind only carries utilities existing source uses, and the design agent writes new markup. Vendored PT Sans (SIL OFL, latin + latin-ext) since `next/font` leaves no font files in the repo.
- **Product bug found, NOT fixed** (`web/components/PixelTiles.tsx`): `pick()` clamps at `Math.min(0.999, …)` before `STOPS[Math.floor(t)]`, so the index is always 0 and every tile renders `#041c2c`. The navy→teal→sand→orange mark has never appeared; on the navy footer it is invisible. Verified by evaluating `pick()` over a 3×8 grid (1 distinct colour; 4 with the ceiling at `STOPS.length - 0.001`) and by full-resolution crop. Affects `Brand`, `TopNav`, `Footer`, `KeyPoints`, `AuthGate`. Left for a separate change — it alters the live brand mark, and golden tests may pin current rendering.
- Two bundler gaps found only by looking at rendered output, both now shimmed: `process` undefined (app reads `process.env` at module scope) killed the whole bundle; `global` undefined (plotly's `has-hover`) silently degraded every chart to RemoteFigure's "unavailable" notice with an empty `pageErrs`.
- Verified: `package-validate` 14/14 clean, driver run all four stages ok, every class/token/component name in `conventions.md` grepped against the built artifacts (caught `rounded-card` missing; added to the safelist rather than dropping the claim), remote `list_files` matches the final build at 86 files with no orphans.
- Limits: no product code, service, deployment, branch, or secret changed. Cards are captured under a frozen clock and an unreachable API, so `Footer` shows `© 2024` and auth affordances render locked; FR locale, authenticated nav, and mobile layouts are unreachable under static capture.

## 15:02 - Harness committed and feature branch published

- Outcome: Committed the harness migration as `f2a11741` and pushed `feat/login-uncapped` to the new tracked remote branch `origin/feat/login-uncapped`.
- Scope: The commit includes the operating-doc migration, preserved pre-condensation memory, dated source records, corrected login-job continuity, and the completed category-cap job moved with its evidence to `docs/jobs/done/`.
- Boundary: The branch remains 10 commits behind `origin/main` and is not merged; no rebase, merge, deployment, pull request, or secret change was performed.

## 15:18 - New design promoted directly to main

- Outcome: Moved local and remote `main` directly to the new Next.js/FastAPI design line at `65661304`; the authenticated uncapped view, public Explore restriction, design-sync inputs, and current harness are now canonical.
- Decision: The 10 commits unique to the prior `main` belonged to the retired Streamlit and temporary worktree line, so they were deliberately not merged. The remote update used `--force-with-lease` against the fetched `3b037d7d` tip.
- Recovery: Preserved the prior `main` tip locally as `backup/main-pre-new-design-20260811-3b037d7d`.
- Verification: The exact promoted code passed 348 Python tests and a production Next.js build, including TypeScript and static page generation. Deployment was not changed; the public bundle still needs to be rebuilt from `main` and verified live.

## 18:04 - Redesign foundation organized and audited

- Outcome: Preserved `Dashboard design exploration 2` at `docs/analyses/labor_market_dashboard/redesign-foundation/` with distinct homes for the selected Pulse and Explore desktop targets, earlier directions, the portable design-system export, the source-state UI kit, provenance, and the ACLMR brand audit.
- Design decision: Kept square geometry for dense analytical controls while documenting rounded pills for primary navigation or access actions, matching the live ACLMR CTA language. The supplied editorial content was retained.
- Portability: Repointed the three design documents from Google Fonts to the vendored PT Sans files and added stable page titles and English document metadata.
- Verification: Rendered the durable Pulse and Explore targets, the revised control specimen, and the source-state UI kit through a local server; visually inspected the assembled desktop surfaces and preserved screenshots beside the package. The custom design-document renderer emits transient SVG placeholder errors while hydrating and loads its runtime from `unpkg.com`.
- Boundary: No file under `web/`, `api/`, or `src/` changed; no service, deployment, branch, remote, or secret changed. The high-fidelity prototypes are desktop-only references with a fixed 1100 px minimum, so mobile targets remain required before production implementation.

## 18:50 - Redesign foundation repaired and adversarially verified

- Outcome: Repaired the durable redesign package after independent review found conflicts between its portable guidance, selected targets, and the live ACLMR language. The package is now ready as desktop redesign groundwork.
- Identity and type: Vendored the exact live ACLMR white logo with checksum provenance, removed the invented pixel-wordmark treatment, kept pixel tiles decorative, and replaced monospace figures in the selected Explore target with PT Sans tabular numerals.
- Components and guidance: Made the portable `TopNav` default to the selected navy redesign, added the live gradient-ring and solid-orange CTA treatments without inventing a universal hierarchy, clarified that the UI kit is comparison-only, and removed the unsupported CVD-safe claim.
- Accessibility and consistency: Added spoken KPI trend direction, corrected MapToggle active contrast to 5.25:1, made the CTA states meet their applicable contrast thresholds, and changed Explore's contradictory “no raw text browsing” line to “no bulk raw-text browsing.”
- Verification: Rendered the Pulse, Explore, and CTA surfaces; refreshed current evidence; parsed all 18 JSX files; resolved local references and CSS tokens; matched the official logo SHA-256; and confirmed no production file changed. A fresh-context adversarial recheck returned READY with no remaining desktop-groundwork defect.
- Boundary: The user explicitly excluded the supplied Pulse hero wording from revision. Mobile composition remains unresolved by design and requires separate targets before implementation. The current site and deployment were not modified.
