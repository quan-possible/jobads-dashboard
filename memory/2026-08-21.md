# 2026-08-21

## Single supported dashboard and publication

- Bruce authorized committing and pushing the current dashboard and removing
  obsolete dashboard versions so future agents see one implementation.
- `main` is the only supported product line: Next.js in `web/`, FastAPI in
  `api/`, Python Plotly factories and aggregate code in `src/jobads_dashboard/`,
  and runtime data in `data/derived/labor_market_dashboard_v1/`. The `_v1`
  suffix is the current aggregate-schema name, not an app version.
- The canonical product contract and private runbook were rewritten around that
  topology. Retired Streamlit commands, the stale `redesign2` launch target,
  historical UI captures, and old-version wording in current code comments were
  removed.
- Completed and superseded June-August jobs moved out of `docs/jobs/active/`.
  Their evidence remains under `docs/jobs/done/` or `docs/jobs/archive/`, while
  Git history remains the recovery path for deleted implementations.
- All obsolete local feature, audit, redesign, and backup refs were reconciled;
  every branch except the deliberately retired pre-Next backup was already
  contained in `main`. The completed redesign worktree and obsolete remote
  branches were removed after publication.
- A regenerated, untracked `geography_top_markets.parquet` file was removed.
  Commit `09733372` had already retired that dead aggregate; the current
  geography path uses `monthly_by_market.parquet` and validates without it.
- Before cleanup, the current local Next.js `/healthz` and FastAPI `/health`
  routes both returned `200`. Final verification is recorded in the cleanup
  commit and task close-out.
- Public tunnels remain disabled and the old Render service remains suspended;
  repository publication did not authorize or perform a public relaunch.
