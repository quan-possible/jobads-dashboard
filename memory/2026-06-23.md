# 2026-06-23

## Tuesday public dashboard readiness

- Monday 2026-06-22 launch-check memory showed the dashboard healthy at `https://invitation-discount-refuse-nonprofit.trycloudflare.com`, but the Tuesday check found that URL no longer resolved.
- The public LaunchAgent was still trying to run the removed Streamlit entrypoint `src/jobads_dashboard/streamlit_app.py`, while an unmanaged Next process was temporarily occupying `127.0.0.1:8522`.
- Backed up the LaunchAgent to `tmp/com.aclmr.jobads-dashboard-public.plist.20260623-080407.bak`, then repaired `/Users/brucenguyen/Library/LaunchAgents/com.aclmr.jobads-dashboard-public.plist` for the current two-process app: FastAPI on `127.0.0.1:8530` from `/Volumes/ACLMR/jobads-dashboard/.venv`, and Next on `127.0.0.1:8522`.
- Verified `com.aclmr.jobads-dashboard-public` is running under launchd with listening PIDs on ports `8522` and `8530`. Verified `com.aclmr.jobads-dashboard-cloudflared` remains pointed at `http://127.0.0.1:8522`.
- Current public URL from cloudflared logs: `https://friends-reality-requirements-enemies.trycloudflare.com`.
- Verification evidence:
  - `curl -i http://127.0.0.1:8522/healthz` returned `HTTP/1.1 200 OK` with `{"status":"ok","service":"web"}`.
  - `curl -i http://127.0.0.1:8530/health` returned `HTTP/1.1 200 OK` with `{"status":"ok"}`.
  - `curl -i http://127.0.0.1:8522/api/auth` returned `HTTP/1.1 200 OK` with auth configured and unauthenticated.
  - `curl -i https://friends-reality-requirements-enemies.trycloudflare.com/healthz` returned `HTTP/2 200` with `{"status":"ok","service":"web"}`.
  - Public root `https://friends-reality-requirements-enemies.trycloudflare.com/` returned `HTTP/2 200`.
- The old Streamlit health route `/_stcore/health` now returns `404` locally/publicly because this checkout is Next/FastAPI, not Streamlit.
- Andrew was not emailed during the check: the URL changed, but the project-documented Keychain lookup `security find-generic-password -a jobads-dashboard-public -s jobads-dashboard-public-password -w` still exits `44`. Per the automation prompt, that blocks sending the password until Bruce supplies the password or confirms the corrected credential source.
