# 2026-06-02

## Private query dashboard path

- Added an aggregate-only `Explore` tab to the existing Streamlit dashboard so a supervisor can ask common curated questions from the local derived bundle.
- The first query surface covers posting trends, top local markets, top occupation groups, top industry groups, advertised wage coverage, and field coverage.
- The tab is intentionally not arbitrary SQL and does not expose raw postings, raw text, or bulk downloads.
- Extended the plan to support specific posting lookup through a private `posting_lookup.parquet` index. The app queries this index lazily and shows bounded description excerpts instead of a bulk raw-text export.
- Added `docs/analyses/private_query_dashboard/runbook.md` with the Mac mini deployment path:
  - mirror the repo to `/Volumes/ACLMR/jobads-dashboard`
  - keep it beside `/Volumes/ACLMR/jobads-data/main`
  - run the app on local port `8520`
  - let the supervisor connect through Tailscale or an SSH tunnel
- This preserves the existing project boundary: `jobads-dashboard` owns the dashboard/query app, `jobads-data/main` owns processed upstream data, and `ai-labor` should export stable derived inputs rather than becoming a runtime app dependency.

## Browser UI polish pass

- Polished the ACLMR-style Streamlit shell after live interaction revealed the collapsed left sidebar could not reliably be reopened.
- Fixed the sidebar control states so the collapsed app shows a single `Open filters` control and the expanded drawer shows the close control without stale button overlap.
- Removed deprecated Plotly container-width usage from the app and made the private posting lookup tolerate a missing or zero-byte `posting_lookup.parquet` rather than failing during a partial build.
- Updated the app surface tests to select controls by label instead of brittle Streamlit widget indexes, which became important after the Explore tab introduced its own selector.
- Verified with the in-app Browser by opening and closing the filter drawer, changing geography, occupation, industry, and date-range filters, switching Explore to `Top local markets`, and visiting Data Quality.
- Saved Browser screenshots at `docs/analyses/labor_market_dashboard/screenshots/browser-final-dashboard.png` and `docs/analyses/labor_market_dashboard/screenshots/browser-final-filters-open.png`.
- Current checks for the pass: `PYTHONPATH=src pytest -q` passed with `25 passed`; local `http://127.0.0.1:8520/_stcore/health` returned `ok`.
- The old ngrok tunnel was no longer active, so the page was redeployed through `https://2b0e-2001-569-5046-4801-f08a-3b33-3407-3627.ngrok-free.app` with `/_stcore/health` returning `ok`.

## Low-impact sidebar verification follow-up

- Switched the remaining polish work into low-impact mode because upstream querying and data-building make the local machine laggy.
- Avoided `jobads-dashboard refresh`, posting-lookup builds, and upstream scans; used the existing local derived aggregate bundle only.
- Found and fixed a second sidebar trap: at desktop width, reopening filters after the main page had been scrolled could show only the `Filters` pill while the actual controls were off-screen.
- Made the sidebar a fixed drawer whenever open, so the date range and filter controls are visible immediately at desktop, tablet, and mobile widths.
- Replaced the clipped Streamlit slider value bubble with an explicit ACLMR-style selected range pill in the sidebar.
- Tightened page overflow handling so the app hides horizontal overflow while preserving normal vertical page scroll.
- Browser viewport checks covered `1440x1000`, `1024x768`, `768x900`, and `390x844`; each closed state exposed a reopen control and each reopened state exposed the filter controls.
- Explore was rechecked after the patch by switching the question selector to `Top local markets`.
- Saved screenshots at `docs/analyses/labor_market_dashboard/screenshots/browser-low-impact-desktop-filters-open.png`, `browser-low-impact-mobile-filters-open.png`, and `browser-low-impact-explore-top-markets.png`.
- Current checks: `PYTHONPATH=src pytest -q` passed with `25 passed`; local and ngrok `/_stcore/health` returned `ok`.
- Current live URL: `https://ee39-2001-569-5046-4801-f08a-3b33-3407-3627.ngrok-free.app`.

## Final low-impact Browser polish pass

- Continued in low-impact mode: no `jobads-dashboard refresh`, no posting-lookup build, and no upstream processed-corpus scan.
- Used the in-app Browser only, without Playwright, for interaction and viewport checks.
- Rebalanced the tab ribbon from an awkward orphan final tab into a polished wrapped grid: four columns on desktop, three on tablet, two on mobile.
- Hid Streamlit's default tab underline so the custom ACLMR pill state is the only selected-tab treatment.
- Applied the existing compact occupation and industry labels to the sidebar selectors so selected values fit better in the drawer while preserving the underlying filter values.
- Browser walkthrough covered desktop sidebar collapse/reopen, mobile drawer open/close, date range change, geography/occupation/industry filter changes, every tab, and Explore selector switching to `Top local markets`.
- Browser viewport checks covered `1440x1000`, `1024x768`, `768x900`, and `390x844`; the mobile horizontal-scroll attempt produced no screenshot movement.
- Saved fresh screenshots under `docs/analyses/labor_market_dashboard/screenshots/` with the `browser-final-*` and `browser-final-responsive-*` prefixes.
- Current checks: `PYTHONPATH=src pytest -q` passed with `25 passed`; local and ngrok `/_stcore/health` returned `ok`.
- Current live URL: `https://d04f-2001-569-5046-4801-c86f-a223-3a1e-dd7c.ngrok-free.app`.

## Latest data refresh

- Refreshed the local dashboard aggregate bundle after upstream processed data advanced beyond the old March bundle.
- Fixed the aggregate source glob from `20*/processed_*.parquet` to `20[0-9][0-9]/processed_*.parquet` so refresh and validation use canonical year folders only and ignore backup/sample-like directories such as `2025.previous_...`.
- Rebuilt `data/derived/labor_market_dashboard_v1/` from `../jobads-data/main/data/processed/<year>/processed_*.parquet`.
- New bundle metadata: 164 source parquet files, source window `2016-01-01` through `2026-03-31`, and 25,356,735 postings.
- Validation reconciled metadata, local `monthly_overall`, and the live upstream source at 25,356,735 postings with matching source window and no missing/schema issues.
- `PYTHONPATH=src pytest -q` passed with `26 passed`.
- Streamlit app-harness check rendered the refreshed bundle with no exceptions or app errors; headline metrics included 25,356,735 postings and latest refresh month `2026-03`.
- The local app is running on `http://127.0.0.1:8520` in tmux session `jobads-dashboard`.
- The old ngrok URL was inactive, so the tunnel rotated to `https://1c4b-2001-569-5046-4801-c86f-a223-3a1e-dd7c.ngrok-free.app`; local and tunnel `/_stcore/health` returned `ok`.

## Sidebar dropdown polish

- Fixed clipped sidebar dropdown labels by matching the styled select height to Streamlit's 64px internal text layer.
- Browser checks confirmed the Geography, Occupation Group, and Industry Group dropdown text now stays inside the visible boxes.
- The same check confirmed the old internal-facing wording such as `surface`, `redesigned`, `provenance`, and `endpoint` is absent from the live page.
- The local app is running on `http://127.0.0.1:8520` in tmux session `jobads-dashboard`.
- The old ngrok URL was inactive again, so the tunnel rotated to `https://d5e4-162-156-88-90.ngrok-free.app`; local and tunnel `/_stcore/health` returned `ok`.

## Sidebar toggle and slider labels

- Made the sidebar open and close controls use fixed visible hit areas in both states, with only the active wrapper clickable.
- Restored date-range month labels above the two slider handles and kept the lower duplicate tick labels hidden.
- Browser DOM checks verified a stable open state over several seconds, a single visible `Close filters` control when open, a single visible `Open filters` control when collapsed, and visible `2016-01` / `2026-03` labels above the handles.
- Current checks: `PYTHONPATH=src pytest -q tests/test_app_surface.py` passed with `10 passed`; local and tunnel `/_stcore/health` returned `ok`.

## Sidebar reflow fix

- Fixed the desktop layout so opening the filters sidebar reserves sidebar width and narrows the main dashboard instead of floating over it.
- Browser checks at the 1040px review viewport confirmed the main dashboard changes from full-width collapsed layout to `left: 352px` and `width: 682px` when filters are open.
- The mobile breakpoint keeps the drawer overlay behavior so small screens do not squeeze the dashboard into an unusably narrow column.
- Current checks: `PYTHONPATH=src pytest -q tests/test_app_surface.py` passed with `10 passed`; local `/_stcore/health` returned `ok`.

## Date range control cleanup

- Replaced the cramped Streamlit date slider label/value layout with a custom `Date range` header and two selected-month pills above a simple rail.
- Hid Streamlit's built-in slider label, thumb values, and tick labels so the control no longer repeats `2016-01` and `2026-03` inside the box.
- Browser DOM checks confirmed the custom header is visible, the slider rail and handles remain visible, and the built-in slider text elements are hidden.
- Current checks: `PYTHONPATH=src pytest -q tests/test_app_surface.py` passed with `10 passed`; local `/_stcore/health` returned `ok`.

## Mac Mini private hosting

- Copied the current `jobads-dashboard` working tree to `bruces-mac-mini:/Users/brucenguyen/jobads-dashboard/` with `rsync`, excluding virtualenv/cache/log-only paths.
- The Mac Mini did not have `/Volumes/ACLMR` mounted, so the deployment uses the copied derived dashboard bundle instead of rebuilding from upstream there.
- Installed the remote app with `/usr/local/bin/python3.12` because the Mini's system Python is `3.9.6` and the package requires Python `>=3.10`.
- Launched the remote Streamlit app bound to `127.0.0.1:8520`; process details and the runtime log are on the Mini under `/Users/brucenguyen/jobads-dashboard-logs/`.
- Opened a local SSH tunnel from `http://127.0.0.1:8521` to the Mini's `127.0.0.1:8520`.
- Verification evidence: remote focused tests passed with `13 passed`; remote `http://127.0.0.1:8520/_stcore/health` returned `ok`; local tunnel `http://127.0.0.1:8521/_stcore/health` returned `ok`; both remote and tunneled root pages returned HTTP `200`.
- Remaining limitation: remote refresh, live-source validation, and posting-lookup rebuilds need `jobads-data/main` or the ACLMR drive available on the Mini.

## Public website deployment

- Pushed the refreshed dashboard bundle and app updates to both `origin/codex/render-hosting` and `origin/main` for Render auto-deploy.
- Added `.gitignore` and `.dockerignore` guards so the private `data/derived/labor_market_dashboard_v1/posting_lookup.parquet` file stays out of Git and the public Docker image.
- Live browser verification showed the public site was serving Streamlit but displaying the missing-bundle panel because the Docker container launched `streamlit_app.py` directly after package install, causing the app to resolve its default data root under `/usr/local/lib/python3.12`.
- Fixed the Docker entrypoint to run `jobads-dashboard app --output-root /app/data/derived/labor_market_dashboard_v1` and set `JOBADS_DASHBOARD_DATA_ROOT` to the same path.
- Verification before pushing the Docker fix: `PYTHONPATH=src pytest -q tests/test_cli.py tests/test_app_surface.py tests/test_refresh_contract.py` passed with `21 passed`.
- Render still served the old missing-bundle container after the push, and the local Render CLI token was expired, so a forced Render redeploy was not available from this shell.
- Switched the existing project ngrok URL to a public-safe local Streamlit app on port `8520`, backed by `tmp/public-web-data/labor_market_dashboard_v1/` with `posting_lookup.parquet` excluded.
- Public-safe ngrok URL: `https://d5e4-162-156-88-90.ngrok-free.app`.
- Browser verification with the ngrok warning bypass header showed the live dashboard content, including `25,356,735` postings, latest month `2026-03`, Overview tab content, and no missing-bundle error.

## Persistent Mac Mini website service

- Moved the public-safe web hosting target from the local session to `bruces-mac-mini`.
- Kept the original private Mac Mini app on port `8520` and created a separate public-safe app on port `8522` backed by `/Users/brucenguyen/jobads-dashboard-public-data/labor_market_dashboard_v1`.
- Copied the local authenticated ngrok binary/config to the Mini without printing the token.
- Installed three user LaunchAgents on the Mini:
  - `com.aclmr.jobads-dashboard-public`
  - `com.aclmr.jobads-dashboard-ngrok`
  - `com.aclmr.jobads-dashboard-caffeinate`
- The `caffeinate` agent runs `/usr/bin/caffeinate -dimsu` so the Mini should not sleep while the service is intended to stay online.
- Current Mac Mini public URL: `https://047e-2001-56a-f068-c900-dc77-45fd-2bfb-31da.ngrok-free.app`.
- Verification evidence: Mini app health on `127.0.0.1:8522` returned `ok`; Mini ngrok API returned the public URL; browser verification through the URL showed dashboard content with `25,356,735` postings and latest month `2026-03`, with no missing-bundle error.
- Durability limit: user LaunchAgents persist across this Codex session closing and restart if the user session stays active, but free ngrok URLs can rotate after tunnel restarts unless a reserved ngrok domain is configured.
