# 2026-08-13

## Public dashboard takedown

- Bruce asked to take the dashboard down. Disabled and unloaded the persistent
  `com.aclmr.jobads-dashboard-public`,
  `com.aclmr.jobads-dashboard-cloudflared`, and
  `com.aclmr.jobads-dashboard-ngrok` LaunchAgents.
- Verified that no dashboard process or listener remained on local ports `8522`
  or `8530`. The former Cloudflare URL returned `530`; the former ngrok URL
  returned `404`. These disabled states persist across login until explicitly
  re-enabled.
- A separate historical Streamlit service remains reachable at
  `https://jobads-dashboard.onrender.com`. The local Render CLI token is expired
  and no matching Keychain credential was found, so that remote service could
  not be suspended from this machine.
- Exact-host web searches found no indexed result for either tunnel hostname and
  no matching ACLMR site result. The ngrok inspector retained one `curl`
  `/healthz` request and no dashboard page view. Cloudflare's default log does
  not retain successful-request access history, so available evidence cannot
  identify or count external visitors and does not establish that anyone
  outside the project viewed the redesigned dashboard.
