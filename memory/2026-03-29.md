# 2026-03-29

## Progress highlights
- Added first-class Render deployment support to `jobads-dashboard`.
- Updated the Docker image startup command to honor Render's dynamic `PORT` instead of a fixed local-only port.
- Added a root `render.yaml` blueprint for a Docker-based Render web service with `/_stcore/health` as the health check.
- Pushed the deployment changes to GitHub branch `codex/render-hosting` without including the unrelated in-progress local `app.py` edits.
- Created the Render web service successfully and brought the dashboard live at `https://jobads-dashboard.onrender.com`.

## Key decisions
- Use Render's free web-service tier for the initial durable host to avoid introducing a paid service without an explicit request.
- Keep the service pointed at `codex/render-hosting` for now because the deployment fixes were not merged into `main` during this task.
- Keep the app self-contained on Render by continuing to bundle the local derived parquet package inside the Docker image.

## Files touched
- `Dockerfile`
- `render.yaml`
- `README.md`
- `MEMORY.md`
- `memory/2026-03-29.md`

## Verification evidence
- Local Render-style entrypoint boot on port `8533` -> `/_stcore/health = ok`
- `render blueprints validate render.yaml` -> `valid: true`
- `git push -u origin codex/render-hosting` -> success
- `render services create ...` -> created service `srv-d74gjbc50q8c73dvqfdg`
- Render deploy logs -> Docker build, image push, and Streamlit startup completed successfully
- `https://jobads-dashboard.onrender.com/_stcore/health` -> `ok`
- `https://jobads-dashboard.onrender.com/` -> `200 OK`

## Remaining risks
- The Render service is on the free tier, so it can still idle out and incur cold starts after inactivity.
- The live Render service currently tracks branch `codex/render-hosting`, so deployment continuity depends on that branch until the changes are merged or the service branch is updated.
- The unrelated local changes in `src/jobads_dashboard/dashboard/app.py` and `docs/jobs/ui-polish-ongoing.md` were intentionally left untouched and are not part of the hosted deployment.
