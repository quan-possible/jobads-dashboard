# 2026-08-18

## Historical Render service suspended

- Bruce confirmed that the remaining historical dashboard should be taken
  down and asked to use Chrome.
- Signed into Render through the authorized Chrome session, opened web service
  `srv-d74gjbc50q8c73dvqfdg` (`jobads-dashboard`), and chose the reversible
  **Suspend Web Service** action. Render confirmed “Suspended by you.” The
  service was not deleted.
- Verified `https://jobads-dashboard.onrender.com/` and `/healthz` both return
  `503`. The former Cloudflare and ngrok URLs remain unavailable, local ports
  `8522` and `8530` remain closed, and their LaunchAgents remain disabled.
- All known public dashboard endpoints are now offline. Relaunch requires an
  explicit decision to resume Render or re-enable the local service and a
  tunnel.
